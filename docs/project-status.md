0.108.8 Shared Shipment Place Inputs and Yandex Manual Attach Texts: the shared shipment modal now renders factual place `weight_g`, `length_cm`, `width_cm` and `height_cm` inputs empty for every carrier on initial open. Calculated package/order dimensions are never written into editable inputs; submitted `places[]` remain the only source for preview/create, so empty fields stay invalid and block preview/create through existing strict validation. Existing draft places, place numbers, `shipment_items[]`, allocation and split quantities are preserved, including multi-place drafts. For a single initial place the modal shows the calculated weight only as the hint `Расчётный вес товаров: %d г`; for multiple places it can show the total hint above the list and never copies it into each input. Yandex manual attach presentation now uses the button `Ввести номер Яндекс вручную`, field label `Request ID Яндекс`, and placeholder `***-udp`; the placeholder is not an input value and the request/info attach business flow is unchanged. Version is 0.108.8. No Yandex API/HTTP, status/cancel/reconciliation, persistence, checkout/pricing/Packaging or DPD/CDEK/Russian Post payload logic was changed.
0.108.7 Yandex Manual Attach: Yandex button policy now allows the existing generic manual attach UI when no local shipment exists. Manual attach interprets the generic input as Yandex `request_id`, calls only `request/info?slim=false`, verifies `request.info.operator_request_id` against the WooCommerce order number, persists canonical request/info through `YandexShipmentPersistenceMapper`/`YandexShipmentRepository`, syncs `_wdc_yandex_delivery_request_id`, and hides create/manual attach afterwards. Empty submitted `places[]` remain invalid and are mapped to Russian validation messages before any Yandex HTTP. Offers/create, offers/confirm, request/create, retry, new modal/metabox/AJAX/storage and checkout/pricing/Packaging or DPD/CDEK/Russian Post business logic were not changed. Version is 0.108.7.
0.108.6 Shipment Modal Preview Gate and Russian UI Messages: Yandex and DPD shipment drafts now expose carrier-neutral `requires_successful_preview`, and shared admin JS disables `Создать отправление` until preview has loaded successfully and has no errors. `ajax_create()` now has the same JSON-safe boundary as preview: validation errors return JSON with deterministic codes, unexpected throwables are logged and return a controlled Russian JSON error, and HTML critical-error output is not exposed. Runtime shipment AJAX parsing uses the shared safe JSON parser instead of direct `response.json()`, so malformed HTML responses no longer surface `Unexpected token`. User-facing shipment modal/create/preview fallbacks were Russianized, including allocation validation messages mapped at the AJAX boundary. Version is 0.108.6. JSON contract and Yandex HTTP flow were not changed.
0.108.5 Yandex Shipment Modal Preparation: the existing shared shipment modal now receives a concrete Yandex service variant from `OrderShipmentDraftFactory::draft_array()` (`Яндекс до ПВЗ` or `Яндекс до двери`, matching the saved order delivery type) instead of an empty services list. For `yandex_delivery` the modal no longer renders the generic tariff select/warning or Russian Post postoffice select; it shows a read-only source platform station, pickup destination snapshot or courier address fields, and ready interval hidden values. Initial parcel places are rendered from the draft instead of one empty row, dimensions use decimal-compatible inputs while server-side mapper still rounds upward, and weight remains integer. Shipment preview remains local-only and does not call Yandex HTTP; preview AJAX now returns controlled JSON errors for validation and unexpected throwables, and admin JS parses malformed non-JSON responses without exposing `Unexpected token` or raw HTML. Version is 0.108.5. No new modal, metabox, Yandex-specific AJAX lifecycle, HTTP layer, retry, request/create, checkout/pricing/pickup-map or auto-registration was added.
0.108.4 Shipment Metabox Capability Policy: the shared `OrderShipmentsMetabox` now delegates button visibility to the carrier adapter status payload whenever capability keys are present (`has_shipment`, `can_create`, `can_attach_manual`, `can_update_status`, `can_cancel`, `can_remove_from_order`) and keeps legacy fallbacks only for carriers/paths that do not expose a key. Yandex intermediate states `reconciliation_required` and `cancellation_started` are therefore treated as existing shipments: create/manual/cancel/remove are hidden and status update remains available; terminal `CANCELLED` hides cancel and allows remove according to adapter policy. `ShipmentModalRequestMapper` now parses dimensions as decimal centimeters and rounds them upward, while weight remains a strict integer-grams contract; invalid values still fail existing allocation validation. The CDEK assessed-cost ambiguity was documented as technical debt without changing `CdekShipmentAllocationAdapter` or CDEK payload semantics. Version is 0.108.4. No new UI, HTTP layer, retry, request/create, persistence model, checkout/pricing/Packaging or DPD/CDEK/Russian Post business logic changes were added.
0.108.3 Shipment Modal Contract and Yandex Persistence Mapper: the shared shipment modal now uses canonical `shipment_items[]` for item allocation rows, while CDEK temporarily keeps a server-side `cdek_items[]` fallback only as migration debt. `ShipmentModalRequestMapper` and `ShipmentPreparationData` parse submitted `places[]` plus item rows once for CDEK and Yandex, then reuse the existing `CdekShipmentAllocationAdapter`/`ShipmentAllocation` validation so broken allocation is rejected instead of silently repaired. Generic admin JS functions/selectors were renamed from CDEK-specific names to shipment-neutral names and old common CDEK data aliases were removed; CDEK-specific label/status UI hooks remain carrier-specific. The empty `ShipmentCarrierAdapterInterface` alias was deleted in favor of canonical `CarrierShipmentAdapterInterface`. Yandex-specific persistence fields moved from `ShipmentCreationService` into `YandexShipmentPersistenceMapper`, including canonical request/info snapshots, reconciliation pending fields and lookup meta sync through `YandexShipmentRepository`; DPD/CDEK/Russian Post persistence remains in `ShipmentCreationService` as documented technical debt. Version is 0.108.3. No new UI, Shipment Framework, HTTP endpoint, retry, request/create, checkout/pricing/Packaging or DPD/CDEK/Russian Post business logic changes were added.
0.108.2 Yandex Shipment Framework Final Registration Hardening: the Yandex draft allocation source was re-audited against the existing CDEK/modal production flow. The canonical multi-place source is the submitted shipment modal data (`places[]` and `cdek_items[]`) parsed by `OrderShipmentDraftFactory::create_*_request_from_admin_data()` into `ShipmentCreateRequest::places` plus carrier item rows; no production code writes the previously probed `_wdc_shipment_places`/`_wdc_cdek_shipment_places`/`_wdc_prepared_shipment_places` keys, so those invented order-meta searches were removed. Initial Yandex draft now mirrors CDEK and starts one-place until modal admin data is submitted; submitted modal rows preserve split quantity and same-SKU/different-item identity. Yandex item row parsing no longer silently repairs broken allocation values: amount/place/weight/quantity are passed through and the existing `CdekShipmentAllocationAdapter`/`ShipmentAllocation` validation rejects damaged rows before HTTP. Cancel lifecycle now uses the same terminal status helper as `YandexShipmentButtonPolicy`; pending cancel is closed not only by `CANCELLED`, but also by `DELIVERED`, `RETURNED`, `RETURNED_TO_SENDER` and `REJECTED`, without writing a false “cancelled” note for non-CANCELLED terminal statuses. Version is 0.108.2. No new UI, framework, HTTP endpoint, retry, request/create, checkout/pricing/Packaging or DPD/CDEK/Russian Post business logic changes were added.
0.108.1 Yandex Shipment Framework Lifecycle Hardening: Yandex shipment registration now safely persists a `reconciliation_required` shipment when `offers/confirm` succeeds but mandatory `request/info` fails. The confirmed `request_id` is saved as `external_id`/`tracking_number`/`request_id`/`yandex_request_id`, lookup meta `_wdc_yandex_delivery_request_id` is written immediately, create is hidden by the existing duplicate guard, and the only active action is status update; no confirm retry is attempted. Status update reconciles the shipment through `request/info`, clears reconciliation flags, restores canonical request/info snapshots, barcodes, destination/items/places and keeps selected offer audit fields including `yandex_offer_expires_at`. Yandex cancel now treats `request/cancel` as asynchronous: `cancellation_started` is persisted before follow-up `request/info`; repeat cancel/remove are blocked while cancellation is pending, and a failed post-cancel info call still leaves an accepted local state for later reconciliation. `OrderShipmentDraftFactory` reuses existing prepared/shared/CDEK shipment places and item rows for Yandex drafts before falling back to one place, preserving split quantity and same-SKU/different-item identity. API lifecycle request_id resolution now uses only `yandex_request_id`, `request_id`, then `external_id`, never courier `tracking_number`. No new metabox/modal/AJAX/retry/request-create architecture was added; checkout, pricing, Packaging, DPD/CDEK/Russian Post business logic were not changed.
0.108.0 Yandex Shipment Framework Integration: Яндекс.Доставка подключена к существующему Shipment Framework как carrier-specific adapter, без новой системы регистрации. Новый `YandexShipmentAdapter` зарегистрирован в `CarrierShipmentAdapterRegistry` рядом с DPD/CDEK/Почтой и вызывается общим `ShipmentCreationService`; `OrderShipmentDraftFactory` теперь формирует `ShipmentCreateRequest` для carrier `yandex_delivery` с готовым `ShipmentAllocation` на основе существующих shipment places/item rows. Регистрация идёт через уже подготовленный `YandexDeliveryShipmentRegistrationService` и HTTP client: payload builder -> offers/create -> earliest offer -> confirm -> mandatory request/info. Сохраняется canonical request/info state в `_wdc_shipments[yandex_delivery]`: `request_id`, `courier_order_id`, `sharing_url`, статус, `operator_request_id`, selected offer, delivery policy, destination/recipient/items/places snapshots, temporary-to-real barcode map and full request/info snapshot. Offers/create payload body intentionally is not persisted. Existing metabox/modal/lifecycle/repository/button flow is reused; Yandex button policy hides manual attach, exposes create/status/cancel/remove states, and duplicate create is blocked by `OrderShipmentRepository::has_created_for_carrier()`. Checkout, pricing, pickup map, Packaging algorithm and DPD/CDEK/Russian Post business logic were not changed.
0.105.11 Yandex Order Recalculation Admin Pickup Bounds Lifecycle: the order-admin pickup picker now binds viewport bounds to the loaded pickup dataset generation. Initial provider bounds emitted before points load, such as the fallback Novosibirsk viewport, are invalidated when a new city dataset (for example Moscow) arrives, so stale bounds cannot produce a false empty side list. After `renderMarkers()` and camera actions (`setCenter`, `focusPoint`, `fitToMarkers`) the admin picker performs a deferred read-only provider bounds sync via the new `getBounds()` method on both Yandex and Leaflet providers; bounds callbacks and `getBounds()` share the same `west,south,east,north` converter. Selected pickup/location coordinates are used as the initial map center before the fallback center. Pan/zoom remains local: it does not call REST, does not rerender the full marker dataset, and does not clear the selected point. Backend pickup search, location resolution, checkout picker filtering, clustering, pricing, save, Rule Engine, Packaging and DPD/CDEK/Russian Post business logic were not changed.
0.105.10 Yandex Order Recalculation Admin Pickup Viewport UX: the order-admin pickup picker now keeps the full loaded pickup dataset on the map/provider for markers and clustering, but filters the side list locally to points inside the current map viewport (`west,south,east,north` bounds). Pan/zoom only rerenders the list and status count (`Показано X из Y ПВЗ`) and does not call REST or reset the selected pickup; if no loaded point is visible, the list shows the dedicated viewport-empty message instead of saying the city has no pickup points. Side-card `presentation_comment` now renders in a vertical heading block immediately under the title and before the address, while popup presentation, hidden Yandex station id behavior, backend pickup search, pricing, save, location resolution, checkout picker/providers, clustering, Rule Engine, Packaging and DPD/CDEK/Russian Post business logic were not changed.
0.105.9 Yandex Order Recalculation Admin Pickup Map: order-admin preview now returns the read-only resolved numeric location in the preview `location` payload as both `id` and `location_id`, without treating it as an explicit override and without writing order meta. The admin JS syncs that resolved id into `selectedLocations` after the first preview while preserving the fuller current/selected location payload, so opening the Yandex pickup picker for the original order city sends a usable numeric location immediately. The Yandex pickup search endpoint also has a server-side fallback through `OrderDeliveryRecalculationService::resolved_location_payload()` for old/incomplete JS payloads. Admin Yandex popup/list cards now use checkout formatter presentation fields (`point_title`/`card_title`/`display_title`, `presentation_comment`) and hide the technical station id visually while keeping `platform_station_id`/`point_code` in the payload for identity, pricing and save. Pricing, saving, location lookup, checkout picker, representative selection, courier fallback, Rule Engine, packaging and DPD/CDEK/Russian Post business logic were not changed.
0.105.8 Yandex Delivery Calculation Persistence Fixes: checkout and order-admin replacement now persist Yandex API base price separately from the final rule-adjusted price. `api_base_price_rub` is canonical; `pricing_total_kopecks / 100` is the Yandex fallback, then safe `Money::to_array()`/`original_cost` extraction, and final cost is used only last. `change_delivery_days` audit entries are preserved in `formula_visualization` for both checkout-created orders and admin replacement, so 535 RUB API / 662 RUB final formulas start at 535 and end at 662 while showing the delivery-days rule. Admin method titles replace an original suffix like `8 дней` with the final `10 дней` without duplicating terms, and Money-array `original_cost` is no longer cast to float. Location resolution, Yandex pricing payload, representative selection, courier fallback, pickup map, checkout sorting, pipeline/import/mapping, Packaging and DPD/CDEK/Russian Post business logic were not changed.
0.105.7 Yandex Order Recalculation Location Resolution Regression: order-admin recalculation smoke now proves the read-only `OrderQuoteRequestMapper` location recovery path with an injected `LocationRepository`. Exact `fias_id` wins first, `city_fias_id` is accepted only when the repository finds exactly one active location, duplicate `city_fias_id` returns no location instead of choosing a random row, and GAR remains the exact numeric fallback. A first-preview regression verifies that an order with only FIAS meta receives numeric `location_id=92468`, returns `yandex_pickup` with `pickup_source=representative`, and does not mutate order meta. Production lookup, Yandex pricing, representative selection, courier fallback, checkout, DPD, CDEK, Russian Post, Rule Engine and packaging were not changed.
0.105.1 Yandex Delivery Order Recalculation Review Fixes: admin save now accepts Yandex pickup only when its carrier/family are canonical and a platform_station_id can be resolved from the pickup or its snapshot; point_code is then forced to exactly that station id. The existing full local Yandex PVZ list now supports case-insensitive admin address/title/station-id search after loading, without a new query or changed geo-id resolution. Checkout, pricing, representative selection, courier fallback, map runtime, DPD, CDEK, Russian Post, Rule Engine and packaging were not changed.
0.105.0 Yandex Delivery Order Recalculation: the WooCommerce order-admin delivery calculator now receives Yandex pickup and courier rates through the existing OrderQuoteRequestMapper -> CheckoutOrchestrator -> YandexDeliveryCarrier -> YandexDeliveryPricingRequestBuilder -> pricing-calculator runtime. The common pickup map loads locally mapped active Yandex destination points, preserves checkout-compatible station identity/snapshot, and sends a fresh preview after explicit PVZ selection so yandex_pickup is priced to that station. Save writes the canonical _wdc_pickup_* data and the same Yandex aliases as checkout; courier clears pickup data. DPD, CDEK, Russian Post, import/pipeline, Rule Engine and packaging were not changed.
0.104.13 Pickup Selection Intent Timestamp Fix: every checkout pickup REST save now carries selection_intent. Real map and confirmed cross-location choices use explicit; automatic same-family rate synchronization uses technical. The API client forwards intent at the POST top level, and the REST controller defaults missing/unknown values to explicit for compatibility. Explicit always assigns a fresh server UTC selected_at. Technical preserves existing family top-level/snapshot selected_at before considering incoming values and never creates a current timestamp when none exists; existing operator_id/platform_station_id remain fallback identity sources. Yandex 5Post expiration/warning, pricing, representative selection, sorting, pipeline, Rule Engine and packaging were not changed.
0.104.12 Yandex 5Post Calendar-Day Selection Expiration: Yandex REST selection now stores top-level operator_id alongside the unchanged safe snapshot and UTC selected_at. CheckoutSessionManager explicitly expires only yandex_delivery:pickup when a selected 5Post timestamp is missing, invalid or belongs to another WordPress-site calendar day. Expiration runs before QuoteRequest context, pickup UI localization/rendering and checkout validation; validation blocks stale hidden fields from restoring the point in the same request and preserves selected_at during ordinary repricing/rate-id rebinding. CheckoutRateRenderer shows a dedicated highlighted 5Post warning after ordinary comments only for a valid current-day selected Yandex pickup. Other pickup families, representative selection, pricing, sorting, pipeline, Rule Engine and packaging were not changed.
0.104.11 Checkout Pickup POST and Repricing Stabilization: visible WooCommerce shipping-item meta now removes the technical pickup_family key while hidden _wdc_pickup_family and calculation/session/snapshot data remain intact. The shared pickup checkout script enables named fields only inside the active pickup container, preserving inactive family values without serializing duplicate empty fields. After a Yandex point save, yandex_pickup is remembered through rate repricing/re-sorting and restored when still available, with at most one guarded recovery update_checkout. A 5Post regression verifies the common yandex_delivery:pickup session, validation and order station flow without carrier-specific production logic. Pricing, sorting, map loading/clustering, pipeline, Rule Engine and packaging were not changed.
0.104.10 Yandex Courier Pickup-Address Pricing Fallback: yandex_courier first prices time_interval against the real checkout destination address. When that address is empty or the primary API/parser fails, one fallback request uses the address of the same selected or representative destination PVZ chosen by the existing pickup-pricing priority. The PVZ row is resolved locally by platform_station_id; full_address is preferred, with locality/street/house as a fallback. Rate diagnostics record pricing source, fallback flag, pickup source/station and primary/fallback error codes without storing the PVZ address. QuoteRequest, checkout/session/address/order data, courier validation, pickup pricing, Rule Engine, sorting, map, pipeline and packaging were not changed.
0.104.8 Single Checkout Rate Delivery Labels: WooCommerceRateMapper now derives the displayed delivery term for standalone rates from final DeliveryRate::delivery_days through DeliveryDaysFormatter. If a carrier title ends with the formatted original_delivery_days suffix, only that suffix is replaced; otherwise the final term is appended once with an em dash. Grouped/tariff-selector labels remain unchanged. CheckoutRateRenderer no longer prints planned_delivery_comment below a standalone method, while ordinary rate comments are still rendered. Rule Engine, carrier pricing, sorting, pickup map, pipeline and packaging were not changed.
0.104.6 Leaflet Zoom Cluster Rebuild: the Leaflet pickup map provider now retains the latest full point array and search-marker state, then clears and rebuilds marker/cluster layers from local data on every zoomend. Active point identity is preserved, the active style is reapplied, and an open pickup popup is restored whenever the point is rendered as an individual marker. Yandex city points are not fetched again for this rebuild. Yandex Maps provider, pickup formatting/presentation, checkout pricing, selected/representative station logic, pipeline/import/mapping, DPD, Rule Engine and packaging were not changed.
0.104.5 Yandex Pickup Map Presentation and Viewport List: Yandex checkout pickup points now use buyer-facing titles derived from operator_id/type/name, keep platform_station_id only in identity fields, and expose presentation_comment separately from pickup instructions. The pickup map renders presentation_comment below the title in popups and side-list cards, hides Yandex display_code, filters the Yandex side list by the current map viewport while keeping all loaded city markers for clustering, and shows counts as Показано X из Y. Yandex and Leaflet cluster grids were widened to 128px. Pricing, selected/representative station logic, source station, pipeline/import/mapping, DPD, Rule Engine and packaging were not changed.
0.104.4 Yandex Pickup Checkout Chain Fix: the missing Yandex pickup button was traced to two chain breaks. WooCommerceRateMapper did not expose top-level pickup_family, and CheckoutRateRenderer read only already-associative meta arrays while real WooCommerce rates can provide meta as key/value meta-data objects. The mapper now emits pickup_family for pickup rates, the renderer normalizes Woo meta-data entries before checking requires_pickup_point/delivery_type, and the JS maps legacy yandex_pickup to yandex_delivery:pickup for both family matching and pickup-rate detection so toggleForMethod keeps the container visible. PickupPointPresentationResolver is registered in DI and passed to PickupPointCardRenderer. update_checkout after yandex_delivery:pickup save remains enabled. Pricing request logic, representative/selected station logic, pipeline/import/mapping, DPD, Rule Engine and packaging were not changed.
0.104.3 Yandex Pickup Checkout Renderer Fix: CheckoutRateRenderer now renders the shared pickup UI container/button for every pickup rate with requires_pickup_point=true and delivery_type=pickup, including yandex_pickup with pickup family yandex_delivery:pickup. CheckoutDeliveryTypeSelector keeps session/capture compatibility but no longer registers a duplicate woocommerce_after_shipping_rate renderer. wdc-pickup-checkout.js now triggers WooCommerce update_checkout after saving yandex_delivery:pickup, so the selected platform_station_id can be used by the existing Yandex pricing flow. Pricing request logic, representative/selected PVZ selection, import/pipeline/mapping, DPD, Rule Engine and packaging were not changed.
0.104.2 Yandex Pickup Checkout Button Init Fix: checkout pickup map assets now enqueue on checkout whenever the WDC checkout platform is registered, even before session rates contain a pickup method. This keeps the picker JS/CSS available when WooCommerce later returns yandex_pickup from ajax rate calculation. wdc-pickup-checkout.js now opens the picker through delegated document click handling for data-wdc-pickup-open, while updated_checkout still reruns boot() over fresh data-wdc-pickup-checkout containers, so buttons recreated by WooCommerce remain clickable. Yandex pickup continues to use carrier_key=yandex_delivery, rate_id=yandex_pickup and pickup family yandex_delivery:pickup; pricing, selected-station logic, representative fallback, pipeline/import/mapping, DPD, Rule Engine and packaging were not changed.
0.104.1 Yandex Delivery Checkout PVZ Review Fixes: checkout Yandex pickup map/list no longer applies the REST limit or SQL LIMIT to buyer destination PVZ loading. For the selected checkout location_id, /wdc/v1/points resolves all mapped/manual yandex_geo_id values and returns every active destination row with non-empty platform_station_id and type pickup_point or terminal; available_for_dropoff remains ignored for buyer pickup. NewShippingMethod now passes both pickup_selection and family-scoped pickup_selections into QuoteRequest customer_context. YandexDeliveryCarrier restores yandex_delivery:pickup from pickup_selections before looking at the global pickup_selection, so a previously selected Yandex PVZ is reused after the buyer temporarily chooses another carrier. DPD, CDEK, Russian Post, import/pipeline/mapping/source station, Rule Engine and packaging were not changed.
0.104.0 Yandex Delivery Checkout PVZ Selection: checkout now reuses the common pickup picker for yandex_delivery:pickup. /wdc/v1/points loads active Yandex pickup destination rows from wp_wdc_yandex_delivery_pickup_points_v2 by every mapped/manual yandex_geo_id for checkout location_id, formats them into the shared pickup point shape with safe snapshots and no raw Yandex JSON, and /wdc/v1/checkout/pickup-point persists the selected platform_station_id in the standard pickup session bucket. yandex_pickup pricing now uses the selected destination station when present and records pickup_source=selected; without selection it keeps the representative PVZ fallback and records pickup_source=representative. Checkout validation requires a Yandex PVZ only for yandex_pickup, rejects pickup selections from other carriers, and yandex_courier remains address-only. Order meta now saves canonical _wdc_pickup_* data plus Yandex aliases including _wdc_yandex_delivery_pickup_platform_station_id. Import, staging import, geo_v2 builder, enrichment, region/location mapping, manual overrides, source station admin UI, pricing-calculator endpoint, checkout sorting, DPD, Rule Engine and packaging were not changed.
0.103.6 Yandex Delivery Shared Packaging Places: Yandex pricing-calculator requests now use the shared generic PackagingBuilder from src/Packaging through YandexDeliveryPricingRequestBuilder. PackagingParcel objects become Yandex places[] entries with physical_dims.weight_gross/dx/dy/dz; parcel quantity is expanded into repeated places and total_weight is the sum of every sent weight_gross. The builder records safe rate meta diagnostics package_builder_source, packing_strategy, parcels_count, total_weight_g, places_count and sanitized package dimensions/weights. If the packaging result is empty or invalid, Yandex falls back to the previous single-place model with generic Packaging defaults 500 g and 20x15x10 cm. DPD still uses DpdPackagingBuilderFactory with its legacy config, and checkout sorting, Rule Engine, carrier endpoints, tariffs, source station and representative PVZ selection were not changed.
0.103.5 Checkout Selector Group Key Fix: RateSorter no longer treats shared service_key as a checkout method group. Ordinary rates without tariff_selector_group are standalone checkout methods keyed by their own rate_id, so pairs such as Yandex pickup and Yandex courier remain separate even when they share one service. Only rates marked with tariff_selector_group are grouped, and only by checkout_group_id; those grouped variants still sort by original carrier values inside the selector, while checkout methods still sort by final active values. NewShippingMethod::rates_for_wc() behavior is unchanged except that it now receives correctly separated methods from the sorter. Carrier payloads, Rule Engine behavior, PackagingBuilder, DPD and Yandex runtime code were not changed.
0.103.4 Checkout Sorting Level Split Fix: checkout sorting now deliberately separates tariff-selector variant order from method order. RateSorter::sort_group_rates() keeps variants inside one method/selector ordered by original carrier values (original_cost for price mode, original_delivery_days.min_days for fastest mode, falling back to current fields), so discounts and rule changes do not reshuffle selector options. RateSorter::sort_methods() orders checkout methods by the final active rate values visible to the buyer (price or delivery_days.min_days after Rule Engine, discounts, minimum price, rounding and service post-processing). NewShippingMethod::rates_for_wc() now replaces the first selector-group rate with the grouped selector in-place, preserves variant order, applies a selected tariff as the active selector rate, and then sorts methods by that active final value. Carrier payloads, Rule Engine behavior, PackagingBuilder and pickup UI were not changed.
0.103.3 Deterministic Checkout Sorting: checkout sorting is centralized in RateSorter for every carrier. DeliveryRate now carries neutral original_cost and original_delivery_days values, preserved through rule application, service post-processing and WooCommerce technical metadata. Price mode sorts rates inside one delivery method by original carrier cost, then original delivery days, title, tariff_key and rate_id; method groups are ordered by their minimum original carrier cost. Fastest mode uses the original minimum delivery days first, then original cost, title, tariff_key and rate_id. Rule Engine discounts/final WooCommerce prices no longer affect checkout order. Carrier payloads, DPD/Yandex/CDEK/Russian Post runtime mapping and PackagingBuilder algorithms were not changed.
0.103.2 DPD Packaging Legacy Config Fix: DPD checkout runtime now receives a DPD-configured PackagingBuilder from DpdPackagingBuilderFactory, with the pre-refactor fallback config 1000 g, 20x20x20 cm, declared value 1000 RUB. The shared PackagingBuilderConfig::defaults() remains carrier-neutral at 500 g, 20x15x10 cm, declared value 1 RUB, and the Packaging layer still has no dependency on DpdSettings or Carriers\\Dpd. DPD checkout smoke now guards payload parcels, declaredValue, selfPickup/selfDelivery and sender/receiver cityId for full-dimension and fallback carts, including the legacy 20x20x20 fallback. Packing algorithms, parcel counts, DPD request mapping, Yandex pricing payloads and other checkout services were not changed.
0.103.1 Shared Packaging Builder Review Fix: `PackagingBuilderConfig::defaults()` now lives inside the shared Packaging layer and provides carrier-neutral fallback values `500 g`, `20x15x10 cm`, declared value `1 RUB`; `src/Core/Plugin.php` creates the shared `PackagingBuilder` from these defaults without reading `DpdSettings`; DPD receives a separate legacy `PackagingBuilderConfig(1000, 20, 20, 20, 1000)` so fallback payloads stay unchanged without depending on DPD settings. `PackagingResult::to_array()` now exposes parcel arrays for diagnostics/JSON, while `PackagingResult::parcels()` keeps object access for DPD. DPD still adapts `PackagingParcel` to `DpdTariffParcel`, and packing algorithms, parcel counts, DPD payload behavior and Yandex pricing payloads were not changed.
0.103.0 Shared Packaging Builder: moved the DPD parcel packing engine into the shared `src/Packaging` layer as `PackagingBuilder`, `PackagingBuilderConfig`, `PackagingParcel` and `PackagingResult`. DPD now consumes the shared builder and adapts `PackagingParcel` to the existing `DpdTariffParcel` DTO before building Parcels3 payloads, so DPD parcel count, dimensions, weights, diagnostics and SOAP payload shape remain unchanged. `DpdTariffParcel` remains DPD-specific; `Domain/Package/ShipmentPlace` remains the shipment-domain place model and was not reused to avoid a broad domain refactor. Yandex checkout pricing is not switched to multi-place packaging yet and still sends its aggregate single-place pricing-calculator model. Algorithms for long items, small items, identical-grid optimization, 3D packing, two-box split, stacked fallback, packaging weight and diagnostics were not changed.
0.102.1 Yandex Delivery Pricing Day Labels: Yandex pricing-calculator checkout titles now reuse the shared Russian delivery-days formatter, so API delivery days render as `1 день`, `2 дня`, `5 дней`, `21 день`, `22 дня`, `25 дней` instead of the short `дн.` suffix. The pricing request payload remains unchanged and still uses the aggregate single-place model: one `places` item with package total weight, matching `total_weight`, and package/fallback dimensions. Buyer PVZ selection/map, representative PVZ selection, pricing API payload shape, checkout for other services and geo pipeline code were not changed.
0.102.0 Yandex Delivery Pricing Calculator Checkout: Yandex checkout now calls the B2B pricing-calculator for both `yandex_pickup` and `yandex_courier`. Pickup uses the saved `source_platform_station_id`, all mapped/manual `yandex_geo_id` values for the destination `location_id`, and a stable representative destination PVZ selection from the local imported PVZ table. Courier builds a destination address from checkout data. Successful API responses provide price and delivery days in checkout titles; per-rate failures return disabled rates with readable reasons without breaking the other Yandex rate. Buyer PVZ selection/map, pricing calculator surcharges, import, geo pipeline, mapping pipeline, manual overrides and shipment flows were not changed.
0.101.3 Yandex Delivery Source Station Multi-Geo Fix: the sender PVZ selector now resolves every mapped/manual yandex_geo_id linked to the selected WDC location_id, de-duplicates and sorts them, then loads all active available_for_dropoff PVZ rows across that geo id set with non-empty platform_station_id. The UI status now reports the full geo id list, for example `Найдено ПВЗ сдачи: N; yandex_geo_id: 102057, 102058, 102066.` Checkout/pricing/pipeline/import/geo/mapping/manual override logic were not changed.
0.101.2 Yandex Delivery Source Station PVZ List Fix: the sender PVZ selector now loads every active available_for_dropoff point for the selected yandex_geo_id with a non-empty platform_station_id, without a SQL or in-memory limit. The calculation tab adds a client-side address filter over the already loaded options; filters shorter than 3 characters show the full list, 3+ characters search full_address case-insensitively, and an empty-result message is shown when nothing matches. Checkout/pricing/pipeline/import/geo/mapping/manual override logic were not changed.
0.101.1 Yandex Delivery Source Station Review Fix: the sender PVZ selector no longer loads the first 5000 Yandex PVZ rows across Russia. The admin UI now selects a WDC local location_id, resolves its primary yandex_geo_id through location mapping v2, and loads only active available_for_dropoff pickup points with that yandex_geo_id and non-empty platform_station_id. The optional source_location_id service setting restores the admin city selector; source_platform_station_id remains the saved sender PVZ id and the address is still read-only from the local PVZ table. Warnings now cover missing, inactive, and non-dropoff saved PVZ rows. Checkout/pricing/pipeline/import/geo/mapping/manual override logic were not changed.
0.101.0 Yandex Delivery Source Station Admin: added the first checkout-preparation setting on WDC -> Службы доставки -> Яндекс Доставка -> Расчет for choosing the sender source platform station from the already imported local Yandex PVZ database. The UI filters active available_for_dropoff points by city, stores only source_platform_station_id/platform_station_id in delivery service settings, restores city/PVZ/address after reload from the local table, and shows a warning when the saved station is no longer present after import. Yandex checkout rates remain unchanged: yandex_pickup/yandex_courier still render with zero temporary cost and "без указания срока"; pricing, delivery time, buyer PVZ selection, import, geo_v2, enrichment, region/location mapping, manual overrides, search/dominance/fallback and pipeline code were not changed.
0.92.0 Yandex Geo Region Keyword Auto Filter: added a post-location/detect regional keyword filter before resolution policy. It keeps Yandex candidates whose locality/region/raw_json address contains the WDC region keyword, writes compact diagnostics when candidates are removed, and returns not_found when all candidates are filtered out. Yandex API request, scoring weights, coverage, runner, PVZ import, checkout and pricing were not changed.
0.91.3 Yandex Geo Manual Region Cleanup Redirect: registered the region cleanup POST action in the delivery services action flow, redirected cleanup results back to the Yandex geo mapping tab, preserved service/page hidden fields, and kept candidates when the region fragment is present in raw_json fallback data. Scorer, resolution policy, analytics, coverage, runner, PVZ import, checkout and pricing were not changed.
0.91.2 Yandex Geo Analysis Render Memory Safety: made the consolidated mapping-tab analytics lazy behind yandex_delivery_geo_show_analysis=1 and replaced real DB analysis scans with SQL COUNT/GROUP BY plus limited raw_json reads for matched_by and low-confidence rows. Scorer, resolution policy, coverage, runner, PVZ import, checkout and pricing were not changed.
0.91.1 Yandex Geo Manual Region Cleanup Memory Safety: fixed the manual mapping page memory risk by keeping region cleanup out of GET render and changing the real DB cleanup path to SQL counts, DELETE JOIN, and batched location_id conversion checks. Scorer, resolution policy, coverage, runner, PVZ import, checkout and pricing were not changed.
0.91.0 Yandex Geo Manual Region Cleanup: added an admin-only manual cleanup tool for needs_review candidates by region fragment. It removes only non-primary needs_review candidates whose Yandex locality does not contain the entered region fragment and converts empty non-primary locations to not_found. Scorer, resolution policy, coverage, runner, PVZ import, checkout and pricing were not changed.
0.90.1 Yandex Geo Mapping Runner Pause Race Fix: hardened pause semantics so late step responses may apply counters but cannot move a paused runner back to running or overwrite the pause message. JS bootstrap still resumes only running state; scoring, coverage, PVZ import, checkout and pricing were not changed.
0.90.0 Yandex Geo Mapping Runner Metrics: added ETA diagnostics to the browser-driven mapping runner and a persistent technical_error_markers_count sourced from real yandex_geo_id=999999999 mapping rows. errors_last remains session-scoped only; scoring, coverage, PVZ import, checkout and pricing were not changed.
0.89.1 Yandex Geo Mapping Runner Recovery: fixed paused full-runner resume so Start continues the paused session instead of resetting progress, guarded the browser loop against stale AJAX responses after Pause, added temporary unprocessed recovery mapping from max processed location_id minus 5, and made manual-review candidate address rows bold. Scoring, coverage, PVZ import, checkout and pricing were not changed.
0.89.0 Yandex Geo Manual Review Queue UX: improved manual review for mass work. The queue is full-width, separates candidate diagnostics from approve controls, removes individual reject/comment UI, adds select-all for the current page, and limits paused/running full-runner manual processing to location_id < next_location_id. Runner processing, scoring, coverage, PVZ import, checkout and pricing were not changed.

0.88.1 Yandex Geo Manual Review Queue fix: fixed approve semantics. Approving one candidate now maps it as primary and moves all other candidate rows for the same location_id out of needs_review into diagnostic multiple_matches, so the approved location disappears from the manual queue. Runner running lock text now tells admins to wait for completion or pause. Runner, scoring, coverage, PVZ import, checkout and pricing were not changed.

0.88.0 Yandex Geo Manual Review Queue: added a working manual queue in the Маппинг geo_id admin tab for grouped needs_review rows. Admins can filter by search/region/settlement type, approve a specific candidate as mapped primary with compact manual_review audit, reject a location into a normal not_found diagnostic row, or bulk reject selected location_id values. Manual review actions are blocked while the geo mapping runner is running. PVZ import, checkout and pricing were not changed.

0.87.3 note: simplified the Yandex Geo Mapping Runner back to one browser worker with BATCH_SIZE=50. The runner keeps next_location_id, full/retry_errors modes, pause/reset and technical marker handling, but removes WORKER_COUNT/worker_count from state/UI and removes parallel worker JS markers. Scoring, resolution policy, coverage, PVZ import, checkout and pricing were not changed.

0.87.2 note: fixed Yandex Geo Mapping Runner counter aggregation for parallel browser workers. `apply_step_delta()` now waits longer for the option lock, applies each worker delta to the fresh state read under the lock, saves the aggregate, and returns the fresh persisted state so 3 workers x 30 rows advance processed and status counters by 90. Reservation, retry_errors selection, coverage, PVZ import, checkout and pricing were not changed.

0.87.1 note: fixed the Yandex Geo Mapping Runner admin state table so `worker_count=3` is displayed next to `batch_size=30` on the `Маппинг geo_id` tab. Runner reservation, retry_errors, coverage, PVZ import, checkout and pricing logic were not changed.

0.87.0 note: accelerated the browser-driven Yandex Geo Mapping Runner. Full and retry_errors modes now reserve work through next_location_id before processing, use option-lock protected reservation/state counter updates, run with BATCH_SIZE=30 and WORKER_COUNT=3, and the admin JS starts three bounded parallel browser workers. No cron, background queue, WP Scheduler, CLI, coverage, PVZ import, checkout or pricing changes were added.

0.86.1 note: fixed Yandex Geo Mapping Runner full reruns. Full mode now rebuilds mappings for every active RU location with a non-empty display_name regardless of existing mapped/needs_review/manual/error rows, deletes old location mappings before each remap, and no longer tracks skipped_existing. Retry technical-error mode, coverage diagnostics, PVZ import, checkout and pricing were not changed.

0.86.0 note: added Yandex Geo Mapping Runner for production-style full automatic mapping of the real WDC locations base from the `Маппинг geo_id` admin tab. The runner stores state in `wdc_yandex_delivery_geo_mapping_runner_state`, processes active RU locations with non-empty `display_name` in `id ASC` order, uses fixed batch size 20 with no UI `limit`/`batch_size`, survives page refresh and continues from `last_location_id`. AJAX actions start full mode, retry technical errors, run steps, pause, reset and read status; `assets/admin/yandex-delivery-geo-mapping-runner.js` loops step-by-step while the page is open. Technical location/detect failures are stored as non-primary marker `yandex_geo_id=999999999`, never returned by `find_primary_geo_id()` and never available as manual primary. Retry mode processes only marker rows and replaces/clears the marker on successful mapped/not_found/needs_review outcomes, or updates it on another technical error. Manual detect/primary actions are blocked while the runner is running. PVZ import, coverage batch, checkout and pricing were not implemented.0.85.0 note: consolidated the Yandex Delivery admin UX into the future working structure: `Данные для входа`, `Маппинг geo_id`, `Покрытие Яндекса`, `Яндекс ПВЗ`. The `Маппинг geo_id` tab now visually contains manual geo_id search, mass mapping controls, mapping analytics and a placeholder for manual review of `needs_review` rows. `Покрытие Яндекса` keeps the manual/selective coverage diagnostics and adds location-name search for choosing `location_id`. `Яндекс ПВЗ` is the future pickup-point workspace while the existing Moscow `geo_id=213` import remains clearly labeled as a test diagnostic. Architectural decision: a separate coverage batch is not needed; future PVZ import by confirmed mapped geo_id will update `covered`/`not_covered` while importing points. Checkout, pricing, shipment creation and full Russia PVZ import were not changed.0.84.0 note: added Yandex Coverage Discovery Foundation for manual/selective checks of real Yandex pickup/dropoff coverage by WDC `location_id`. `database/migrations/0034_create_yandex_delivery_geo_coverage_table.php` creates `wdc_yandex_delivery_geo_coverage`; `YandexDeliveryGeoCoverageRepository` upserts one row per location and reports coverage-status stats; `YandexDeliveryGeoCoverageService` uses only the primary `find_primary_geo_id()` mapping, calls `pickup-points/list` with integer `geo_id`, stores compact operator/sample/stat aggregates and never stores the full raw response. At 0.84.0 the initial diagnostic coverage tab supported one-location manual checks, status statistics and last 50 checks; in 0.85.0 it is surfaced as `Покрытие Яндекса` in the consolidated Yandex admin structure. Checkout, pricing, PVZ map/selection, shipment creation, full Russia PVZ import, coordinate fallback and autosync were not changed.
0.82.0 note: added Yandex Mapping Resolution Engine foundation. `YandexDeliveryGeoResolutionPolicy` now separates scored `location/detect` candidates into `mapped`, `needs_review` and `not_found`; `needs_review` was added to the Yandex geo status model for candidates that are similar but not safe as working geo_id mappings. A confident primary still requires confidence >= 95 and at least a 15-point gap from the second candidate. `locality_exact` with a wrong/foreign region is now `needs_review`, not `not_found`; region+district/type context without locality also stays reviewable for future coordinate/manual checks. `YandexDeliveryGeoMappingService` receives the policy from DI, saves mapped primary rows, stores needs_review candidates without primary, and stores not_found as one NULL-geo diagnostic row. Batch counts `needs_review` under the existing ambiguous counter. Checkout, pricing, PVZ import/selection, shipments and autosync were not changed.
0.81.1 note: hardened Yandex Low Confidence Analysis DI verification. `YandexDeliveryGeoAnalysisService` is registered in the plugin container with `LocationRepository` and passed into `DeliveryServicesAdminPage`; the geo analysis smoke now guards both registration and admin constructor wiring. Analysis logic, UI, scorer and batch builder were not changed.
0.81.0 note: added Yandex Low Confidence Analysis for saved geo mapping results. `YandexDeliveryGeoAnalysisService` reads existing `wdc_yandex_delivery_geo_mappings` rows and WDC locations to show confidence bucket statistics, status statistics, low-confidence top regions/types, matched_by pattern frequencies from `raw_json.scoring.matched_by`, and the lowest-confidence rows. The new admin tab is `Yandex geo analysis` after `Yandex geo batch` with a GET `max_confidence` filter defaulting to 59.99. This stage does not call Yandex, does not run `location/detect`, does not generate mappings, and does not change scorer, batch builder, checkout, pricing or PVZ import.

0.80.2 note: hardened Yandex geo mapping smoke coverage for the Тлюстенхабль scorer synonym case. The smoke now separately asserts that the long urban-settlement type pattern appears, the short settlement pattern appears, and the long pattern is checked first. No scorer/runtime behavior was changed.

0.80.0 note: improved Yandex Geo Match Scoring with canonical administrative and settlement-type synonyms. The scorer now treats abbreviations such as `респ`/`республика`, `р-н`/`район`, `МО`/`муниципальный округ`, `ГО`/`городской округ` and `АО`/`автономный округ` as equivalent context, and compares canonical settlement types such as `пгт`/`поселок городского типа`. The live Тлюстенхабль case now scores as a high-confidence exact match with `type_equivalent` diagnostics. Checkout, pricing, pickup points, distance fallback and the batch builder were not changed.

0.79.3 note: fixed Yandex Geo Mapping single-candidate classification. A single weak `location/detect` candidate with confidence below 60 now remains `multiple_matches` without primary instead of producing `mapped` with `is_primary=0`, so the batch builder counts it as ambiguous rather than an unclassifiable error. No new Yandex Delivery functionality was added.

0.79.2 note: hardened the Yandex Geo Mapping Batch Builder bugfix verification. The geo batch smoke now instantiates LocationRepository with the fake DB and asserts method_exists(..., 'find_batch_after_id') before calling the helper, proving the method is present in the loaded class. No new Yandex Delivery functionality was added.

0.79.1 note: fixed Yandex Geo Mapping Batch Builder repository helper contract. LocationRepository::find_batch_after_id() now has the explicit RU/display-name batch signature used by YandexDeliveryGeoMappingBatchService, filters active rows for both real DB and test doubles, and remains covered by geo batch smoke guards. No new Yandex Delivery functionality was added.

0.79.0 note: added Yandex Geo Mapping Batch Builder foundation. `YandexDeliveryGeoMappingBatchService` stores experimental batch state in option `wdc_yandex_delivery_geo_mapping_batch_state`, runs short RU/display_name batches from `LocationRepository::find_batch_after_id()`, skips locations with an existing primary mapping, calls the existing `YandexDeliveryGeoMappingService::detect_for_location_id()` path, and records mapped/ambiguous/not_found/errors/skipped_existing plus confidence buckets and last 10 errors. The Yandex Delivery admin tabs are now `Данные для входа`, `ПВЗ / точки сдачи`, `Yandex geo_id`, `Yandex geo batch`; the new tab supports Start batch, Run step, Pause and Reset through the existing nonce/action framework. The recommended first live run is 1000 locations and results should be evaluated by mapped / ambiguous / not_found / errors. Full 160000-location mapping, checkout, pricing calculator, pickup selection/map, full Russia PVZ import, shipments, statuses and documents remain intentionally not implemented.
0.76.1 note: fixed Yandex Delivery geo scorer DI wiring. `YandexDeliveryGeoMatchScorer` is now registered in `Plugin.php` and passed explicitly into `YandexDeliveryGeoMappingService`; the service no longer fallback-constructs its scorer dependency. No new functional stage was added.

0.76.0 note: added Smart Geo Match Scoring for Yandex Delivery geo_id mapping. `YandexDeliveryGeoMatchScorer` scores location/detect candidates with normalized locality/region matching, noisy/foreign candidate downranking, auto-primary thresholds, and raw `variant` + `scoring` diagnostics; the admin table now shows compact scoring diagnostics. Multiple geo_id for large cities remains a working hypothesis to recheck later against pickup-points/list and the completed mapping database. No checkout, pickup selection, full Russia import or mass 160k mapping was added.

0.75.3 note: fixed Yandex Delivery geo_id live-test issues. `location/detect` variants with string `address` now save that text as fallback locality when no structured locality exists, and the geo detect admin notice now stores compact details instead of full location/mappings payloads. No new Yandex functional stage was added.

0.75.2 note: fixed Yandex Delivery geo_id admin tab DI. `DeliveryServicesAdminPage` now promotes the Yandex geo mapping repository and service passed by `Plugin.php`, so the `Yandex geo_id` tab receives non-null dependencies instead of returning at the guard. No new Yandex functional stage was added.

0.75.1 note: fixed Yandex Delivery geo mapping review issues. `yandex_geo_id` is now nullable for `not_found` and `error` rows, repository saves use deterministic upsert semantics instead of repeated live inserts, and failed `location/detect` calls preserve existing working mappings while adding a nullable error diagnostic row. No new Yandex functional stage was added.

0.75.0 note: added Yandex Delivery geo_id mapping foundation. `database/migrations/0033_create_yandex_delivery_geo_mappings_table.php` creates `wdc_yandex_delivery_geo_mappings` for 1 WDC `location_id` to many Yandex `geo_id` rows with locality/region/source/status/confidence/primary/raw JSON. `src/Carriers/YandexDelivery/Geo/*` adds status constants, repository CRUD/statistics and a `location/detect` service that builds search strings from existing `LocationRepository`/`Location` models, saves `mapped`, `multiple_matches`, `not_found`, `manual` and `error` rows, and keeps `wp_wdc_location_delivery_codes` untouched. The Yandex admin page now has tabs `Данные для входа`, `ПВЗ / точки сдачи`, `Yandex geo_id` with single-location detect, location search, result table, primary selection and mapping statistics. Full Russia pickup import, checkout, pricing calculator, pickup selection, maps and shipments remain out of scope. Control figures for later full Russia pickup import are 35222 pickup points and 12605 dropoff points.

0.74.2 note: Yandex Delivery pickup import no longer sends unsupported limit/page_token pagination fields. Until WDC has a location-to-Yandex-geo_id model, manual AJAX import is temporarily limited to Moscow with integer geo_id=213 and reports mode=geo_id_fixed, geo_id and geo_label.

0.74.1 note: Yandex Delivery pickup AJAX import now shows a recovery hint on browser-side AJAX errors/timeouts, keeps the reset button available and re-enables the start button so admins know to reset lock or retry after an interrupted step.

0.74.0 note: Yandex Delivery pickup import is now a manual AJAX step-runner. Admin start creates option-backed state and lock, each step processes one pickup-points/list page, progress is rendered on the pickup tab, final success saves the import report, and reset clears both state and lock. No cron/autosync, checkout, tariffs, map, shipments or statuses were added.

0.73.3 note: Yandex Delivery pickup import now defaults to page_size=100, supports admin-configurable page size clamped to 20..500, and stores page_size/pages/memory_peak_mb diagnostics in the import report to avoid production memory exhaustion on large pickup-point pages.

0.73.2 note: Yandex Delivery pickup import is now streaming by API page, so production imports do not accumulate all pages/points in memory; the admin reset action also clears the pickup import lock.

0.73.1 note: fixed Yandex Delivery pickup repository review issues. The repository constructor now resolves the default WordPress global `$wpdb` through a local variable so live code gets the real DB object while smoke tests can still inject a fake object. `create_schema_if_needed()` no longer treats fake row storage as the reason to skip schema creation; fake/non-WP smoke environments are skipped through an explicit schema-environment check, while live WordPress can still reach `dbDelta`. No Yandex checkout, tariffs, shipments, statuses, documents, cron or autosync were added.

0.73.0 note: added Yandex Delivery Pickup Points Foundation. `database/migrations/0032_create_yandex_delivery_pickup_points_table.php` creates the local `wdc_yandex_delivery_pickup_points` table with unique `platform_station_id`, type/geo/city/dropoff/active indexes and raw JSON storage. `src/Carriers/YandexDelivery/Pickup/*` now contains repository, normalizer, manual import service and service layer for search/statistics/sender dropoff validation. The Yandex Delivery admin page now has `ПВЗ / точки сдачи` with active/type/dropoff/last-import stats, active-environment sender point validation, manual `Импортировать точки Яндекс` button and basic search. The import calls `POST /api/b2b/platform/pickup-points/list` with `type=pickup_point`, downloads pages, stores pickup/dropoff/partner points including `available_for_dropoff=false`, marks old rows inactive and activates imported rows. This stage still deliberately does not add checkout, tariffs, pickup selection/map, `YandexDeliveryQuoteCarrier`, pricing calculator, order recalculation, shipment creation, offers/create, offers/confirm, cancellation, statuses, documents, autosync or Yandex-specific cron.

0.73.0 note: added Yandex Delivery Pickup Points Foundation. `database/migrations/0032_create_yandex_delivery_pickup_points_table.php` creates the local `wdc_yandex_delivery_pickup_points` table with unique `platform_station_id`, type/geo/city/dropoff/active indexes and raw JSON storage. `src/Carriers/YandexDelivery/Pickup/*` now contains repository, normalizer, manual import service and service layer for search/statistics/sender dropoff validation. The Yandex Delivery admin page now has `ПВЗ / точки сдачи` with active/type/dropoff/last-import stats, active-environment sender point validation, manual `Импортировать точки Яндекс` button and basic search. The import calls `POST /api/b2b/platform/pickup-points/list` with `type=pickup_point`, downloads pages, stores pickup/dropoff/partner points including `available_for_dropoff=false`, marks old rows inactive and activates imported rows. This stage still deliberately does not add checkout, tariffs, pickup selection/map, `YandexDeliveryQuoteCarrier`, pricing calculator, order recalculation, shipment creation, offers/create, offers/confirm, cancellation, statuses, documents, autosync or Yandex-specific cron.

0.72.1 note: fixed Yandex Delivery foundation review issues. The explicit connection diagnostic now sends the documented pickup-points/list filter payload with pickup_point_ids, type=pickup_point and available_for_dropoff=true instead of the earlier assumed source.platform_station_id shape. Yandex API error-code extraction now reads only scalar code values from code, error_code, error.code or scalar error and returns an empty string when no scalar code exists, avoiding Array to string conversion warnings.

0.72.0 note: started Yandex Delivery other-day integration with foundation/API/settings only. Added the built-in yandex_delivery service (Яндекс Доставка) as RU-only, disabled by default, sorted after DPD and protected from custom-service deletion. WDC -> Службы доставки -> Яндекс Доставка -> Данные для входа now stores active environment, encrypted test/production Bearer tokens, test/production source platform_station_id, timeout, debug and last connection check fields. The HTTP layer selects test https://b2b.taxi.tst.yandex.net or production https://b2b-authproxy.taxi.yandex.net, sends JSON with Authorization: Bearer ..., exposes a replaceable HTTP client and runs an explicit admin-only pickup-points/list diagnostic with token/PII-safe errors. This stage deliberately does not add Yandex pickup-point tables/import, checkout rates, order-admin recalculation, shipment actions, offers/create, offers/confirm, statuses, documents, cron or autosync.

0.71.0 note: added DPD pickup point autosync. The DPD -> ПВЗ tab now stores a dedicated enable flag plus three independent 15-minute Moscow-time slots, validates invalid times to empty and deduplicates repeated slots for scheduling. `DpdPickupPointAutoSync` owns the separate WP-Cron hook `wdc_dpd_pickup_points_autosync`, converts GMT+3 times to UTC daily events, clears/recreates events on settings save and activation/deactivation, and calls the existing `DpdPickupPointImportService::import_all()` with context `auto_cron`. Manual import and cron import share the same lock, safe no-wipe failure behavior and last import report; DPD shipment status autosync/getEvents/confirm, createOrder2, documents, checkout pricing and order recalculation were not changed.

0.70.0 note: completed the DPD WooCommerce status audit. DPD EventCode values still resolve through DpdStatusMapping into DeliveryStatus universal statuses and then through the shared ShipmentOrderStatusMappingService, with no DPD-specific WooCommerce order status logic. DpdEventSyncResult now records order_statuses_changed, order_statuses_skipped and order_status_change_errors, and ShipmentStatusAutoSyncService aggregates those counters into the shared autosync diagnostics for DPD getEvents runs. The new DPD audit smoke test covers mapped 1401/2201/3304 events, missing mappings, duplicate/older event idempotency, old dpdOrderNr protection, manual refresh, multi-order autosync, confirm enabled/disabled behavior, notes and diagnostics.

0.69.3 note: fixed and covered the DPD pickup side of order-admin delivery recalculation. DpdQuoteCarrier now forwards explicit DPD receiver cityId from the recalculation QuoteRequest into DpdTariffCalculationService, and the Parcels3 diagnostics expose
eceiver_location_id,
eceiver_city_id, delivery terminal selection/code/source, raw tariff count, skipped counters and DPD filter removals. Pickup auto-selection still uses only active receiver parcel_shop points and avoids 	erminal_self_delivery; if none exists, pickup is unavailable with DPD pickup tariff unavailable: no active parcel_shop for receiver cityId {cityId} while DPD courier remains available. Checkout pricing, DPD create/documents/getEvents/confirm flows were not changed.

0.69.2 note: restored DPD rates in the WooCommerce order-admin `Калькулятор доставок` preview for selected-location payloads that use `location_id` instead of `id`. `OrderQuoteRequestMapper` now preserves the selected location id and DPD receiver cityId in the checkout-compatible customer context, so DPD pickup/courier rates continue through `CheckoutOrchestrator -> DpdQuoteCarrier` instead of disappearing before render. Order recalculation group/title fallbacks now include DPD (`DPD до пункта выдачи`, `DPD курьером`). The DPD post-create `getEvents` path was reviewed and left unchanged: a first empty inbox response is not treated as a registration failure, checked timestamps are still updated, and delayed `1401 / OrderCreate` can be picked up by a later manual refresh.

0.69.1 note: fixed the initial PHP render of DPD shipment action buttons. Reloaded order pages now use the DPD adapter status payload for cancel/remove visibility, matching AJAX update/create behavior: EventCode `1401` shows cancel plus document download and hides local remove, while EventCode `1301` hides cancel and shows `Удалить из заказа`.

0.69.0 note: added DPD shipment documents download. The order `Отправления` block now shows `Скачать документы` only for DPD shipments with `dpd_order_number` and strict `dpd_event_code=1401`. The download endpoint builds a temporary ZIP containing `dpd-invoice-{dpd_order_number}.pdf` from `order2/getInvoiceFile` and `dpd-label-a6-{dpd_order_number}.pdf` from `label-print/createLabelFile`; invoice payload intentionally omits `parcelCount` and `cargoValue`, and label payload requests `PDF` + `A6` through wrapper `getLabelFile`. The service does not store documents in order meta and does not change shipment status or `tracking_checked_at`; partial failures return an error and clean temp files. DPD label error `У заказа дата забора ранее текущей даты` is surfaced as `Наклейка DPD недоступна: у заказа дата забора ранее текущей даты.`.

0.68.0 note: connected DPD to the existing shipment status autosync architecture. ShipmentStatusAutoSyncService now performs one DPD global pre-pass through DpdEventSyncService::sync(null, true) per run, records DPD stats in diagnostics, and skips per-order DPD polling because getEvents is already a client-wide inbox. DpdEventSyncService still owns the DPD lock, confirm loop and matching; autosync-only enrichment calls getStatesByDPDOrder only for newly updated shipments missing actual cost or planned delivery date. DPD Расчет now has enabled-by-default Автоматическое обновление статусов DPD plus readonly wdc_dpd_autosync_last_run / wdc_dpd_autosync_last_result. Manual create/update, checkout pricing, documents and labels were not changed.

0.67.5 note: fixed the DPD post-create admin UI refresh. The create AJAX response now builds carrier UI/status payload from the refreshed shipment returned by `DpdOrderRegistrationService` after event sync/enrichment/touch, so a found `1401 / OrderCreate` immediately renders the DPD last operation and button policy (`Обновить статус` + `Отменить отправление в DPD`). The registration polling message now uses `.wdc-shipment-inline-spinner` with CSS keyframes instead of a static-looking indicator.

0.67.4 note: refined DPD pending-registration event matching for orders that previously had another DPD shipment. While a local shipment has no `dpd_order_number`, `clientOrderNr` fallback now filters events by `registration_started_at - 300 seconds`, rejects old cancellation/return events that carry a DPD number, logs them as unmatched with `reason=stale_or_cancelled_pending_event`, and chooses the latest valid event for that client order. After `dpd_order_number` is saved, matching remains strict by DPD number and `clientOrderNr` cannot update a different active shipment.

0.67.3 note: hardened DPD `getEvents` matching for WooCommerce orders that have had multiple DPD shipments over time. `DpdEventSyncService::match_order()` now uses `dpdOrderNr` first and falls back to `clientOrderNr` only when no DPD-number match is found; before applying an event it also verifies that a shipment with a saved `dpd_order_number` matches the incoming `dpdOrderNr` case-insensitively. Mismatches are logged as unmatched with `saved_dpd_order_number` and do not update the active shipment or block confirm.

0.67.2 note: fixed DPD create/status QA issues. Successful `createOrder2` now immediately runs DPD event sync, enrichment and checked-time touch before the AJAX payload is returned, so `1401 / OrderCreate` appears in the order shipment block when DPD provides it. Manual DPD status refresh always updates `tracking_checked_at` and `updated_at` even when no new event is matched. DPD button policy now works from persisted shipment fields after reload, including generic operation-code fallback. The exact `parcel[]` rows and cargo summary sent to DPD are stored in `_wdc_shipments[dpd].dpd_sent_places` and rendered in `Отправления`; they are intentionally not moved into the order delivery recalculation calculator.

0.67.1 note: fixed four DPD lifecycle blockers from review. DPD `OrderDuplicate`, `OrderError` and `OrderCancelled` responses that include `errorMessage` now stay business responses for registration handling instead of becoming transport errors. The shipment UI renders generic DPD operation fields as date, EventCode and marker while preserving CDEK/Russian Post fallbacks. `getEvents` matches to an existing Woo order without a local DPD shipment are logged as unmatched and do not create shipments. Saved valid event timestamps are protected from incoming no-date events.

# Project Status

0.67.0 note: rebuilt the DPD shipment lifecycle without carrying over experimental DPD status/debug branches. `DpdApiClient` now covers `order2/createOrder2`, `order2/getOrderStatus`, `event-tracking/getEvents`, `event-tracking/confirm`, `order2/cancelOrder` and `tracing1-1/getStatesByDPDOrder` with WSDL-checked wrappers (`orders`, `orderStatus`, `request`). Manual create is controlled by the current order page in two AJAX stages: save pending local DPD shipment first, then submit `createOrder2` with a fixed 10-second timeout. Pending/uncertain results use non-overlapping frontend `getOrderStatus` polling every 10 seconds and stop on success, terminal registration status or transport error.

DPD events are processed as a global inbox through `DpdEventSyncService`: one atomic option lock, up to 20 packages per run, latest event per DPD/client order, all matched WooCommerce orders updated from the same package, unmatched events logged without PII and optional `confirm(docId)` after successful processing. `resultComplete=false` continues only when confirm is enabled. `getStatesByDPDOrder` is limited to `orderCost` + `planDeliveryDate` enrichment and never drives universal status. The shared shipment block now reads carrier-neutral status/operation/planned-date fields for DPD, CDEK and Russian Post; DPD documents/labels, scheduled sync, Action Scheduler, legacy cleanup and checkout/recalculation changes are intentionally absent.

0.66.0 note: added real manual DPD shipment creation from the order `Отправления` modal. The selected DPD method is SOAP `order2/createOrder2` from `https://wstest.dpd.ru/services/order2?wsdl` / `https://ws.dpd.ru/services/order2?wsdl`; `createOrder2` is used because the DPD guide marks it as the v2 create method with the same input shape as `createOrder` and extra response fields `pickupDate`/`dateFlag`. Preview remains dry-run, but live create now uses the exact same `DpdShipmentPayloadBuilder` body with `header.datePickup`, `header.senderAddress`, `order.serviceCode`, `serviceVariant`, cargo fields, receiver address and parcel rows. Successful creates save `_wdc_shipments[dpd]` with `pending_creation_in_carrier`, DPD order/request/parcel numbers, service/delivery/terminal/date/cargo snapshots, sanitized request/response, created timestamps and admin marker. Active duplicates are blocked with `DPD отправление уже создано для этого заказа.` No DPD auto-create, cron, status sync, labels, cancellation, COD/NPP or unitLoad were added.

0.66.2 note: fixed DPD shipment modal frontend state after site verification. Opening a DPD modal now runs the preview request immediately, so `preview_pending` is only the server-rendered initial placeholder and not the final opened state. DPD courier address normalization syncs the hidden structured DPD address fields, clears stale address-validation errors on the next preview and requests preview automatically. `ФИО курьера` input/change/blur refreshes preview, with blur forcing an immediate request after clearing the debounce timer. The DPD create button now depends on the latest preview without errors plus local DPD readiness checks; courier does not require receiver terminalCode, while pickup still requires receiver/sender terminalCode. `Комментарии курьеру` is hidden for DPD pickup and visible only for courier. CDEK/Russian Post modal behavior is unchanged.

0.66.1 note: fixed DPD manual create after live site verification. `DpdApiClient::createOrder2()` uses a dedicated 90-second order-create timeout and maps SOAP `Error Fetching http headers` to `dpd_order_create_uncertain`, so managers see that the order may have been created in DPD and must check the DPD cabinet before retrying. DPD duplicate business errors remain errors and do not create local shipment records without a confirmed DPD number. `DPD Расчет` now stores cargo category, sender name and sender phone; the create payload sends `header.payer` from clientNumber, `header.senderAddress.name/contactPhone` from settings and `contactFio` from the modal. The modal stores unique contactFio history values with per-item removal, sends courier comments only as `receiverAddress.instructions`, syncs DaData address parts into hidden structured DPD fields, and strips legacy `dry_run/live_api_call` from visible preview. Russian receiver addresses no longer use courier `receiverAddress.addressString`. Auto retry, status sync, labels and cancellation remain out of scope.

0.65.1 note: refined DPD status mapping and universal shipment statuses. `DeliveryStatus` now includes `pending_creation_in_carrier` (`Попытка создания в ТК`) before `created_in_carrier`, so the new status is first in admin universal-status selects. `src/Shipments/Dpd/DpdStatusMapping.php` keeps the DPD WS Integration Guide 5.5.4 EventCode dictionary at 75 rows, but status mapping now stores EventCode, EventName, optional DPD marker/code name and comments only; ParamName/event parameters are removed from runtime storage and the DPD admin table. Defaults were updated for offer/pre-registration events, unknown-safe problem/payment/cancel/archive events, in-transit delivery/problem-repeat events and return-refusal events. The DPD tab still only saves settings through `SettingsRepository`; this release deliberately does not add DPD status API polling, cron/sync, shipment updates, live create, labels or cancellation.
0.65.0 note: added DPD status mapping settings only. `src/Shipments/Dpd/DpdStatusMapping.php` contains the DPD WS
Integration Guide 5.5.4 EventCode dictionary (75 rows), default universal shipment-status mapping, saved mapping
sanitization and `resolve($eventCode, $paramName = null)`. `WDC → Службы доставки → DPD → Статусы DPD` renders the
dictionary with ParamName details, editable universal-status selects, default values, save and reset-to-default behavior.
Settings are stored in `wdc_core_settings[dpd_status_mapping]` through `SettingsRepository`. This release deliberately
does not add DPD status API polling, cron/sync, shipment updates, live create, labels or cancellation.

0.64.2 note: tightened DPD current-pickup prefill for order-admin delivery recalculation. `OrderDeliveryMetabox::current_pickup_payload()` now emits DPD `terminal_code` only when the saved order context is DPD pickup and a real DPD pickup code exists; DPD courier, empty orders and non-DPD orders no longer expose shipping address or generic snapshot `point_code` as a selected DPD pickup. JS prefill remains carrier-aware, request preview sends `selected_pickup_point` only after explicit manager selection, and backend auto-selected terminalCode remains quote-only.

0.64.1 note: fixed the DPD pickup picker inside order-admin delivery recalculation. DPD pickup popup/list labels now use `Пункт выдачи DPD {terminal_code}` and `Код пункта`, while CDEK and Russian Post labels are unchanged. Current pickup prefill is carrier-aware: DPD pickup is prefilled only from saved DPD pickup meta/terminalCode, so courier, CDEK, Russian Post or other orders keep the DPD pickup selector in `ПВЗ не выбран` state. Auto-selected DPD receiver terminalCode remains quote-only until a manager explicitly selects a pickup point, and saving DPD pickup without a selected point is blocked.

0.64.0 note: DPD is connected to the existing order-admin delivery recalculation flow in `Калькулятор доставок`. The preview path still runs through `OrderQuoteRequestMapper` -> `OrderDeliveryRecalculationService` -> `CheckoutOrchestrator`; DPD rates come from the same `DpdQuoteCarrier` / `getServiceCostByParcels3` terminalCode-aware runtime used by checkout. Admin DPD pickup point selection uses `DpdPickupPointService`, passes selected `terminal_code` into a fresh preview so quote/cache keys do not reuse the auto-selected receiver terminalCode, and then saves canonical `_wdc_pickup_*` meta plus DPD aliases. Saving DPD courier clears pickup meta and receiver terminalCode aliases. `OrderDeliveryReplacementService` updates the WooCommerce shipping item title/total and DPD calculation meta so the `Отправления` draft reads the recalculated serviceCode, delivery type, sender terminalCode and receiver terminalCode for pickup. Live DPD create calls remain disabled; CDEK and Russian Post recalculation behavior is unchanged.

0.63.4 note: micro-refined the DPD preparation `Дата отправки` UI. The label is now its own `.wdc-dpd-date-label` row, with the compact input and `−`/`+` calendar-day buttons in `.wdc-dpd-date-row` below it. Interacting with `input[data-wdc-dpd-date-pickup]` on pointerdown/click/focus attempts native browser `showPicker()` behind a short guard and falls back to `focus()` when unsupported. The step buttons still adjust one calendar day and dispatch input/change; DPD payload logic, checkout pricing, live create calls, CDEK and Russian Post behavior are unchanged.

0.63.3 note: cosmetically refined the manual DPD shipment preparation `Дата отправки` control. The DPD date input now uses a compact inline row instead of the modal-wide input style and has small `−`/`+` buttons with `data-wdc-date-step="-1"` / `data-wdc-date-step="1"` to move by one calendar day. The JS handler falls back to today's date when the input is empty, writes `YYYY-MM-DD`, dispatches `input`/`change`, and lets the existing dry-run preview plus `DpdShipmentDateResolver` validation catch past or non-working dates. DPD payload logic, checkout pricing, live create calls, CDEK runtime and Russian Post runtime are unchanged.

0.63.2 note: applied site-check refinements to the manual DPD shipment preparation modal. The DPD receiver pickup-point block now shows only code/address plus the shared picker button; `Тип точки` remains hidden for DPD while CDEK behavior is unchanged. The DPD-only comment field and DPD payload `comment` were removed. DPD dry-run payloads now include `request.header.datePickup`; the default date uses the store timezone, a 17:00 cutoff and the store calendar (`Календарь магазина`) to choose the current or next working day, with fallback to calendar-day behavior when the calendar service is unavailable. DPD courier normalization keeps using the shared CDEK-like address-processing flow but hides the CDEK city-code row and does not use `cdek_city_code` as a DPD payload field. No live DPD create call, auto creation, labels, statuses, cancellation, checkout pricing, CDEK runtime or Russian Post runtime changes were added.

0.63.1 note: refined the manual DPD shipment preparation modal after site verification. The modal now supports request-only sender and receiver DPD `parcel_shop` selection through the shared pickup picker, pickup/courier scenario switching, and active DPD tariff switching from `DpdSettings` enabled service codes/custom titles. The visible DPD technical service block was removed in favor of `В заказе тариф`, while hidden fields still feed the dry-run payload. DPD courier preparation reuses the CDEK-like address processing button/snapshot flow and uses the normalized address only for the current preview. Single-place weight hint remains common and hides when multiple places exist. No modal changes are saved to order meta/settings, no live DPD create call was added, and DPD auto shipment creation remains explicitly out of scope.

0.63.0 note: added manual-only DPD shipment preparation. `DpdShipmentAdapter` is registered in `CarrierShipmentAdapterRegistry` so DPD orders can show the shared `Отправления` preparation modal and dry-run preview, but `create()` returns `dpd_create_disabled`, labels/status/cancel are disabled, and `ShipmentCreationService` live-create adapters remain Russian Post + CDEK only. `DpdShipmentPayloadBuilder` builds a future-facing `order2/createOrder` preview from saved DPD rate/order meta, recipient/order data and manager-entered places; it never reads checkout `parcel[]`, does not save parcels to order meta, and derives declared value from goods/order value. The DPD `DPD Расчет` tab stores optional `ПВЗ отправителя по умолчанию` terminalCode and shows an active `parcel_shop` summary/warning. DPD auto shipment creation will not be implemented; 0.63.0 does not call live DPD create APIs.

0.62.1 note: cleaned up the DPD `DPD Расчет` admin tab after checkout runtime moved to `calculator2/getServiceCostByParcels3`. The old `Тестовый расчет DPD` form, submit action, result block and `dpd_last_tariff_action_result` storage were removed. The display-only sender city text field/settings were removed; sender information is now shown read-only from `sender_location_id`/location display name plus resolved or overridden DPD cityId. Checkout runtime pricing, Parcels3 pickup/courier payloads, DPD pickup selection, shipment adapter/metabox scope, CDEK and Russian Post behavior are unchanged.

0.62.0 note: DPD checkout runtime pricing now uses `calculator2/getServiceCostByParcels3` with terminalCode after diagnostic prices matched the DPD personal cabinet. Pickup rates send sender `pickup.terminalCode` and receiver `delivery.terminalCode`; courier rates send only sender `pickup.terminalCode` and omit `delivery.terminalCode`. Sender and automatic receiver terminal selection use active `parcel_shop` rows, prefer rows without a same-code `terminal_self_delivery` duplicate, and fall back to a duplicated `parcel_shop` when no unambiguous parcel shop exists. Buyer-selected DPD pickup terminalCode replaces the auto-selected receiver terminalCode and triggers checkout recalculation; quote/cache keys include the selected terminalCode. The old admin terminalCode diagnostic UI/classes were removed, while the low-level `getServiceCostByParcels3` wrapper and runtime request builder remain. No DPD shipment adapter/metabox, shipment creation, labels, COD/NPP, unitLoad, CDEK or Russian Post runtime changes were added.

0.61.0 note: added an admin-only DPD terminalCode pricing diagnostic on the DPD `DPD Расчет` tab. `DpdTerminalCodeTariffDiagnosticService` builds `calculator2/getServiceCostByParcels3` requests with `parcel[]`, cityId and optional/manual terminalCode, performs a side-by-side `getServiceCostByParcels2` comparison, and stores the result through the existing one-shot tariff action result. `DpdPickupPointService::find_diagnostic_parcel_shop_for_city_id()` chooses only `parcel_shop` rows for diagnostics, avoids duplicate `terminal_self_delivery` rows when possible, allows the only duplicated parcel shop as a flagged fallback, and refuses ambiguous cities. Checkout runtime pricing remains unchanged: `DpdTariffCalculationService` and `DpdQuoteCarrier` still use `getServiceCostByParcels2` without terminalCode. No DPD shipment adapter/metabox, pickup import, CDEK or Russian Post runtime changes were added.

0.60.4 note: fixed stale checkout pickup-map distances after address search or browser geolocation changes the active distance origin. `assets/frontend/pickup-map/wdc-pickup-map.js` now uses a shared refresh path that recalculates `distanceMeters`/`distanceText`, resorts `visiblePoints`, rerenders markers and then rerenders the side list so `Ближайший ПВЗ` uses the new nearest point. DPD pricing, `getServiceCostByParcels2`, terminalCode-aware pricing, pickup import, shipment adapter/metabox, CDEK and Russian Post backend behavior are unchanged.

0.60.3 note: DPD pickup `raw_json` is now diagnostic/admin/internal storage only. DPD checkout REST responses, checkout session snapshots and order meta do not expose or persist `raw_json`, while the local `wdc_dpd_pickup_points.raw_json` column remains available for admin diagnostics. `DpdPickupPointService` now consumer-deduplicates active rows by `terminal_code`: when both `parcel_shop` and `terminal_self_delivery` exist, checkout/map/search and terminal-code validation prefer `parcel_shop`; terminal-only rows are still returned. Import safe-replace and stored supplementary terminal rows are unchanged. DPD pricing, `getServiceCostByParcels2`, terminalCode-aware pricing, shipment adapter/metabox, CDEK and Russian Post behavior are unchanged.

0.60.2 note: DPD pickup schedules are now formatted before they reach checkout map balloons, side cards and saved pickup cards. `DpdPickupPointScheduleFormatter` handles array/object/JSON-string/plain-string schedule values, prefers `SelfDelivery`, then `SelfPickup`, `Payment`, `PaymentByBankCard`, compacts all-week schedules to strings like `Пн–Вс: 10:00–22:00`, and is used by the DPD normalizer plus REST/checkout summary layers so existing JSON schedule strings in `wdc_dpd_pickup_points` render readably. Raw DPD `raw_json` stays unchanged. DPD pricing, `getServiceCostByParcels2`, terminalCode-aware pricing, shipment adapter/metabox, CDEK and Russian Post behavior are unchanged.

0.60.1 note: fixed the checkout pickup resolve-location order for DPD/CDEK skip handling. `CheckoutPickupPointRestController::resolve_location()` now reads the point payload or `point_id` first, then returns `requires_location_change=false` for DPD and CDEK without invoking the Russian Post location resolver. Russian Post resolve-location still follows the resolver path. DPD pricing, terminalCode-aware tariff requests, shipment adapter/metabox, CDEK runtime and Russian Post runtime are unchanged.

0.60.0 note: DPD checkout pickup selection foundation is connected. DPD pickup rates now set `requires_pickup_point=true` and reuse the shared checkout pickup map/list UI. The shared `/wdc/v1/points` endpoint supports `carrier=dpd` with `location_id`/optional search and returns active local rows from `wdc_dpd_pickup_points` via `DpdPickupPointService`. The checkout save endpoint and validation verify that the selected DPD `terminal_code` exists and is active before saving. Orders store canonical `_wdc_pickup_*` meta plus DPD aliases such as `_wdc_dpd_pickup_terminal_code`, `_wdc_dpd_pickup_type`, `_wdc_dpd_pickup_name`, `_wdc_dpd_pickup_address`, `_wdc_dpd_pickup_city_name`, coordinates and `_wdc_dpd_pickup_source`. DPD pricing is unchanged: runtime still uses `calculator2/getServiceCostByParcels2`, cityId/selfPickup/selfDelivery, and does not send `terminalCode`. No DPD shipment adapter/metabox, CDEK runtime or Russian Post runtime changes were added.

0.59.2 note: removed the obsolete private `DpdPickupPointRepository::mark_source_inactive()` helper after confirming it had no callers after the DPD pickup safe-replace change. The active safe-replace flow is unchanged: imports upsert a non-empty valid point set first and then mark inactive only old active rows missing from the new source key set. Checkout pricing, `getServiceCostByParcels2`, shipment adapter/metabox, CDEK and Russian Post behavior are unchanged.

0.59.1 note: DPD pickup import now uses safe-replace semantics. `DpdPickupPointImportService` refuses to call repository replacement when DPD returns no rows or when rows are fetched but zero valid points are normalized, returning clear reports: "Existing points were left unchanged." `DpdPickupPointRepository::replace_all_for_source()` no longer starts by marking the full source inactive; it upserts the new valid point set first and then marks inactive only old active rows for the same source whose `terminal_code + type` key is absent from the new set. Reports now include `marked_inactive`, and DPD pickup smoke tests cover empty API responses, unrecognized response shapes, successful upsert and stale-row inactivation. Checkout pricing, `getServiceCostByParcels2`, shipment adapter/metabox, CDEK and Russian Post behavior are unchanged.

0.59.0 note: DPD pickup points / terminals foundation is implemented. The DPD geography WSDL and `docs/dpd/ws-integration-guide.docx` were checked for `geography2/getParcelShops` and `geography2/getTerminalsSelfDelivery2`: `getParcelShops` is the primary PVZ/parcel-shop source with `request.auth` plus optional `countryCode`, `regionCode`, `cityCode`, `cityName` filters; `getTerminalsSelfDelivery2` is the supplementary self-delivery terminal source with direct `auth`. Both responses expose codes, address/city/region/country fields, coordinates and schedule; `getParcelShops` uses `code`, while terminals use `terminalCode`. WDC now has `DpdApiClient` wrappers, migration `0031_create_dpd_pickup_points_table.php`, `src/Carriers/Dpd/Pickup/` repository/normalizer/import/read-only services, a manual `DPD ПВЗ` admin tab with import buttons and diagnostics, and `tests/dpd/run-dpd-pickup-points-smoke.php`. Checkout pricing remains on `calculator2/getServiceCostByParcels2` with cityId/selfPickup/selfDelivery only; no checkout map/selection, terminalCode-aware pricing, shipment adapter/metabox, labels, COD/NPP, CDEK or Russian Post runtime changes were added.

0.58.9 note: Fixed the DPD 3D parcel packer shelf row state. `DpdParcelBuilder::place_unit()` now tracks closed row width separately from the current row width, so placing several items into `current_row` no longer double-counts the occupied layer width. `getServiceCostByParcels2`, DPD checkout runtime behavior, shipment adapter/metabox scope, CDEK and Russian Post behavior are unchanged.

0.58.8 note: DPD checkout parcel building now uses a fast deterministic 3D shelf/bin packing model while keeping `calculator2/getServiceCostByParcels2` as the runtime tariff method. `DpdParcelBuilder` supports `box_50_50_30` and `box_40_40_40`, sends actual occupied dimensions rather than full box format dimensions, keeps long items over 49 cm as separate parcels, aggregates small items with volume <=50 cm3 into one synthetic 3:1:1 volume block, optimizes identical item groups into grid blocks, attempts one box and then bounded two-box distribution before falling back to stacked rows, and adds packaging weight per parcel through the existing `PackagingWeightCalculator` tiers. Diagnostics now include packing strategy, tried/selected box formats, small-item and identical-grid counters, parcel goods/packaging/final weights, and packing limit reason. DPD pickup points, terminalCode pricing changes, shipment adapter/metabox, COD/NPP, `unitLoad`, labels and full optimal bin packing remain out of scope.

0.58.7 note: Documented DPD terminalCode pricing debt without changing runtime code. Current checkout calculation remains on `calculator2/getServiceCostByParcels2`: the pickup/terminal group sends `selfPickup=true` and `selfDelivery=true`, while the courier group sends `selfPickup=true` and `selfDelivery=false`. Future DPD pickup point / terminal map work must obtain concrete `pickup.terminalCode` and `delivery.terminalCode`, test `getServiceCostByParcels3` with `parcel[]` plus terminal codes against the DPD cabinet, compare prices, and only then switch to terminalCode-aware pricing. `getServiceCost3` is not the default target because it does not match the current `parcel[]` packaging-place model.

0.58.6 note: Review cleanup verified `single_sku_box_dimensions` with a full project search before changing code. The DPD-local private helper in `src/Carriers/Dpd/Tariff/DpdParcelBuilder.php` had no direct callers, was no longer called through `calculated_single_box_dimensions()`, and had no test or documentation references, so it was removed. The same-named CDEK helper remains untouched because `src/Carriers/Runtime/CdekCarrier.php` still calls it. DPD runtime behavior is unchanged: long items over 49 cm become separate parcels, regular items go through `single_box_fit()` then stacked rows then package dimensions then defaults, and DPD still uses `calculator2/getServiceCostByParcels2`.

0.58.5 note: DPD checkout parcel building now expands product quantities before packaging and treats items with any side over 49 cm as long items. Each long item becomes its own `DpdTariffParcel` with quantity `1`, its own dimensions and its own weight, and is excluded from the regular common box. Remaining regular items are packed into one parcel by `single_box_fit()` when it fits; otherwise the deterministic stacked-rows fallback orients each item as length=max side, width=middle side and height=min side, closes rows at a 45 cm row width threshold, and returns one regular parcel. Package-level dimensions are now used only when item dimensions are missing; DPD defaults remain the final fallback. `DpdQuoteCarrier` diagnostics include `parcels_count`, `long_item_parcels_count`, `regular_items_count`, `package_builder_source`, `parcel_dimensions` and `box_limit`. DPD still uses `calculator2/getServiceCostByParcels2`, and DPD `parcel[]` means packaging places, not cart items. Pickup points, shipment adapter/metabox, labels, order creation, COD/NPP, `unitLoad` and complex 3D bin packing remain out of scope.

0.58.4 note: DPD tariff calculation now uses `calculator2/getServiceCostByParcels2` as the primary calculator method while leaving the older Parcels3 wrapper available in `DpdApiClient` for history/compatibility. `DpdTariffCalculationService` calls `getServiceCostByParcels2`, keeps the same `request` SOAP wrapper/auth strategy, and accepts explicit `params['parcels']` when supplied; otherwise it falls back to the old single-parcel weight/dimensions/default settings path so the admin diagnostic form remains single-parcel compatible. `src/Carriers/Dpd/Tariff/DpdParcelBuilder.php` is the new checkout package-place builder: DPD `parcel[]` means packaging places, not cart items. Runtime starts with a safe single-box model, using package-level dimensions/total weight first, then a deterministic item-fit box inside the temporary 50x50x30 cm limit, then DPD default weight/dimensions. `DpdQuoteCarrier` now delegates parcel creation to that builder, passes `parcels[]` into the tariff service, and stores `parcels_count`, total weight, dimensions, declared value and `package_builder_source` diagnostics in quote metadata. `DpdTariffRequestBuilder` still sends `pickup.cityId`, `delivery.cityId`, `selfPickup`, `selfDelivery`, `declaredValue` and `parcel[]` only; it does not add `extraService`, `unitLoad`, COD/NPP or cart items as parcels. DPD pickup points, shipment adapter/metabox, labels, order creation and complex multi-box/bin-packing remain out of scope.

0.58.3 note: DPD checkout tariff candidates are now filtered inside each delivery-type group after enabled service-code filtering and numeric-cost validation, before `DeliveryQuote` is returned. `DpdQuoteCarrier::filter_rates_by_price_and_delivery_days()` compares only final `DeliveryRate` price and normalized `DateRange` min/max days; tariff title, service name and service-code semantics are intentionally ignored. If two DPD tariffs have the same known min/max delivery period, only the cheaper one survives. A tariff is also removed when another tariff has known min/max days that are no worse, a price that is no higher, and at least one strictly better parameter. Unknown-duration tariffs are kept and are not removed by speed dominance. Filtering is scoped to the current quote/delivery type, so pickup filtering never removes courier candidates and vice versa. `DeliveryQuote::raw_reference` stores `dpd_filter_removed_count` and `dpd_filter_removed_tariffs` diagnostics. DPD API payloads, fixed `selfPickup=true` runtime logic, courier enable checkbox, pickup points, shipment adapter/metabox, CDEK and Russian Post behavior are unchanged.

0.58.2 note: DPD checkout runtime now follows the clarified DPD business model. Checkout shipment is always calculated from a DPD terminal (`selfPickup=true`), so the old runtime pickup mode setting is no longer rendered, saved or read. Terminal delivery is the default pickup entry and sends `selfDelivery=true`; courier delivery sends a separate `calculator2/getServiceCostByParcels3` request with `selfDelivery=false` only when the DPD `Тарифы` checkbox `Использовать курьерские тарифы` is enabled. `CheckoutOrchestrator` adds the DPD pickup entry whenever the DPD delivery service is active, and adds the courier entry only when that checkbox is on. `DpdQuoteCarrier` reads `customer_context.delivery_type`, defaults to pickup, returns `courier_rates_disabled` without a SOAP call when courier is disabled, and keeps pickup/courier groups separate even when the same DPD `serviceCode` appears in both responses. Default enabled DPD service codes remain `ECN,CSM,MXO`; unchecked-all still means no DPD rates. `quote_id` diagnostics now include `delivery_type`, fixed `selfPickup=true`, delivery-mode-derived `selfDelivery`, courier-rates enablement, enabled service codes, city IDs, package params, calculation date and environment. DPD pickup points/map/terminal selection, shipment creation, statuses, labels, COD/NPP, `unitLoad` and receipts remain out of scope, and DPD is still not registered in `CarrierShipmentAdapterRegistry` or metabox shipment actions.

0.58.1 note: DPD checkout runtime now follows the existing CDEK/Russian Post grouped tariff UX instead of exposing every DPD `serviceCode` as an independent shipping method. `DpdQuoteCarrier` marks DPD options with `tariff_selector_group`, `checkout_group_id`, `selected_tariff_object`, `selected_tariff_title`, `pickup_method_title` and `courier_method_title`, so WooCommerce checkout groups options into one `DPD курьером` or calculation-only `DPD до пункта выдачи` method with DPD service options as `tariff_variants`. The DPD `Основное` tab now stores pickup/courier method titles; the new DPD `Тарифы` tab stores fixed known service codes, enabled checkboxes, custom tariff titles and checkout pickup/delivery modes. Default enabled DPD service codes are `ECN,CSM,MXO`, and unchecked-all means no DPD checkout rates. The old runtime method title prefix is no longer rendered or used. `quote_id` diagnostics now include receiver location, sender/receiver city IDs when available, package weight/dimensions/declared value, pickup/delivery modes, enabled service codes, calculation date and DPD environment. Terminal delivery sets `selfDelivery=true` and returns a pickup-type calculation rate with `dpd_pickup_points_not_implemented=true`; DPD pickup point API, map/selection, shipment creation, statuses, labels, COD/NPP, `unitLoad` and receipts remain out of scope. DPD is still not registered in `CarrierShipmentAdapterRegistry` or metabox shipment actions.

0.58.0 note: DPD checkout runtime rates are now enabled through the existing `CarrierRegistry`/`CheckoutOrchestrator` architecture. `src/Carriers/Runtime/DpdQuoteCarrier.php` maps `DpdTariffCalculationService` options from `calculator2/getServiceCostByParcels3` into courier `DeliveryRate` objects, uses selected checkout `location_id` plus `wdc_location_delivery_codes.dpd_city_id` through `DpdCityResolver`, and builds package params from cart/domain package weight, default DPD dimensions, and cart declared value. The first runtime mode is delivery-to-door only (`selfDelivery=false`); pickup mode can be `door` or `terminal` via DPD calculation settings, with default `door`. `dpd_runtime_allowed_service_codes` defaults to `MAX,NDY`; empty value allows all returned DPD service codes, and missing/non-numeric `cost` options are skipped. DPD service still must be enabled in delivery services and credentials/sender/receiver city IDs must be valid; API errors simply hide DPD rates and log diagnostics. DPD is not registered in `CarrierShipmentAdapterRegistry`, shipment creation UI, statuses, labels, pickup points, maps, COD/NPP, `unitLoad`, receipts, or cron. CDEK and Russian Post runtime behavior are unchanged.

0.57.1 note: DPD tariff calculator now sends `calculator2/getServiceCostByParcels3` through an explicit SOAP `request` wrapper strategy, so centralized `DpdSoapRequest` auth is placed under `request.auth` for calculator calls while geography calls keep the direct payload shape. Tariff calculation checks credential completeness before SOAP and returns `DPD credentials are incomplete for current environment.` without making an API call when the active environment is missing client number/key. DPD tariff debug output stores only redacted payload shape metadata, not plaintext `clientKey`. DPD Geography and DPD Расчет action result notices are one-shot after their blocks render; last import report and current import progress remain persistent. DPD remains admin-only and is still absent from checkout, shipment adapters, metabox runtime, CDEK and Russian Post behavior.

0.57.0 note: DPD tariff calculation foundation is implemented as admin-only diagnostics without checkout runtime registration. `DpdApiClient::getServiceCostByParcels3()` calls the existing `calculator2` SOAP service through `DpdSoapClientInterface`/`DpdSoapRequest`, so credentials/auth remain centralized. `src/Carriers/Dpd/Tariff/` contains request, parcel, builder, calculation service, result and tolerant normalizer classes. The new `DPD Расчет` delivery-service tab stores sender/default parcel settings, runs a protected test calculator, resolves receiver `location_id` through `DpdCityResolver` and `wdc_location_delivery_codes.dpd_city_id`, stores a visible action result, and can show raw debug response when DPD debug is enabled. DPD remains absent from `CarrierRegistry`, `CarrierShipmentAdapterRegistry`, checkout rates, shipment metabox actions, order creation, statuses, labels, pickup points, COD, `unitLoad`, receipts, cron and tariff sync. CDEK and Russian Post runtime behavior are unchanged.

0.56.9 note: DPD География now has a dedicated visible action result block stored separately from import progress and the last import report. `DpdSettings` stores the last geography action result with `type`, `title`, `message`, compact `details`, and `created_at`; the DPD Geography tab renders it as a WordPress notice after redirects. Diagnostics, manual `cityId` mapping, DaData fallback, SFTP/no-ssh2 warnings, manual/FTP import start, settings save, and reset now save structured results so admins can see `location_id`, `cityId`, `source`, `matched_by`, saved status, token usage hints, source file, file size, phase, and warning messages. Runtime carriers, checkout, shipment adapters, parser, staging, matching, finalization, DaData API logic, CDEK and Russian Post behavior are unchanged.

0.56.8 note: DPD Geography CSV parser now uses a custom bounded `fread()` line reader and `str_getcsv()` for row parsing. It no longer depends on `fgets()` or `fgetcsv()` for file reading, which avoids Windows/Local PHP buffering false positives on valid `GeographyNewDPD_*.csv` files. The reader supports Windows-1251 without BOM and LF, CRLF, or CR line endings, preserves offset-based step import, and still rejects rows that exceed `MAX_CSV_ROW_LENGTH=262144` before any line ending is found. Staging architecture, matching, finalization, SFTP warning behavior, DaData fallback, checkout/runtime carriers and shipment adapters are unchanged.

0.56.7 note: DPD Geography CSV parser now reads physical lines with bounded `fgets(MAX_CSV_ROW_LENGTH)` and parses them with `str_getcsv()` instead of using `fgetcsv()` directly. The row-length guard only fires when the line lacks `\n`/`\r` and the file is not at EOF, avoiding Windows/Local PHP false positives where valid rows advanced by roughly the buffer length. Windows-1251 without BOM remains supported through row-level UTF-8 conversion, no full scan is added, and oversized rows still fail as controlled diagnostics. SFTP unavailable warnings remain separate from import job errors and do not mutate the active import state. Staging architecture, matching, finalization, DaData fallback, checkout/runtime carriers and shipment adapters are unchanged.

0.56.6 note: DPD Geography CSV parsing is now bounded-row memory-safe for Windows/Local PHP. `DpdGeographyCsvParser` uses `MAX_CSV_ROW_LENGTH=262144` for every `fgetcsv()` call instead of `length=0`, so reading the header or import steps cannot request an unbounded buffer on a valid large `GeographyNewDPD_*.csv`. DPD files encoded as Windows-1251 without BOM are still converted to UTF-8 row by row. Oversized/broken rows now raise controlled parser errors; start failures are caught by import start diagnostics, and step failures move the import state to `failed` with `DPD geography CSV parse failed: ...` instead of a PHP fatal. Staging architecture, matching, finalization, SFTP warning behavior, DaData fallback, runtime carriers and shipment adapters are unchanged.

0.56.5 note: DPD Geography import no longer performs a full preliminary CSV scan. `DpdGeographyCsvParser::inspect_header()` reads only the first non-empty row to detect columns and data offset; start stores `file_size`, keeps `total_rows=0`, and progress is calculated from `byte_offset / file_size`. The DPD География UI now reports processed row count plus percent of file read instead of `0 из 0 строк`. Missing PHP `ssh2`/`ssh2_connect` is no longer treated as a failed import job: `DpdGeographyFtpClient` returns `status=warning` with a manual-upload message, does not create a job, and does not mutate the current import state. Manual CSV upload remains the fully supported import path. Staging architecture, matching, finalization, DaData fallback, runtime carriers and shipment adapters are unchanged.

0.56.4 note: DPD Geography import cleanup now respects an internal `delete_file_on_finish` state flag. Manual upload and SFTP jobs set it to `true`, so their temporary CSV files are deleted on finish/reset. CLI/smoke/diagnostic imports through `DpdGeographyImportService::import_file()` set it to `false`, so caller-provided CSV files are preserved while the serialized location index and per-job staging table are still removed. The flag is hidden from public AJAX state. Staging architecture, matching, finalization, DaData fallback, runtime carriers and shipment adapters are unchanged.

0.56.3 note: DPD Geography import is now staging-first. Import steps no longer write directly to `wdc_location_delivery_codes`; each job creates a temporary `wdc_dpd_geography_stage_<job_hash>` table with `location_id`, `dpd_city_id`, `match_method`, `status`, and `updated_at`. Matching still uses `DpdLocationIndex`, but candidates/conflicts are written to staging only. On EOF finalization, WDC clears only `dpd_city_id` values absent from candidate staging rows, upserts candidate rows into `wdc_location_delivery_codes`, stores the final report, and removes the CSV temp file, index file, and staging table. Reset removes the same temporary resources and marks the job cancelled. Import state is intentionally light: job metadata, paths hidden from public state, `stage_table`, offsets, counters, timestamps, errors, and last message; the cancelled `seen_mappings`/`saved_by_job`/`blocked_locations` arrays are no longer stored in the option. DPD remains unregistered in checkout/runtime and shipment adapter registries; CDEK/Russian Post runtime behavior is unchanged.

0.56.2 note: DPD Geography import now runs as a stateful AJAX step import for production-sized `GeographyNewDPD_*.csv` files. SFTP/manual uploads create an import job, count rows, build a reusable `DpdLocationIndex` from active RU `wdc_locations` columns, process CSV rows in limited batches, update visual progress in the DPD География tab, and save the final report in DPD settings. Matching no longer performs SQL lookup per CSV row: FIAS, city FIAS, KLADR variants, city KLADR variants, and conservative name keys are resolved through the prebuilt index, with ambiguous keys excluded from auto-match. Import state is stored in `wdc_dpd_geography_import_state`; CSV/index temp files are removed on finish or reset. DPD remains unregistered in checkout/runtime and shipment adapter registries; CDEK/Russian Post runtime behavior is unchanged.

0.56.1 note: DPD Geography Import Foundation is added without DPD tariffs, checkout runtime, pickup points, shipment creation, statuses, labels, cron or Action Scheduler jobs. `GeographyNewDPD_*.csv` can be imported from the DPD SFTP settings in the admin DPD География tab or uploaded manually; the parser streams `;` CSV rows, supports UTF-8/Windows-1251, imports RU rows only, and writes only `wdc_location_delivery_codes.dpd_city_id` plus `updated_at`. Matching is conservative: FIAS/city FIAS exact, normalized KLADR/city KLADR, then unique name+region+district+type fallback. `DpdDaDataDeliveryFallbackService` uses the shared DaData token pool and increments the same usage counters for a single admin-requested `location_id`. The last import report is stored in DPD settings. DPD remains unregistered in `CarrierRegistry` and `CarrierShipmentAdapterRegistry`; CDEK/Russian Post runtime behavior is unchanged.

0.56.0 note: DPD cityId storage moved from the cancelled generic `wdc_location_carrier_codes` model to the new 1:1 `wdc_location_delivery_codes` table keyed by `location_id`. The table currently stores only `location_id`, nullable `dpd_city_id`, and `updated_at`; future carriers can add columns such as `yandex_city_id` or `pec_city_id`. `LocationDeliveryCodeRepository` owns reads, upserts, deletes, and orphan cleanup. `DpdCityResolver` requires a saved WDC `location_id`, reads `dpd_city_id` from the new table only, and still never calls live DPD geography APIs. Manual DPD cityId mapping in the DPD settings tab writes to `wdc_location_delivery_codes`. Legacy `LocationCarrierCodeRepository` and `wdc_location_carrier_codes` creation/use were removed from code and tests. No legacy data migration is provided because the old table was manually removed before this stage.

0.55.0 note: DPD geography layer is added as a production-safe foundation without tariff calculation, checkout runtime, pickup points, shipment creation, labels, statuses, cron or FTP import. After live checks returned `java.lang.NullPointerException` for both sparse `getPossibleExtraService` payloads and `getCitiesCashPay(countryCode)`, automatic live API city lookup is disabled. `DpdCityResolver` now resolves DPD `cityId` only from existing `wdc_location_carrier_codes` mappings and returns a manual-mapping-required diagnostic message when no mapping exists. The supported path for this stage is manual admin mapping into `wdc_location_carrier_codes` with `carrier_key=dpd` and `external_code=<dpd_city_id>`. `DpdApiClient` keeps low-level geography wrappers for future verified API work, but `DpdCityResolver` does not call them automatically. Future tariff work must require an existing DPD cityId mapping or a separately implemented/verified geography import. DPD is still not registered in `CarrierRegistry` or `CarrierShipmentAdapterRegistry`.

0.54.0 note: DPD foundation is added without runtime tariff, pickup, shipment, status, label, COD, `unitLoad` or receipt functionality. The built-in `dpd` delivery service is created disabled by default, DPD credentials are stored through encrypted settings with redacted dry diagnostics, and `src/Carriers/Dpd/` contains the replaceable `DpdSoapClientInterface` transport boundary, settings, credentials, endpoints, API client, SOAP DTOs and future `DpdCityResolver` boundary. DPD is intentionally not registered in `CarrierRegistry` or `CarrierShipmentAdapterRegistry`, so it cannot appear in checkout rates or shipment actions at this stage. See `docs/wdc-dpd-integration.md`.

0.53.2 note: CDEK BARCODE print status detection no longer uses `date_time` ordering. For `GET /v2/print/barcodes/{uuid}`, WDC now treats a PDF `entity.url` as `READY`, then applies print-form status priority `READY`, `INVALID`, `REMOVED`, `PROCESSING`, `ACCEPTED`. This fixes CDEK responses where all print statuses have the same timestamp and the form is already downloadable.

0.53.1 note: CDEK BARCODE preparation now recovers from print requests that stay in `ACCEPTED`/`PROCESSING`: pending print UUIDs track creation/check timestamps and check counts, and after 60 seconds or more than 30 checks WDC recreates the print request while keeping READY labels cached for 50 minutes. Shipment AJAX actions now return one fresh adapter-built UI payload after create/update/cancel/remove/manual attach, so Russian Post buttons switch immediately to the saved-shipment state after AJAX create or status refresh.

0.53.0 note: the order `Отправления` lifecycle now has a shared carrier action layer for the current carriers. `CarrierShipmentAdapterInterface` and `CarrierShipmentAdapterRegistry` register Russian Post and CDEK adapters for presentation labels, status payloads, manual attach, cancel, local remove, label actions and auto-sync dispatch. `OrderShipmentsMetabox` now delegates update/cancel/remove/manual attach through the registry, CDEK label download is exposed as an adapter label action, and `ShipmentStatusAutoSyncService` uses adapter support/tracking/throttle/update hooks instead of hardcoded carrier dispatch.

0.52.0 note: CDEK now participates in the shared shipment status auto-sync controlled by `WDC -> Статусы -> Автообновление статусов отправлений`. Automatic and manual bulk runs dispatch CDEK shipments through `CdekOrderStatusService`, reuse raw CDEK latest-status selection, save universal status mapping, respect terminal universal statuses, throttle CDEK status calls to about 100 rps, and report `updates_by_carrier[cdek]` in diagnostics. The CDEK service `Расчет` tab renamed sender city to its real door-origin use, and the CDEK service `Правила` tab now has a dedicated test calculator with city resolution, one-package CDEK tariff calculation and before/after rules output.

0.51.3 note: CDEK label download no longer relies on a hidden iframe after BARCODE `READY`. The admin script downloads the ready PDF with `fetch()`, validates HTTP/PDF/blob state, starts a temporary anchor download from an Object URL, and shows a visible error toast when the endpoint returns an error or non-PDF response. The backend ready-PDF path now rejects empty, failed, or explicit non-PDF responses.

0.51.2 note: CDEK label download now runs as an AJAX-controlled flow: the order metabox keeps only `Скачать этикетку`, requests/refreshes BARCODE print status in short polling, caches a READY print UUID for 50 minutes, and then downloads the PDF from the final endpoint. The final PDF endpoint only streams cached READY forms and does not create or long-poll print requests. Saving `Статусы СДЭК` now redirects back to the CDEK service tab.

0.51.1 note: CDEK shipment label actions gained manager-facing wording and the CDEK status mapping UI moved from the general shipment statuses page into `WDC -> Службы доставки -> СДЭК -> Статусы СДЭК`; storage remains unchanged. The mapping list now covers every order status code from CDEK documentation appendix `Приложение 1. Статусы заказов`.

0.51.0 note: CDEK shipments now support BARCODE PDF actions directly in the order shipment metabox. The actions create the documented async CDEK print form (`POST /v2/print/barcodes`, `copy_count=1`, `format=A6`, `lang=RUS`), poll print status until `READY`, and stream the resulting PDF without storing print files or print UUIDs in order meta. The CDEK mapping maps raw CDEK order statuses to the existing universal shipment statuses while leaving CDEK cancel/remove rules based on raw `entity.statuses[]`.

0.50.5 note: the CDEK managed tariffs admin page now has compact bulk controls above the tariff table. Administrators can delete all managed CDEK tariffs, enable/disable all tariffs, or enable/disable only one `delivery_mode` group (`1` door-door, `2` door-warehouse, `3` warehouse-door, `4` warehouse-warehouse). Bulk actions are POST-only, reuse the existing delivery-services nonce/capability gate, clear delivery quote cache, and show an admin notice with the number of changed/deleted tariffs.

0.50.4 note: CDEK postamats are now treated consistently as the warehouse/PVZ side of tariff direction and order creation. The CDEK tariff sync fallback classifier recognizes `дверь-постамат`, `постамат-дверь`, `склад-постамат`, `постамат-постамат` and English `locker/postamat` variants, while `POSTAMAT`/`LOCKER` deliverypoints normalize to `Постамат СДЭК`. Shipment creation still uses the same `delivery_point` architecture as PVZ, and the shipment modal now shows `Тип точки` for known CDEK pickup/postamat rows.

0.50.3 note: CDEK courier shipment creation now handles the full door/warehouse direction of the managed tariff. Tariffs store editable `delivery_mode` values (`1` door-door, `2` door-warehouse, `3` warehouse-door, `4` warehouse-warehouse); order creation sends `from_location` for door-origin modes and `shipment_point` only for warehouse-origin modes. The shipment modal shows sender-door or sender-PVZ context accordingly, shows `Комментарий курьеру` only for recipient-door CDEK modes, and sends it to CDEK `POST /v2/orders.comment` when filled. Admin delivery recalculation now reuses the common apartment/office/premise splitter before DaData normalization, so normalized WooCommerce shipping addresses keep flats such as `кв 150`.

0.50.1 note: CDEK courier shipment address preparation keeps the DaData verification step but now reuses known CDEK recipient city codes from saved calculation/rate metadata before doing any location lookup. Admin delivery replacement syncs `rate_meta.location.cdek_to_city_code` into `_wdc_delivery_calculation_data.api.cdek_to_city_code`, shipment draft fallback reads `_wdc_platform_rate_meta` and saved `request_payload_sanitized.to_location.code`, and CDEK courier address processing calls the DaData suggestion stack directly so the old `Внешний нормализатор не настроен.` message is replaced by a CDEK-specific DaData setup error.

0.50.0 note: CDEK courier shipment registration now uses a carrier-aware recipient address preparation path. The shipment modal sends CDEK courier address processing through DaData and resolves the recipient CDEK city code through `GET /v2/location/cities` before creation. Prepared draft data stores `cdek_city_code`, city, postal code, delivery-address part and coordinates; CDEK `to_location` uses those fields and blocks creation if the city code cannot be resolved. Russian Post keeps its existing address normalization, and CDEK pickup flows continue to use `delivery_point`.

0.49.2 note: shipment package table UX was corrected. Product rows now inherit WooCommerce product/variation length, width and height instead of falling back to `0.1` when catalog data exists; item price/dimensions accept dot or comma decimals; split/base quantities are clamped so every row stays at least `1` and the group total remains the ordered quantity; split/remove actions are borderless centered icons; manual SKU search closes on focus-out without auto-filling and supports partial `_sku` matches for products and variations.

0.49.1 note: shipment preparation packages were hardened after the first 0.49.0 review. The `Грузоместа` tab now renders the same item table, summary, split controls and manual item rows for all carriers, including Russian Post; CDEK still maps the data into `/v2/orders` while other carriers can ignore item-level rows safely. Deleting a package forces split rows back into their base rows and restores base item cost/weight/dimensions from original order data, while deleting an individual split row preserves manager edits. Item cost/dimensions accept comma or dot decimals; package place dimensions remain integer-only.

0.49.0 note: shipment preparation package UI is improved on `feature/shipment-modal-packages-ui`. CDEK settings now store `cdek_shipment_point_address`; the order shipment modal renders sender pickup point code plus address, allows a temporary CDEK sender pickup replacement without changing settings, and submits the chosen code as `shipment_point`. The universal `Грузоместа` tab now has API-weight hints for single-package drafts, package summaries with package weight and assigned item totals, Russian item-table labels, compact numeric constraints, split rows with delete/rebalance behavior, manual item rows, and WooCommerce product search for manual SKU entry.

0.46.4 note: nullable CDEK tariff limit persistence is fixed. Insert/update formats for `weight_*`, `length_*`, `width_*` and `height_*` are built dynamically so `null` API values remain SQL `NULL` rather than being sent through `%f` and becoming `0.000`; numeric limits still use decimal/float storage.

0.46.3 note: CDEK tariff sync now stores API limit fields from `GET /v2/calculator/alltariffs`: weight min/max/calculation max and length/width/height min/max. The CDEK tariff admin table displays compact limits, Russian labels `до ПВЗ` / `до двери` for delivery type, and active-first/name/code sorting. Existing custom title/comment/active state survives sync; without a separate manual-override flag, delivery type is still refreshed from API. Safe mojibake normalization fixes obvious CDEK strings before storing names/modes.

0.46.2 note: delivery rate cache reset now invalidates every checkout session through a global package-hash marker. `wdc_delivery_rates_cache_version` is added to WooCommerce shipping packages, and full cache reset bumps it after clearing WDC transients/runtime quote cache. Manual reset, CDEK tariff save and confirmed CDEK tariff sync all bump the version, so a disabled managed tariff disappears for existing checkout sessions without requiring a cart change.

0.46.1 note: delivery tariff cache reset now clears all checkout rate layers affected by managed CDEK tariff changes: WDC quote transients, runtime quote namespace, WooCommerce `shipping_for_package_*` session rates, and WDC runtime rate/tariff session caches. Saving CDEK tariff rows or confirming CDEK tariff sync automatically clears the same quote layers, so disabled tariffs disappear from checkout after refresh without requiring a cart change.

0.46.0 note: CDEK tariff management is implemented on `feature/cdek-tariffs-management`. The predefined CDEK service has a `Тарифы` tab that syncs available contract tariffs from `GET /v2/calculator/alltariffs`, shows a preview diff before applying, stores `tariff_code`, CDEK name, custom site title, delivery type, admin comment, active flag and last sync time, and preserves custom title/comment/active state during sync. Runtime CDEK quotes use managed tariff title/type/active state when a row exists. Insurance was audited in `docs/wdc-cdek-insurance-audit.md`; CDEK order creation, statuses, webhooks and print forms remain future work.

0.45.22 note: temporary checkout pickup diagnostics were removed after the canonical bucket/session investigation. `wdc-pickup-checkout.js` no longer writes grouped console traces, pickup localization no longer exposes debug flags, and PHP pickup save/state/localized/validation/session-write debug logs were removed. Restore/save/validation business logic remains unchanged; real save failures still use normal warning/error logging.

0.45.21 note: checkout pickup restore no longer treats different destination fingerprint formats as a location change when stable destination identity still matches. The frontend compares selected pickup points by `location_id`, FIAS, GAR, city/region and postcode+city before fingerprint fallback, and keeps explicit `destination_*` aliases in saved point payloads.

0.45.20 note: checkout pickup card restore now tolerates generic or stale pickup containers after WooCommerce reload. The frontend matches containers against the active pickup family, rewrites stale `data-shipping-method-id` on the active/generic block, and applies the selected family bucket.

0.45.19 note: checkout frontend restore now treats backend pickup state as a family-keyed dictionary regardless of camelCase/snake_case response shape. `wdc-pickup-checkout.js` extracts and merges localized/REST `pickupSelections`, selected point aliases and state responses into `selectedPickupPoints`, then reapplies the active family bucket after reload/`updated_checkout` so the selected pickup card and hidden fields return from backend state.

0.45.18 note: checkout pickup REST endpoints now initialize WooCommerce session context before touching canonical pickup buckets. REST save/state/reset ensure `WC()->session` exists, set the customer session cookie when supported, and then read/write `wdc_platform_pickup_selections`, fixing the missing-session path where REST save returned success but left canonical bucket keys empty.

0.45.16 note: canonical pickup bucket persistence now bypasses fragile generic session access for the nested `wdc_platform_pickup_selections` dictionary. A dedicated raw array writer/reader stores the family bucket and verifies the raw key immediately.

0.45.15 note: REST pickup save now writes selected points into the canonical `wdc_platform_pickup_selections` dictionary via an explicit family-scoped session writer before updating legacy mirrors. Russian Post and CDEK buckets are present immediately after save, reload checkout receives non-empty `pickupSelections`, and CDEK validation can read the active `cdek:pickup` bucket without posted hidden fields.

0.45.13 note: checkout pickup restore and validation now depend on the active `pickup_family`, destination identity and saved point identity only. Selected CDEK/Russian Post pickup points survive reloads and tariff/rate suffix changes for the same city; CDEK validation passes from the `cdek:pickup` bucket without posted hidden fields; localized `pickupSelections` exposes raw saved buckets while `selectedPickupPoints` remains the renderable-card subset; and pickup address resolution for checkout cards/order meta now reads top-level, snapshot and raw address aliases.

0.45.12 note: checkout pickup state now has a single canonical dictionary, `wdc_platform_pickup_selections`, keyed by `pickup_family`. Legacy singleton keys are derived mirrors only and no longer override existing buckets. Localized checkout config exposes the complete bucket dictionary plus active shipping method/family for reload restore; validation and order persistence read the active family bucket; Russian Post aliases normalize to `russian_post_domestic`; and destination fingerprints are based on stable location/city identity so same-city reloads keep the selected point while real location changes still invalidate saved pickups.

0.45.11 note: remaining checkout pickup state blockers are fixed. Checkout boot now restores the active family bucket from localized `pickupSelections` / `activePickupFamily` and fills hidden pickup fields on page reload; Russian Post validation passes from the active `russian_post_domestic:pickup` bucket before carrier-key fallback comparisons; CDEK `/v2/calculator/tarifflist` api_error/403 and zero-rate results are not cached as successful empty quotes; delivery cache reset includes CDEK city/deliverypoints caches but not token cache; and grouped tariff selectors keep inactive nested rates disabled even when one carrier disappears after checkout update.

0.45.10 note: checkout pickup restore now uses `pickupSelections` / `pickup_selections` as the shared source of truth across localized checkout config and REST state/save/reset responses. The frontend merges saved family buckets without replacing complete points by code-only payloads, restores the active `pickup_family` after method switching, checkout updates and page reloads, and the pickup map side list now uses the same `display_title` / `display_code` as marker popups. Russian Post checkout validation is covered from its family bucket while CDEK bucket validation remains intact.

0.45.9 note: pickup bucket reset scope is corrected. `CheckoutSessionManager::clear_pickup_selection()` is now explicitly documented as a GLOBAL reset that clears every pickup family, while normal method switching and family actions preserve unrelated buckets or call `clear_pickup_selection_for_family()`. `clear_pickup_selection_if_allowed()` no longer turns non-location method-family changes into broad resets; global clears are reserved for destination/location/full-context reset reasons. Tests cover CDEK/Russian Post/custom buckets surviving family resets and being removed only by global reset.

0.45.8 note: checkout pickup selections are now bucketed by `pickup_family` in `wdc_platform_pickup_selections`. CDEK and Russian Post selected points can coexist in the same checkout session, switching methods restores the active family's saved point when the destination identity still matches, validation reads only the active family bucket, and reset can target one family unless the destination changes. Russian Post map titles use repository postcode instead of technical `point_code`; CDEK titles keep `cdek_code`. CDEK pickup prefetch starts in the background for active `cdek:pickup`, and grouped tariff selectors disable inactive nested rates.

0.45.7 note: carrier/pickup-family propagation after the universal pickup refactor is fixed. Pickup REST responses now include full normalized presentation/state fields for CDEK and Russian Post: `carrier_key`, `service_key`, `pickup_family`, `point_title`, `point_type_label`, `marker_type`, address/postcode aliases and `snapshot`. CDEK map/list/popup titles no longer fall back to Russian Post; Russian Post checkout save maps the public REST carrier back to `russian_post_domestic:pickup`; and CDEK selected points keep `cdek:pickup` through checkout save and validation.

0.45.6 note: checkout pickup state after the universal `pickup_family` refactor is hardened. CDEK selected pickup points pass checkout validation from full hidden/session payloads, localized checkout restore suppresses code-only selected points, the frontend selected card is rendered only when `point_code`, matching `pickup_family` and address are present, switching CDEK/Russian Post hides inactive cards and shows the empty chooser for incomplete active selections, and Russian Post map requests keep the `russian_post` REST carrier after switching back from CDEK.

0.45.5 note: checkout pickup restore no longer looks up CDEK/custom pickup point ids or codes in the Russian Post pickup repository. `CheckoutValidation` gates `selection_from_pickup_row()` and `selection_from_pickup_code()` behind `pickup_family=russian_post_domestic:pickup`; CDEK/custom restore uses current session, hidden/full posted payload, or minimal fallback. Collision tests cover CDEK/custom point codes that match Russian Post rows.

0.45.4 note: pickup point presentation/state is now carrier-neutral. `PickupPointPresentationResolver` centralizes built-in Russian Post and CDEK titles, CDEK POSTAMAT `Срок хранения 3 дня`, marker type and generic/custom fallback presentation. Checkout JS, checkout validation, REST/session payloads, order meta and admin recalculation save use normalized `pickup_family = {carrier_key}:pickup` plus `point_title`, `point_type_label`, `marker_type`, `description` and `storage_notice` instead of scattered CDEK/Russian Post card conditions. CDEK order creation, statuses, webhooks and print forms remain intentionally outside this stage.

0.45.3 note: CDEK checkout selected pickup card state is fixed after QA. The checkout card under rates no longer renders technical `Код пункта` / `Индекс` rows for CDEK, but keeps title/address/work time/description/POSTAMAT storage notice. Checkout reload restores the full CDEK selected pickup payload from session/localized state, switching shipping method hides inactive pickup-family cards/buttons, and order/thankyou/email rendering receives the populated CDEK pickup payload. Permanent FIAS/GAR -> CDEK `city_code` mapping remains technical debt.

0.45.2 note: CDEK pickup card rendering is fixed after QA. Checkout selected-point cards, thankyou/order cards and email cards now use CDEK-aware titles, render descriptions with `Описание:`, suppress empty/numeric-zero `work_time` and description values such as `0.000000`, and keep POSTAMAT `Срок хранения 3 дня` visible in red bold output. Russian Post pickup cards keep their existing titles and no longer render accidental numeric-zero descriptions.

0.45.1 note: CDEK pickup QA fixes are implemented on `fix/cdek-pickup-selection-and-ui`. Checkout validation now handles CDEK pickup as a CDEK carrier family throughout restore/match logic and can create an order after the user selects a CDEK pickup point. CDEK `point_code`/`cdek_code` uses the CDEK point code, not postcode; `PVZ` renders as `Пункт выдачи СДЭК`; `POSTAMAT` renders as `Постамат СДЭК`, uses a separate marker color and shows `Срок хранения 3 дня`. CDEK pickup descriptions are saved in session/order calculation data and rendered in pickup cards/order/email display. Permanent FIAS/GAR -> CDEK `city_code` mapping is explicitly left as technical debt for a later CDEK integration stage.

0.45.0 note: CDEK pickup points are implemented for checkout and admin order delivery recalculation. WDC calls CDEK API v2 `GET /v2/deliverypoints` with `city_code`, `country_code=RU` and `type=ALL`, normalizes the response through `CdekDeliveryPointService`, caches it by environment/city/type, and reuses the existing pickup map/picker. CDEK pickup rates now require a selected pickup point; checkout and admin save persist the selected point into `_wdc_delivery_calculation_data.pickup` and write the pickup address to the WooCommerce shipping address. Visible shipping item meta remains only delivery time. CDEK order creation, statuses, webhooks and print forms remain intentionally not implemented. The next feature branch is `feature/cdek-order-creation`.

0.44.9 note: Russian Post courier calculation postcode fill on `WDC -> Локации` now retries technical request failures up to 5 attempts. If every attempt fails technically, it counts one error and stores courier technical marker `999999999` meaning "technical error, retry later"; this marker is distinct from "courier delivery unavailable". New/reset runs process marker rows first, then cities, then other settlements. A later success overwrites `999999999`, while a valid business response that courier delivery is unavailable clears it.

0.44.8 note: CDEK method titles are normalized across checkout rates, checkout-created shipping items, admin recalculation preview tariff payloads and admin save. Titles use `{Название доставки}, {название rate} - {срок доставки}`, honor custom CDEK pickup/courier names from the service `Основное` tab, and do not duplicate delivery ranges.

0.44.7 note: visible WooCommerce shipping item meta is now carrier-neutral and compact for checkout and admin delivery replacement. Every service writes only `Срок доставки`; if no delivery range/comment is available, the visible value is `не указан`. Technical carrier/rate/tariff/API/rules/package data remains in hidden WDC order meta, `_wdc_platform_rate_meta`, `_wdc_delivery_calculation_data`, and the `Калькулятор доставок` metabox.

0.44.5 note: CDEK tariff calculation now classifies `delivery_mode` by the destination side from the CDEK docs: door destinations are courier, warehouse/PVZ destinations are pickup (`1` door-door -> courier, `2` door-warehouse -> pickup, `3` warehouse-door -> courier, `4` warehouse-warehouse -> pickup). The predefined `cdek` service main tab also stores service-specific `pickup_method_title` and `courier_method_title`, defaulting to `СДЭК до пункта выдачи` and `СДЭК курьер`, and these titles flow into checkout grouped rates, admin recalculation preview, saved shipping metadata and calculation data. CDEK pickup map/point selection, order creation, statuses, webhooks and print forms remain intentionally not implemented. The next feature branch is `feature/cdek-pickup-points`.

0.44.0 note: CDEK tariff calculation is connected to checkout/runtime and admin order delivery recalculation preview. The predefined `cdek` service remains disabled by default; when enabled with complete active credentials, sender CDEK city code and a resolvable destination city, WDC calls CDEK API v2 `POST /v2/calculator/tarifflist`, maps pickup/courier tariff candidates to `DeliveryRate`, applies the existing rule engine, and saves safe calculation data without tokens or secrets. CDEK pickup map/point selection, order creation, statuses, webhooks and print forms are intentionally not implemented. The next feature branch is `feature/cdek-pickup-points`.

0.43.1 note: the CDEK foundation settings UX now stores test and production credentials separately. The active CDEK environment chooses the base URL and matching Account / Secure password; switching environments does not copy, clear or mutate the other environment. The duplicate CDEK enabled checkbox was removed from CDEK settings, so runtime enablement remains the common delivery service `Основное` flag. Russian Post and CDEK credentials tabs are now labeled `Данные для входа`, and CDEK diagnostics include the active environment without exposing secrets or tokens.

0.43.0 note: CDEK Carrier Foundation is implemented for branch `feature/cdek-carrier-foundation`. The new predefined delivery service key is `cdek`, disabled by default, with admin settings for environment, Account / client_id, encrypted Secure password / client_secret, OAuth token cache diagnostics and a protected "Проверить подключение" action. Runtime checkout rates, tariff calculation, pickup points, shipments/orders, statuses, print forms, autosync and webhooks are intentionally not connected. The next recommended feature branch is `feature/cdek-tariff-calculation`.

0.42.0 note: the order-admin delivery recalculation stage is closed after an HPOS audit and documentation refresh. The completed flow covers preview, settlement override, pickup map selection, shared checkout courier address suggestions, safe WooCommerce shipping item create/replace, WDC delivery/calculation meta rewrite, shipping address update, totals recalculation, private order notes, non-blocking courier settlement mismatch warnings and save blockers for multiple shipping items or registered shipments. The audited order-delivery recalculation code uses `wc_get_order()`, WooCommerce order/shipping-item CRUD, `$order->get_meta()`, `$order->update_meta_data()`, `$order->calculate_totals(false)` and `$order->add_order_note()`; no direct order `postmeta`/`wp_posts` access or `WP_Query` over `shop_order` was found. The next recommended major branch is `feature/cdek-carrier-foundation`.

0.41.25 note: the order-admin delivery recalculation modal now treats courier address suggestions as the normal automatic flow and removes the old `Проверить адрес` button. Manual fallback remains explicit through `Использовать этот адрес`. The modal settlement summary no longer duplicates the region when `display_name` already includes it, admin saves write WooCommerce shipping city/state through checkout-compatible location payload/formatter values, and courier save UI shows a non-blocking warning when the normalized/manual address settlement cannot be confidently matched to the settlement used for the calculated rate.

0.41.24 note: the order-admin delivery recalculation courier address block now uses the same shared DaData address suggestion flow as checkout. Admin courier suggestions are served by a thin `wdc_order_delivery_recalculate_address_suggest` endpoint backed by `AddressSuggestionService`, `AddressSuggestionNormalizer` and `AddressLineParser`, so street -> house -> flat/room/premise lookup, typed lower-level filtering, house-level finalization and `address_1` formatting match checkout. Manual fallback remains available as an explicit manager action.

0.41.12 note: the order-admin delivery recalculation save-flow now separates pickup and courier address requirements correctly. Pickup saves require selected rate + selected pickup point and write the selected pickup address to WooCommerce shipping address while keeping pickup details in WDC pickup meta and out of order notes. Courier saves expose a manager address-normalization block in the modal and are blocked until a normalized address is supplied. The pickup map's manual address search now geocodes the typed address through the existing DaData/address-normalization wrapper instead of placing a temporary marker on a pickup point fallback. Saved calculation data keeps package/API/rules/result details from rate meta, API base price remains distinct from final cost, method titles include delivery text, and note city-change detection uses canonical location identity to avoid duplicate/same-city old/new lines.

0.41.11 note: order-admin delivery recalculation save UX was refined. Pickup saves now require selected rate + selected pickup point only; manager-entered address normalization is no longer required for pickup and pickup shipping address is not replaced by pickup address. If the selected location was not changed, the modal preselects the order's current pickup point from WDC pickup meta. The pickup map picker now syncs marker clicks with the side list, scrolls the active row into view, supports a temporary search pin for manual address/postcode matches, and keeps the picker dialog within the visible viewport with a separately scrolling list.

0.41.9 note: the order-admin delivery recalculation scenario is now complete. Preview/recalculation stays available for orders with shipments or unusual shipping item state, but saving the selected delivery is guarded separately: save is blocked for multiple shipping items and any registered shipment marker. Valid saves create a missing WooCommerce shipping item or replace the single existing one, rewrite WDC platform/calculation/pickup meta, update shipping address, recalculate totals, add a private Russian order note, and reload the admin page. Pickup saves require selected pickup point; the pickup point address is not written to WooCommerce shipping address or order notes.

0.41.8 note: the order-admin delivery recalculation pickup map initial load now requests `mode=location` and loads all available pickup points for the selected settlement instead of searching by the settlement base postcode. The admin pickup endpoint resolves location mode through location id/FIAS/GAR, city+region, display/city, and only then postcode fallback. Manual `mode=search` remains available for explicit administrator postcode/address/city searches. The order is not mutated: no delivery save, shipping item replacement, totals recalculation, shipping address update, order note, or `_wdc_delivery_calculation_data` update.

0.41.7 note: the order-admin delivery recalculation pickup selector now uses the existing pickup map provider assets instead of a table-only popup. Pickup rates open a picker with map, markers, status area and side list; marker/list selection updates only the modal `selectedPickupPoint` JS state. If the map provider is unavailable, the picker remains usable as a list and shows an explanatory fallback message. The order is not mutated: no delivery save, shipping item replacement, totals recalculation, shipping address update, order note, or `_wdc_delivery_calculation_data` update. The picker also now uses `escapeAttribute()` for `data-index` attributes.

0.41.6 note: order-admin delivery recalculation preview now supports preview-only pickup point selection for pickup rates. The modal shows pickup controls only for rates marked `requires_pickup_point`, opens an admin pickup search picker backed by the existing Russian Post pickup repository, stores the chosen point only in JS modal state as `selectedPickupPoint`, and clears it when a courier rate is selected. The admin pickup endpoint is protected by nonce/capability checks and the same shipment block. The order is not mutated: no delivery save, shipping item replacement, totals recalculation, shipping address update, order note, or `_wdc_delivery_calculation_data` update.

0.41.4 note: order-admin delivery recalculation preview now supports a modal-only settlement override. The modal shows the current order settlement, searches settlements through the existing checkout location search payload, sends the selected location only in the preview AJAX request, and recalculates rates for that destination with an explicit `Расчет выполнен для: ...` status. The order is not mutated: no shipping address update, delivery save, shipping item replacement, totals recalculation, pickup selection, order note, or `_wdc_delivery_calculation_data` update.

0.41.2 note: order-admin delivery recalculation preview now opens in a custom admin modal instead of rendering inline in the `Калькулятор доставок` metabox. The metabox keeps only the `Пересчитать доставку` button when recalculation is allowed; the modal contains header/body/footer, close controls, status/loading/error area, rates content area, and a disabled save placeholder for the next patch. The preview-only boundary is unchanged: no delivery save, pickup selection, shipping item replacement, totals recalculation, shipping address change, order note, or `_wdc_delivery_calculation_data` update.

0.41.0 note: the first order-admin delivery recalculation foundation is implemented as preview-only UI. `Калькулятор доставок` can request a recalculation from the WooCommerce order, WDC builds a `QuoteRequest` from order items/address/meta, runs all available services through `CheckoutOrchestrator`, renders pickup/courier rates and Russian Post domestic tariff groups, and blocks preview when `_wdc_shipments` indicates an already created shipment. This version intentionally does not save delivery changes, replace shipping items, recalculate totals, change shipping address, select pickup points, override locations, create order notes, or update `_wdc_delivery_calculation_data`.

0.40.0 note: documentation audit branch. No runtime functionality was added. The status below was refreshed against the current codebase, profile documents, migrations, assets and smoke-test entrypoints after the 0.39.x Russian Post shipment/status work.

0.39.5 note: the same Russian Post actual-cost lookup now runs after ordinary automatic shipment creation from the order metabox. After a successful create response with barcode/tracking number, WDC safely calls `GET /1.0/backlog/search?query={barcode}`, extracts `total-rate-wo-vat + total-vat` through the shared actual-cost extractor, and stores source `backlog_search_after_create`. Lookup errors or missing totals do not fail shipment creation, do not create order notes, and do not show warnings.

0.39.4 note: the WooCommerce order shipment metabox shows the real Russian Post shipment price when manual tracking attach finds the parcel through `GET /1.0/backlog/search?query={barcode}`. WDC stores `total-rate-wo-vat + total-vat` from that response in `_wdc_shipments`, formats it as `Цена: {amount} руб.`, and compares it with the checkout `Базовая стоимость API` from `_wdc_delivery_calculation_data`; up to 3% over base is ok/green, more than 3% is warning/red, and missing base cost is neutral.

0.39.3 note: the Russian Post courier calculation postcode fill tool on `WDC -> Локации` runs each backend step as sequential rate-limited probes at about 6 requests/sec. Steps are capped at 18 probes or 3 seconds, the browser keeps one active AJAX step at a time, and job JSON includes target/actual RPS and step timing diagnostics.

0.39.2 note: successful shipment status refreshes no longer create WooCommerce order notes. WDC creates an order note only when automatic order status mapping changes the WooCommerce order status, using the compact format `Посылка {barcode}`, `Статус: {universal status}.`, `Статус заказа изменён:`, `{from_status} -> {target_status}`.

0.39.1 note: autosync no longer drops terminal shipments before order status mapping. Terminal universal statuses still skip carrier tracking refresh, but `ShipmentStatusAutoSyncService` applies `ShipmentOrderStatusMappingService` to the already saved terminal shipment state, records `terminal_status_no_tracking_update`, and includes the mapping result in diagnostics.

0.39.0 note: universal shipment status to WooCommerce order status mapping is implemented. The `WDC -> Статусы -> Соответствие статусов` tab stores a disabled-by-default global enable flag and `shipment_status_order_status_mapping`, reads available WooCommerce statuses through `wc_get_order_statuses()` including custom statuses, and applies mapping through `ShipmentOrderStatusMappingService` after shipment status is saved by `ShipmentStatusUpdateService`.

0.38.0 note: universal shipment status autosync is implemented through `ShipmentStatusAutoSyncService`, `WDC -> Статусы`, WP Cron hook `wdc_shipment_status_autosync`, schedule `wdc_every_6_hours`, and lock `wdc_shipment_status_autosync_lock`. The first dispatcher target is `russian_post_domestic`, which reuses `ShipmentStatusUpdateService::update_russian_post()`.

## Общий статус

- Текущая версия: `0.47.0`.
- Текущая базовая ветка: `develop`.
- Рабочая ветка: `feature/cdek-tariffs-management`.
- Последнее обновление статуса: 2026-06-12.
- Общая готовность проекта: примерно 74%.
- Следующий рекомендуемый этап: `feature/cdek-order-creation`.

## Краткое резюме

Проект уже имеет рабочую платформу расчета доставки для WooCommerce: core bootstrap, DI container, миграции, checkout runtime, календарь доставки, Rule Engine, локальную базу FIAS/GAR, DaData-подсказки и обогащение, слой delivery services, внутреннюю и международную Почту России, ПВЗ Почты России с импортом/REST/картой, а также ручной runtime отправлений Почты России.

После веток 0.38.x-0.39.x закрыты важные части carrier lifecycle для Почты России: создание отправления, отмена backlog-отправления, ручное внесение трекинга, ручное и автоматическое обновление статусов, универсальные статусы, автосинхронизация, автоматическое изменение WooCommerce-статусов заказов и сверка фактической стоимости отправления с расчетом checkout. В 0.41.x-0.42.0 закрыт полноценный admin recalculation для заказа. Не закрыты документы/ярлыки/партии/Ф103 внутри WDC, история статусов, production operations dashboard и будущие перевозчики.

## Готовность по блокам

| Блок | Статус | Готовность | Комментарий |
| ---- | ------ | ---------: | ----------- |
| Core Platform | done | 100% | Entrypoint, autoloader, container, settings, logger, encryption, migrations, HPOS declaration, admin menu. |
| Locations / FIAS | done | 88% | Locations/regions/aliases, GAR import, snapshots, incremental updates, checkout lookup, DaData coordinate/postcode helpers. |
| Delivery Services | done | 90% | Registry, repositories, settings/countries/admin UI, service rules integration, unified Russian Post domestic service migration. |
| Russian Post Domestic | done | 92% | Unified `russian_post_domestic`, pickup/courier split, Tariff API, tariff variants, courier calc postcode substitution, Otpravka credentials and shipment settings. |
| Pickup Points | done | 90% | Russian Post import, diagnostics, local repository, REST API, checkout map, order persistence, admin shipment-modal map selector. |
| Shipments | partial | 80% | Russian Post create/cancel/remove/manual attach, draft/preview, address normalization, actual-cost lookup; documents and other carriers are absent. |
| Shipment Tracking | partial | 78% | Russian Post Tracking API refresh, mapper, universal status persistence and manual metabox refresh; no full status history UI. |
| Shipment Autosync | partial | 82% | WP Cron/manual run, 6-hour interval, lock and diagnostics exist for Russian Post/CDEK per-order updates plus DPD global getEvents autosync; large-store pagination/backoff strategy is still future work. |
| Order Status Mapping | done | 80% | Carrier-neutral universal shipment status -> WooCommerce status mapping, custom statuses, terminal-status autosync handling and compact private notes. |
| Checkout Integration | partial | 88% | WooCommerce method, city/location picker, sorting, tariff selector, courier validation, pickup map, order meta and calculation metabox. |
| Order Delivery Recalculation | done | 100% | Admin order delivery recalculation is complete and HPOS-audited for Russian Post, CDEK and DPD: preview through checkout runtime, location override, pickup map, DPD terminalCode-aware refresh, courier suggestions, shipping item replacement/create, totals, checkout-compatible calculation data, mismatch warning and shipment/save blockers. |
| Rule Engine | done | 88% | Conditions/groups, audit, price/days mutations, comments, service rules, simulation and packaging tab. |
| International Shipping | partial | 75% | Russian Post international rates/country mapping/fallback work; no shipment creation/tracking/documents for international flow. |
| CDEK Carrier Foundation | partial | 38% | `cdek` service metadata, admin settings, separate test/production encrypted credentials, environment switch, OAuth client/cache and connection check exist; runtime rates, pickup points, shipments, statuses, print forms and webhooks are deferred. |
| Future Carriers | partial | 18% | DPD has quote-only checkout runtime plus local pickup-point foundation; Yandex Delivery has API/settings, pickup storage/import foundation and WDC location_id -> Yandex geo_id mapping foundation, but no runtime adapter; PEK, Energia, Aerogruz and Jet have no runtime adapters. |
| Operations / Monitoring | partial | 50% | Logger, diagnostics pages and autosync diagnostics exist; no production dashboard/rotation strategy. |
| Documentation | partial | 78% | Profile docs cover the completed order delivery recalculation stage and current CDEK-oriented roadmap; older historical notes remain as changelog context. |

## Реализовано

### Platform, Data And Checkout

- Plugin entrypoint and `WDC_VERSION` are updated to `0.75.0`.
- `src/Core` wires runtime environment, autoloader, DI container, feature flags, requirements checks, plugin hooks and activation.
- `src/Infrastructure` provides settings, logging/redaction, encryption, Action Scheduler/WP Cron wrapper and migration manager.
- `database/migrations` contains the active schema for calendar, locations, GAR import, rules, delivery services, Russian Post pickup points and unified Russian Post domestic service.
- `src/WooCommerce/HPOSCompatibility.php` declares WooCommerce HPOS compatibility.
- `src/Domain` contains address, calendar, carrier, common, package, pickup, quote, shipment and status domain classes.
- `src/Calendar` stores delivery calendars, generates years, calculates/format delivery dates and exposes admin UI.
- `src/Locations` covers FIAS/GAR clients/import/snapshots/incremental updates, local locations/regions, aliases, display names, search, country index and courier calculation postcode fill.
- `src/Checkout` covers WooCommerce shipping method registration, package/rate mapping, quote orchestration, caching, sorting, validation, city selector, DaData suggestions, courier address handling, pickup map and order meta persistence.
- `src/Carriers/Runtime/CdekCarrier.php` sends CDEK tarifflist `packages[]` per `PackageItem` unit, applies aggregate `packaging_weight_g` once to the first item package unless packaging is already represented by `WDC_PACKAGING`, keeps the old aggregated package only as no-item fallback, performs an optional 50x50x30 cm single-package express tariff pass, merges new single-package tariff codes, and filters CDEK rates by exact periods plus faster/cheaper dominance.
- `src/Rules` implements the Rule Engine, service-level rules, default fallback rules, condition groups/expressions, audit trail, price/days changes, comments, stop-processing, simulation and admin builder.
- `src/Packaging` adds global/service-aware packaging weight calculation used by delivery services and checkout.

### Delivery Services

- `src/DeliveryServices` stores and manages delivery services, countries, settings and service admin UI.
- Runtime availability supports carrier directory, selected countries, all countries and all-except-selected modes.
- Service rules, minimum price, rounding, packaging weight and customer comments are integrated into checkout rates.
- Migration `0026_unify_russian_post_domestic_service.php` copies old domestic pickup/courier settings into one `russian_post_domestic` service and removes obsolete domestic service rows/settings/country rows/service-rule bindings.

### Russian Post Domestic

- A single domestic carrier/service is the source of truth: `carrier_key=russian_post_domestic`, `service_key=russian_post_domestic`.
- Pickup and courier are split by `delivery_type` and checkout group/rate ids such as `russian_post_domestic:pickup` and `russian_post_domestic:courier`.
- Domestic Tariff API requests support `pack=99`, declared value `sumoc`, tariff variants, per-tariff ECOM flag, API token, cache/debug metadata and safe error diagnostics.
- Configurable method titles, tariff labels and delivery ranges are persisted in checkout/session/order calculation data while visible WooCommerce shipping item meta is limited to `Срок доставки` with `не указан` fallback.
- Courier Russian Post tariff calculation can use `russianpost_courier_calc_postal_code` found by the admin location tool `Подобрать индексы для курьерской Почты России`; courier marker `999999999` means a technical fill failure that should be retried later, and is not treated as a normal found postcode.
- Otpravka credentials, Tracking credentials, postoffice acceptance indices, default from postcode and shipment settings live in `WDC -> Службы доставки -> Почта России по РФ`.

### Pickup Points

- Russian Post pickup points are imported into `wp_wdc_pickup_points_russian_post`.
- Import state, diagnostics, local location matching/rebind, point type settings and work-time formatting are implemented.
- Checkout pickup REST/search/detail endpoints and selection state are implemented.
- Checkout map uses the frontend pickup map stack with Leaflet/Yandex providers and selected pickup persistence.
- WooCommerce order admin shipment modal can choose a Russian Post OPS/PVZ on the map; selection updates only the shipment draft/preview/create request and does not rewrite checkout/order meta.

### Russian Post Shipments

- WooCommerce order metabox `Отправления` prepares Russian Post shipment drafts from HPOS-safe order APIs and saved WDC order meta.
- Creation sends `PUT /2.0/user/backlog` through `RussianPostShipmentAdapter`.
- Pickup/OPS payloads use `address-type-to=DEMAND`, `index-to`, `region-to`, `place-to`; domestic payloads send `mail-direct=643`.
- ECOM payloads are controlled by tariff setting `is_ecom` and use `ecom-data.delivery-point-index`.
- Courier payloads require Russian Post address normalization before create.
- Successful create stores barcode/ШПИ and hidden technical `backlog_order_id`.
- After successful create, the modal closes, a toast is shown, the first status refresh starts automatically, and WDC attempts actual-cost lookup through `backlog/search` without failing the create flow.
- Cancellation calls `DELETE /1.0/backlog` with `backlog_order_id` and is available only for operation `28 / Присвоение идентификатора`.
- Local remove-from-order clears only WooCommerce `_wdc_shipments` state and does not call Russian Post.
- Manual tracking attachment normalizes barcode/ШПИ, searches `backlog/search`, falls back to `shipment/search`, stores lookup source/backlog id when available and starts the first status refresh.
- Actual Russian Post cost from `total-rate-wo-vat + total-vat` is stored and compared with checkout `Базовая стоимость API`; <=3% over base is ok, more is warning, missing base is neutral.

### Shipment Statuses, Autosync And Order Status Mapping

- `src/Domain/Status/DeliveryStatus.php` defines universal shipment statuses.
- `RussianPostTrackingApiClient` calls Russian Post Tracking API `getOperationHistory` over SOAP 1.2.
- `RussianPostTrackingStatusMapper` contains the fixed Russian Post operation/attribute mapping, including pickup/courier corrections and `type:-` fallback.
- Manual metabox status refresh stores universal status, raw carrier operation data, checked timestamp and terminal marker in `_wdc_shipments`.
- `ShipmentStatusAutoSyncCron` registers hook `wdc_shipment_status_autosync`, schedule `wdc_every_6_hours` and keeps the event scheduled even when disabled.
- `ShipmentStatusAutoSyncService` scans selected WooCommerce order statuses, skips missing tracking/unsupported carriers/terminal tracking refreshes, dispatches supported per-order carriers through `CarrierShipmentAdapterRegistry`, and runs DPD once per autosync pass through `DpdEventSyncService` because DPD `getEvents` returns a client-wide event inbox.
- Terminal statuses still run order status mapping against saved shipment state.
- `ShipmentOrderStatusMappingService` applies universal shipment status -> WooCommerce order status mapping when enabled, validates target statuses from `wc_get_order_statuses()`, updates orders and adds a compact private WDC note only when the order status actually changes.

### Russian Post International

- `RussianPostInternationalCarrier` supports non-RU Russian Post international quote calculation.
- Country mapping, Russian Post country directory refresh/manual mapping, fallback rates, VAT normalization, packaging weight and service rules are implemented.
- International flow is quote-only: shipment creation, tracking and documents are not implemented for international delivery.

## Частично реализовано

- Checkout UX is functional, but full browser-storage TTL behavior, final edge-case stabilization and carrier-neutral pickup orchestration for future carriers remain.
- Order admin recalculation has the full manager workflow for previewing rates, choosing pickup/courier, saving delivery, replacing shipping items and adding systematic notes; remaining work is real-store QA and carrier expansion.
- Shipment runtime has a shared carrier-action adapter layer for Russian Post domestic and CDEK; future carriers still need their own adapters and runtime support.
- Shipment tracking has current status refresh and autosync, but no full status event history/timeline UI.
- Shipment autosync supports Russian Post domestic and CDEK through per-order adapters and DPD through one global `getEvents` pre-pass, but still scans selected WooCommerce orders for diagnostics/terminal mapping without advanced pagination controls.
- Operations diagnostics exist, but production monitoring, log/document rotation and long-running import hardening remain limited.
- Documentation is mostly current in active Russian Post/status docs, but older foundation docs still contain stage-specific historical wording.

## Не реализовано

- CDEK webhooks and persistent print-form storage.
- Permanent FIAS/GAR -> CDEK `city_code` mapping/storage.
- DPD documents and labels.
- Yandex Delivery runtime adapter, pricing, pickup/courier flow and future offer confirmation.
- PEK, Energia, Aerogruz, Jet adapters.
- Plugin-generated Russian Post labels, forms, batches and F103.
- Carrier document storage, download UI and rotation.
- Full carrier-neutral status history model/UI.
- Automatic shipment creation outside the manager-triggered order metabox flow.
- Shipment integrations for international Russian Post and all future carriers.
- Full manual/fixed pseudo-carrier lifecycle from the target specification.
- Production operations dashboard.

## Известные ограничения

- The plugin is still fresh-install oriented; production data migration compatibility is limited to the active migration path documented in current files.
- Russian Post shipment documents are intentionally prepared manually in the Russian Post account; WDC does not call Forms API and does not generate labels/F103.
- Russian Post shipment creation is manual from the WooCommerce order admin metabox; there is no automatic creation on checkout/order placement.
- Autosync is carrier-neutral in dispatch shape for Russian Post domestic and CDEK, but high-volume batching for more carriers is not yet designed.
- Autosync uses selected order statuses and `wc_get_orders(limit=-1)`; high-volume production batching is not yet designed.
- Terminal statuses skip carrier API refresh and only run order status mapping against the saved shipment state.
- Actual-cost comparison depends on `backlog/search` returning total fields and on checkout calculation data having `api_base_price_rub`.
- Courier shipment creation still depends on each carrier's address-preparation path; failed/stale normalization blocks create.
- Admin PВЗ selection for shipments uses the local Russian Post pickup database and does not recalculate checkout tariffs.
- Pickup map production implementation is Russian Post-specific, even though generic pickup domain/storage exists.
- International Russian Post remains quote-only.
- Runtime monitoring/log rotation and import retry/backoff are not production-complete.

## Технический долг

### Russian Post Pickup Import

- Verify automatic ZIP download through Russian Post APIs on production Linux/VDS.
- Compare direct cURL backend and WordPress HTTP API backend stability.
- Check timeout, SSL, Action Scheduler, background download and PHP ZipArchive behavior on LocalWP and production.
- Keep manual TXT/JSON payload import as the final LocalWP/Windows fallback.
- Add retry/backoff, CLI import mode or chunked streamed download if production testing shows the need.

### Shipments And Statuses

- Add pagination/batching strategy for autosync on large WooCommerce order volumes.
- Add status history/timeline if business users need more than the latest universal/carrier status.
- Keep document generation explicitly out of current scope or start a separate Russian Post documents stage.
- Improve multi-place UI beyond the current basic first-place/default distribution.
- Generalize shipment adapters and autosync dispatch once the next carrier starts.

### Checkout And Orders

- Implement admin recalculation/replacement workflow for existing orders.
- Finish browser-storage TTL and remaining checkout UX edge cases.
- Keep validating hidden WDC order calculation data against WooCommerce order item state before shipment creation.

### Operations

- Define production monitoring/status dashboard.
- Define log retention/rotation policy.
- Harden long background imports and diagnostics for production datasets.

### Documentation

- `docs/wdc-shipments-foundation.md` is historically useful but still describes the original manual stage where background polling was not included; current autosync/order mapping source is `docs/wdc-shipment-statuses.md`.
- `docs/wdc-russian-post-international.md` still carries an old documented version even though the runtime remains present; update it when international shipping is touched.
- `docs/wdc-current-code-map.md` should continue to receive small top notes when major modules change.
- `docs/walls-delivery-calc-tech-spec.md` is a target-state document, not implementation status; do not use it as the source of readiness percentages.

## Завершенные ветки / этапы, отраженные в develop

- Core platform, domain model, migrations, HPOS compatibility and admin menu baseline.
- Calendar and delivery-date foundation.
- FIAS/GAR locations, snapshots, aliases, display names and checkout location picker.
- Rule Engine foundation, admin builder, service-level rules and packaging tab.
- Delivery Services foundation.
- Russian Post international quote/fallback/country mapping baseline.
- Russian Post domestic unified service (`russian_post_domestic`) with pickup/courier split.
- Russian Post pickup points import, diagnostics, REST API and checkout map.
- Russian Post admin shipment-modal OPS/PVЗ selector.
- Russian Post domestic shipment creation, cancellation, manual tracking attachment and manual status refresh.
- Russian Post universal shipment statuses and fixed status mapping.
- Shipment status autosync.
- Universal shipment status -> WooCommerce order status mapping.
- Russian Post actual-cost lookup and checkout base-cost comparison.
- Courier Russian Post calculation postcode fill optimization.

## Следующие этапы

### 1. Admin Recalculation

- Рекомендуемая ветка: `feature/order-delivery-recalculation`.
- Что входит: ручной QA завершенного пересчета доставки в заказе, проверка реальных WooCommerce shipping item/totals/order note сценариев и доработка UX только по найденным edge cases.
- Почему следующий: базовый order support workflow реализован; перед расширением carrier coverage стоит пройти его на реальных заказах и HPOS screens.
- Обновить документы: `docs/wdc-checkout-integration.md`, `docs/wdc-current-code-map.md`, `docs/project-status.md`, `README.md`.

### 2. Russian Post Production Hardening

- Рекомендуемая ветка: `feature/russian-post-production-hardening`.
- Что входит: production checks for pickup import, autosync batching strategy, retry/backoff decisions, diagnostics and log/retention policy.
- Зависимости: current Russian Post domestic shipment/status runtime.
- Обновить документы: `docs/wdc-russian-post-domestic.md`, `docs/wdc-russian-post-pickup-points.md`, `docs/wdc-shipment-statuses.md`, `docs/project-status.md`.

### 3. Shipment Documents Decision

- Рекомендуемая ветка: `feature/russian-post-documents` only if WDC should own labels/forms/F103.
- Что входит: decide whether documents stay manual or become plugin-generated; if implemented, add Forms API clients, storage, UI and rotation.
- Зависимости: stable Russian Post shipment state and production credentials.
- Обновить документы: Russian Post shipment/status docs and `docs/project-status.md`.

### 4. CDEK Carrier Foundation

- Рекомендуемая ветка: `feature/cdek-tariff-calculation`.
- Что входит: CDEK tariff calculation for checkout rates, while keeping pickup points, shipments, statuses, print forms and webhooks for later dedicated stages.
- Зависимости: stable checkout/order recalculation patterns.
- Обновить документы: create `docs/wdc-cdek.md`, update code map and project status.

### 5. DPD Carrier Foundation

- Рекомендуемая ветка: `feature/dpd-carrier-foundation`.
- Что входит: DPD adapter, settings, rates, pickup/courier baseline and smoke tests.
- Зависимости: multicarrier checkout patterns after CDEK.

### 6. Yandex Delivery Foundation

- Рекомендуемая ветка: `feature/yandex-delivery-foundation`.
- Что входит: Yandex pricing, geo, pickup/courier rates, settings and future offer confirmation baseline.
- Зависимости: stabilized multicarrier checkout.

### 7. Operations Stabilization

- Рекомендуемая ветка: `feature/runtime-operations-stabilization`.
- Что входит: production monitoring, dashboard, cleanup/rotation, long import hardening and support diagnostics.
- Зависимости: main runtime integrations.

## Документы, которые нужно обновлять после задач

- `docs/project-status.md` - после каждой завершенной задачи.
- `README.md` - если меняются версия, публичное поведение, runtime-требования или команды проверки.
- `docs/wdc-current-code-map.md` - если меняется структура модулей или добавляется крупный блок.
- Профильный `docs/wdc-*.md` по области задачи.
- `docs/walls-delivery-calc-tech-spec.md` - только если меняется целевой продуктовый или архитектурный контракт.
- `docs/wdc-migration-plan.md` - если меняется порядок этапов, риски или стратегия перехода.
0.48.11 note: Russian Post admin pickup maps no longer stop at the old 300-row location cap; location-context repository/admin lookups now allow large-city result sets up to 2000 rows. The Russian Post shipment preparation modal again loads recipient-location OPS/PVZ rows without requiring a typed search query. Checkout selected pickup preview now matches saved points by carrier-specific stable codes/postcodes (`point_code`, `cdek_code`, `delivery_point`, `postcode`/`postal_code`, `point_id`) instead of only REST `id`, so Russian Post and CDEK selected points are restored as active previews when the map is reopened.

0.48.10 note: the pickup map frontend no longer truncates the side list to the first 100 points. It renders every point returned by backend REST responses, reports the full list count, and selected pickup preview/scroll restoration works for points beyond the first 100 rows in large-city lists such as Moscow.

0.48.9 note: pickup maps now restore the previously selected point as the active preview when reopened, including marker/list highlighting and list scroll. Admin pickup pickers in order recalculation and shipment preparation use the unified larger side-list layout with one bottom `Выбрать этот ПВЗ` button, no duplicate selected preview card and no per-card choose buttons. Address search UI no longer names DaData, CDEK titles render as `ПВЗ СДЭК {code}` / `Постамат СДЭК {code}`, and Russian Post admin pickup maps keep loading their own full point list/markers while address search remains only a temporary orientation marker.

0.48.8 note: CDEK pickup maps now return the full available point list for large cities instead of truncating at 50. Checkout pickup, admin order delivery recalculation and the CDEK shipment preparation modal use a shared DaData-backed address search path that places a temporary marker, focuses the map on the found address, and preserves the active carrier/pickup-family context and point list.

0.48.7 note: CDEK shipment preparation modal now uses managed tariff data instead of a single saved tariff row. Active tariffs are filtered by pickup/courier scenario, the saved order tariff remains selected with a fallback marker when missing from active tariffs, CDEK manager-facing labels replace technical `tariff_code`/`delivery_mode` rows, recipient pickup displays `Код ПВЗ`, and the admin pickup map opens with CDEK carrier context plus recipient locality/address context rather than sender city settings.

0.48.6 note: CDEK internal registration status now maps actual `entity.statuses[]` codes explicitly. `CREATED` is the only order status that means the order is created and valid; `ACCEPTED` remains `registration_pending`, `INVALID` becomes `failed`, `REMOVED` becomes `removed`, and later movement/operation statuses remain active `registered` shipments until a fuller CDEK status mapping stage exists.

0.48.5 note: CDEK internal shipment status no longer treats request state `SUCCESSFUL` as a real shipment/order status. Request state is used only for registration processing: `INVALID` fails registration, while `ACCEPTED`/`SUCCESSFUL` without `entity.statuses[]` stays `registration_pending`. A CDEK shipment becomes internally `registered` only when an actual order status exists in `entity.statuses[]`; `CREATED` and later statuses are displayed from that latest order status.

0.48.4 note: CDEK shipment status handling now treats `requests[].state` only as request-processing state and selects the current order status from `entity.statuses[]` by newest parsable `date_time` among non-deleted statuses. The order metabox saves/renders `planned_delivery_date`, reads actual CDEK delivery cost from `entity.delivery_detail.total_sum`, and compares it with saved “Базовая стоимость API” using the existing 3% tolerance. CDEK tariff calculation settings moved fully to the `Расчет` tab and gained `Цена страховки`; the calculated insurance amount is added to API delivery price before rules.

0.48.2 note: CDEK shipment metabox controls are carrier-aware for manual attach and removal actions. Existing CDEK shipments can be attached by `cdek_number`, continue to use the unchanged manual status refresh, can be cancelled/deleted in CDEK only while the CDEK order status is `CREATED`, and can be removed locally only for known statuses other than protected `ACCEPTED` and `CREATED`.

0.48.1 note: CDEK order creation pre-live-test fixes are applied. `ajax_create()` now returns status data for the active created carrier instead of always reading Russian Post; `POST /v2/orders` with `requests[0].state=INVALID` returns `cdek_registration_invalid` and does not persist `registration_pending`; CDEK logs and request/response snapshots are reduced to sanitized technical data; and the shipment status block label is carrier-aware for CDEK.

0.48.0 note: CDEK order creation now starts from the existing WooCommerce `Отправления` metabox. The shipment modal is carrier-aware, adds `Основное` and universal `Грузоместа` tabs, builds CDEK `packages[]/items[]` from manager-entered package rows, sends `POST /v2/orders` as an async registration request, stores `registration_pending`, and polls `GET /v2/orders` or `GET /v2/orders/{uuid}` every 15 seconds for up to 10 minutes. The payload intentionally omits `services`/`INSURANCE`, `additional_order_types`, `delivery_recipient_cost` and checkout packaging weight, uses prepaid `item.payment.value=0`, and blocks duplicate CDEK creation while an order is pending/created/registered.
### 2026-06-23 - Yandex Delivery geo matching 0.77.0

- `location/detect` now uses `wp_wdc_locations.display_name` as the primary query string, without adding `Россия`; manual assembly remains only for missing display names.
- `YandexDeliveryGeoMatchScorer` now scores locality, region, district/city context and settlement type, with caps for wrong regions, foreign hints, administrative units and weak substring matches.
- Added `YandexDeliveryGeoDistance` as an isolated haversine utility for a future expensive distance-based validation fallback; no full Russia import, checkout or pickup map was implemented.
### 2026-06-23 - Yandex Delivery candidate storage policy 0.78.0

- `YandexDeliveryGeoMappingService` now defaults to `ambiguous_only` candidate storage: confident auto-primary results save only the primary row, while ambiguous results keep all candidates for review.
- The Yandex geo admin tab and integration notes document that candidates are diagnostic and runtime code should use only `is_primary=1` / `find_primary_geo_id()`.
# 0.107.1 Yandex Delivery shipment HTTP production shape alignment

Aligned the shipment HTTP layer with the production API responses verified by live PowerShell requests. `offers/create` now calls `?send_unix=false`; `request/info` and `request/history` use GET query strings without JSON bodies. Offer parsing reads `offer_details.delivery_interval.policy/min/max`, `offer_details.pickup_interval` and `offer_details.pricing_total`. Request info parsing reads top-level `request_id`, `courier_order_id`, `sharing_url` and `state.status`, with destination, recipient, items and places from nested `request`. History parsing uses `state_history`; cancel parsing preserves the async cancellation response (`status=CREATED`, `reason=cancellation_started`). The registration service still has no retry, but if request/info fails after confirm it throws diagnostics with `confirmed_request_id` for later reconciliation.

# 0.107.0 Yandex Delivery shipment HTTP layer

Added the pure Yandex Delivery shipment HTTP layer without wiring it to checkout, admin UI, order save, labels or persistence. The existing `YandexDeliveryApiClient` now exposes shipment endpoints for `offers/create`, `offers/confirm`, `request/info`, `request/history` and `request/cancel` through the existing WordPress HTTP transport abstraction. New shipment DTOs cover offers, offer collections, confirmed requests, canonical request info, request history, shipment state and registration results. `YandexDeliveryShipmentRegistrationService` runs the production flow `payload builder -> offers/create -> earliest offer selector -> offers/confirm -> request/info`, with no retry after confirm and no `request/create` path.

# 0.106.2 Yandex Delivery shipment validation hardening

Closed the remaining validation blockers before the real Yandex Delivery HTTP flow. Shipment allocation now rejects empty allocations and empty places; the CDEK allocation adapter rejects empty source rows and places without allocation rows. The Yandex payload builder validates destination mode strictly, requires pickup station id, requires courier `locality`, `street`, `house` and `full_address` without requiring coordinates, validates recipient name/phone/email, and allows equal ready interval endpoints while rejecting `ready_to < ready_from`. No HTTP/API/UI/persistence flow is included.

# 0.106.1 Yandex Delivery shipment payload hardening

Prepared the shipment allocation foundation for the next real HTTP stage without adding HTTP. Yandex `items[]` now uses the production-confirmed `billing_details` structure for `inn`, `nds`, `unit_price` and `assessed_unit_price`. `ShipmentAllocationItem` stores unit and assessed prices separately, while the current CDEK allocation adapter fills both from the existing CDEK `cost`. Allocation validation now fails fast on broken source rows such as unknown places or non-positive quantities instead of silently producing an incorrect payload.

# 0.106.0 Yandex Delivery shipment payload foundation

Added a pure Yandex Delivery shipment payload builder and a minimal neutral shipment-allocation read-model. The existing CDEK `cdek_item_rows` allocation is adapted without changing CDEK payload construction or packing. The builder covers deterministic temporary place barcodes, item-to-place quantities (including split quantities), recipient naming, pickup/courier destinations, prepaid defaults and UTC ready intervals. No Yandex shipment HTTP/API/UI/persistence flow is included; the next stage will implement offers/create → earliest offer → confirm → info.
