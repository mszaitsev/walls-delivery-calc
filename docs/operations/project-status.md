# Project Status

0.137.0: PEK international calculation runtime is implemented and live-verified for `RU -> AM/BY/KG/KZ` plus existing `RU -> RU`. Checkout live verification covered KZ/Almaty, BY/Minsk, AM/Yerevan, and KG/Bishkek pickup quote, courier quote, PEK pickup map, own/partner point selection, selected-terminal recalculation, correct PEK tariffs, and order creation. New international orders persist canonical WDC `location_id`, and order-admin recalculation live verification covered PEK pickup/courier rates, pickup map, selected-terminal repricing/save, courier save, and foreign country preservation. Foreign PEK shipment automation is intentionally disabled: managers create international cargo in the PEK cabinet, then attach the cargo code for status, configurable universal mapping, raw PEK carrier-status presentation, and actual-cost tracking. International preregistration, cancellation mutations, document-download workflow for manually attached foreign cargo, accompanying-documents service, recipient identity/tax/customs workflows, and accepted-cargo return remain out of scope by design.

0.136.0: PEK read-only contract verification passed, the 0.135.5 services-object fix produced the first successful production submit, documents work, and 0.135.7 added one-shot cancellation reconciliation. The already-cancelled live shipment then returned `cargoStatus="Аннулировано до приемки груза"` with integer `cargoStatusId=-3`; 0.135.8 made that wire shape readable. Live 0.135.8 status update succeeded but left the cancelled shipment in active order storage until manual "Удалить из заказа". 0.135.9 changes `CANCELLED` status reconciliation so fresh carrier-cancelled status terminalizes the creation attempt and removes the active PEK shipment automatically. 0.135.10 hardens that cleanup by requiring the attempt lifecycle dependency, and fixes the next exact preregistration contract evidence: the first controlled courier create for 8800 g cargo passed preview but PEK rejected missing `cargos[0].services.sealing`, so disabled sealing is now serialized as `{enabled:false}`. The reviewed 0.135.10 courier retry succeeded and created a real PEK shipment; status update then failed only on `cargos[].info.deliveryPlanDate`, and 0.135.11 diagnostic phase captured exact production value `2026-08-19T22:23:16.833`. Final 0.135.11 accepts strict 3-digit milliseconds only for `deliveryPlanDate`, preserves the original value, and does not invent a timezone or broaden other status dates. 0.136.0 adds a PEK admin tab for configurable raw-status mapping with independent pickup/courier universal statuses while preserving immutable carrier safety predicates for cancellation, accepted cargo, and cancelled cleanup. Final live acceptance verified the supported RU physical-recipient scope end to end: checkout pickup/courier, order delivery recalculation pickup/courier save, pickup shipment create/status/documents/cancellation/cleanup, courier shipment create/status/documents/cancellation/cleanup, generation 2 allocation after terminalized cancellation, and the `Статусы ПЭК` admin tab. No further production PEK mutation calls are required for release readiness; additional real shipment creation or cancellation should happen only for a newly introduced mutation feature, a concrete reproduced bug, or a separately approved controlled test. Accepted-cargo return, legal recipients, plannedDate/cutoff/holiday scheduling, and the PEK cabinet phone-display peculiarity remain non-blocking follow-up items.

Version: 0.152.10

0.152.10 fixes the post-save manual pickup card disappearance caused by unconditional registry-backed `requires_rate_refresh=true`. Manual pickup now declares the provider-owned refresh capability as false, the save response preserves that server-owned flag, and existing providers without an explicit flag keep the previous refresh behavior.

0.152.9 fixes the selected-card persistence blocker found live after 0.152.8. Manual pickup rates no longer write a carrier-specific md5 destination fingerprint; they use the canonical checkout fingerprint, so the selected manual point remains current across WooCommerce checkout refreshes and page reloads for the same destination.

0.152.8 fixes the selected-card presentation blocker found live after 0.152.7. Manual pickup selection remained saved and valid, but the checkout renderer asked the session manager to match by shipping method id; now renderers pass the authoritative rate `pickup_family`, so the selected manual point card appears without a compatibility alias.

0.152.4 fixes the manual pickup first-load and empty-picker live bugs. Cold checkout renders can now discover pickup families from the actual rate DOM before the WDC session has saved rates, and pickup REST/provider resolution accepts trusted textual locality without a synthetic `location_id`. The generic resolver also follows the shared multi-place cargo contract instead of requiring exactly one place.

0.152.3 fixes the remaining manual pickup picker routing bug found live: manual pickup families such as `manual:manual_nsk_pickup:pickup` were not recognized by the two-segment frontend parser, so stale/empty context could fall back to Russian Post. The parser is now generic for multi-segment pickup families, generic requests without carrier context fail closed, and REST points routes reject carrier/family/method mismatches.

0.152.2 fixes the remaining manual pickup selector live integration bug. The rate metadata survived carrier, orchestrator, mapper, and `add_rate()`, but the renderer could receive real WooCommerce metadata as keyed meta objects; generic renderer normalization now unwraps those entries before checking pickup capability flags.

0.152.1 fixes two live checkout presentation defects in the 0.152.0 manual delivery type stage. Custom manual delivery labels are preserved through service decoration with the existing `preserve_rate_title` contract, and normal manual pickup rates no longer set the generic `no_pickup_selection` suppression flag, so the existing checkout pickup selector is rendered for manual pickup services.

0.152.0 adds manual delivery type and manual pickup points without changing Shipment Framework. Manual services now support courier, pickup, and custom delivery type settings; existing 0.151.x services default to courier. Pickup-type manual services expose selectable points only through the existing Pickup Framework, require at least one active point in the destination locality before a rate is shown, and store point identity as stable per-service code plus textual country/region/place data. Pricing remains flat/per-kg/ranges and zones remain out of scope.

0.151.2 keeps the 0.151.0 manual pricing scope and fixes zero-weight package identity: physical packages with items and zero product weight are no longer equivalent to empty packages in the WDC quote cache. Manual flat rates remain available for zero-weight physical products; manual weight-based modes remain unavailable until the package has positive chargeable weight.

0.151.1 keeps the 0.151.0 manual pricing scope and fixes two live defects: flat tariff saves no longer fail on hidden/stale billing-step values, and WooCommerce simple product or variation updates bump the shared delivery rates cache version so changed product weight/dimensions invalidate checkout shipping package caches globally.

0.151.0 completes the manual delivery pricing engine for the current non-zone scope. Manual services support flat price, price per 1 kg with optional tariff minimum, and upper-inclusive weight ranges; all tariff money is stored/calculated as kopecks and all tariff weight as grams. Billing step is configurable as no rounding, 100 g, 500 g, or 1 kg and is applied before per-kg/range pricing. Weight-based manual modes fail closed for zero chargeable weight or range gaps. Pricing stays carrier-owned and runs before Rule Engine and DeliveryService post-processing. Tariff zones, per-geography prices, Shipment Framework manual attach, documents, actual-cost UI, and external carrier lifecycle remain future stages.

0.150.0 establishes manual delivery services as dynamic DeliveryService rows served by one technical `manual` carrier. Stage 1 supports flat pricing only; `per_kg`, `weight_ranges`, city/region tariff rows, optional tariff zones, manual shipment attach/remove, and canonical actual-cost entry remain future stages. The implementation intentionally reuses existing country availability, rules fallback, delivery comments, lead-time, and order calculation snapshots, and leaves Shipment Framework persistence, documents, modal extensions, external status, autosync, cancellation, and API create untouched.

0.149.2 closes the Ozon pickup import race at the `enrichment -> ready -> active` boundary. The importer no longer performs a read-only completion check followed by generic `update_generation(state=ready)`; it calls repository-owned `mark_ready_if_complete()`, which transitions only a current `building/enrichment` generation with zero pending staging IDs, no conflicts, non-zero discovery, and matching terminal counters. Cancellation remains terminal and irreversible, while normal live-style `95 saved + 5 rejected` completion can still mark ready and reuse existing activation.

0.149.1 hardens manual cancellation of the two-phase Ozon pickup import. New generation rows store `lock_owner`, start passes the acquired scheduler owner into the generation, and stop uses that stored owner for exact step unscheduling and owner-checked lock release. If an in-flight Ozon request returns after the generation has been cancelled, the importer re-reads current state/phase at the commit/catch boundary and returns terminal success instead of writing retry/failure diagnostics or scheduling more work. Real DB commit failures are still surfaced when the generation is still building in the expected phase. Discovery no longer sends a misleading pre-transaction `downloaded_count`; repository commit sets `downloaded_count` and `discovered_count` from the actual unique staging ID count.

0.149.0 moves the Ozon Delivery pickup catalog import from the long single-stage `list -> info -> persist` traversal to a two-phase generation build. Phase `discovery` walks `/v1/delivery-point/list` only and freezes unique IDs in `wdc_ozon_delivery_pickup_ids`; phase `enrichment` processes pending frozen IDs through `/v1/delivery-point/info` batches. The previous active generation remains the only provider/checkout/map source until the new generation passes the completion invariant and existing activation succeeds. HTTP 404 from `/info` is now localized by bounded binary split: valid IDs in the batch are saved and individual disappeared IDs are marked rejected with `not_found_404` instead of stopping discovery. Managers can manually stop a building import; the generation becomes `cancelled`, staging and non-active partial point rows are removed, pending stale steps no-op, and a fresh import can be started.

0.148.07 completes the Ozon pickup import resilience hardening by separating permanent `redirect_rejected` cases from retryable `redirect_limit` overflow of an otherwise valid same-host Ozon redirect chain. Ozon delivery API calls now wrap HTTP-client infrastructure exceptions with endpoint-aware operations, so pickup import diagnostics identify `v1/delivery-point/list` or `v1/delivery-point/info` instead of generic `http`. OAuth token transport behavior remains unchanged because it does not flow through a delivery API `$path`.

0.148.06 hardens Ozon Delivery pickup catalog import against temporary API/transport failures. A retryable Ozon API exception, including transport errors, HTTP 429, and HTTP 5xx, no longer fails the whole generation immediately: the same page cursor is retained, counters are not advanced, safe transient diagnostics are stored, and Action Scheduler retries after 2, 5, and 10 seconds. If the fourth attempt fails, the generation is marked failed with safe fields for code, operation, HTTP status, retryability, page, cursor, attempt, and timestamp. Permanent parser, pagination, duplicate-conflict, persistence, credentials, unsafe redirect, and request validation failures still fail immediately. New/default Ozon timeout is 30 seconds; explicitly stored values such as 15 seconds remain unchanged and still pass through the 1..30 second bounds.

0.148.05 closes the DPD order-admin recalculation same-location pickup leak: saved terminal reuse now requires both the same canonical destination and explicit DPD pickup ownership, while explicit current DPD pickup selection and customer checkout session behavior stay unchanged.

0.148.01 fixes the grouped domestic Russian Post tariff selector variant payload so `delivery_days_label` and `planned_delivery_comment` are both derived from the final `DeliveryRate->delivery_days` state.

0.148.00 fixes two interim defects: existing international Russian Post services keep the administrator's enabled/disabled state when builtin services are ensured, and the order shipments metabox now shows only `Добавьте стандартную службу доставки` for orders without an explicitly supported Shipment Framework carrier.

0.147.24 fixes the final Jet Logistic order-admin lifecycle UI details. Shipment remove AJAX now resets the metabox from the returned server capability payload, so Jet keeps `can_create=false` and shows only manual attach after local remove without reload, while create-capable carriers can still show prepare immediately when their payload allows it. Jet manual attach now opts into one best-effort status refresh through the existing generic status AJAX path; the attached tracking number remains saved if that refresh fails. The 0.147.23 same-day Jet status resolver, status mappings, autosync architecture, pricing, geography, and Shipment Framework contracts were not changed.

0.147.23 fixes Jet Logistic same-day status chronology. When Jet returns date-only events with equal timestamps, the resolver now treats later source rows as later same-day events, so the current status for cargo `6333837000` resolves to `Отправка груза со склада приемки...` rather than the earlier same-day receiving event. Status mappings, API client behavior, autosync architecture, max-five event storage, and Shipment Framework contracts were not changed.

0.147.18 fixes the remaining Jet Logistic checkout order validation issue without adding a Jet-specific bypass. Generic checkout validation now requires pickup-point selection only when the selected rate is `pickup` and explicitly has `requires_pickup_point=true`; Jet Logistic pickup is persisted as pickup delivery with `requires_pickup_point=false`, no selector, and no fake pickup-point order meta. CDEK/Yandex/DPD/PEK-style selectable pickup validation remains protected by the same current-selection and stale-selection checks. No migrations, Jet pricing, geography, status runtime, Shipment Framework, or Rule Engine changes were made.

0.147.17 fixes the Jet Logistic pickup contacts URL regression: the carrier now stores `https://jet.com.kz/контакты.html` as the canonical URL and the checkout renderer continues to escape it at output. Tests cover carrier metadata, WooCommerce metadata, rendered href, and the guard against `https://jet.com.kz/.html`. No migrations, pricing changes, geography changes, status changes, Shipment Framework changes, or Rule Engine changes were made.

0.147.16 finishes the Jet Logistic pickup comment requirement: pickup rates now carry a structured warehouse contacts link rendered safely in checkout before ordinary carrier comments, Rule Engine comments, and planned delivery text. The link points to the Jet Logistic contacts page and is underlined via checkout CSS; courier rates do not receive it. No migrations, pricing changes, geography changes, status changes, Shipment Framework changes, or generic Rule Engine changes were made.

0.147.15 finishes the confirmed Jet Logistic checkout pricing adjustment: carrier base prices now add configurable Jet insurance before the generic Rule Engine sees the rate, `api_base_price_rub` includes that insurance, and raw API components remain available as `jet_price_*` metadata. The Jet settings tab adds insurance percent, minimum insurance rubles, and an optional Алматы free-courier checkbox that zeroes only the effective `price_delivery` component for canonical Jet destination `Алматы`. Remote-terminal courier titles now include the final Jet city, for example `Джет Логистик курьером в Атбасар`. No Jet geography, status runtime, courier component interpretation, Shipment Framework, or generic checkout change was made.

Delivery service country synchronization is idempotent and concurrency-safe in 0.145.1. `DeliveryServiceCountryRepository::replace_countries()` still performs an exact replace, but it now normalizes and sorts the desired set, exits without writes when current rows already match, deletes only stale country rows, and inserts missing rows with a duplicate-safe `ON DUPLICATE KEY UPDATE` no-op that preserves existing `created_at`. Repeated `ensure_builtin_services()` bootstrap for fixed `RU` services no longer does DELETE-all/INSERT-all churn on every request, while a missing builtin `RU` row still self-heals on the next boot.

Ozon Delivery is at 0.147.8: buyers can choose either `Ozon до ПВЗ` or `Ozon курьером`, and managers can create both pickup and courier Ozon shipments through the existing Shipment Framework lifecycle. Courier checkout still uses official `/v1/order/checkout` `delivery.courier.coordinates` from server-confirmed DaData exact-address geo data or the nearest active Ozon pickup proxy within 1 km for preliminary pricing. Order creation freezes trusted structured recipient-address evidence in generic `_wdc_platform_structured_recipient_address` when it still matches the final active billing/shipping destination; shipment modal preview/create revalidates manager-edited courier address text server-side before Ozon preflight/create, takes selected location identity only from server-owned order snapshot/meta/calculation context, and fails closed when DaData city/settlement FIAS contradicts selected location FIAS. Courier modal Create stays unavailable until the existing normalized address snapshot confirms street, house/stead, required structured fields, and server-confirmed coordinates, including initial raw-address render, the live JS availability path, and post-error busy-state release; failed empty snapshots are treated as empty plain fields and generic recognition failure says `Не удалось распознать адрес, попробуйте исправить его.` The left recipient column contains name, phone, original address, and optional apartment/entrance/floor/intercom fields, the right delivery column contains scenario, analysis action/status, confirmed address, and readonly structured address fields, the unused Ozon recipient email field is hidden, and success status reads `Адрес для Ozon подтвержден.` Courier `order_create` sends official `delivery.courier` coordinates plus structured address fields, while shipment-time preflight is derived from the same canonical create body and strips create-only fields.

Ozon Delivery has a local pickup-point catalog at `Службы доставки -> Ozon Доставка -> ПВЗ Ozon`, checkout pricing runtimes for `Ozon до ПВЗ` and `Ozon курьером`, and Shipment Framework integration for pickup and courier shipments. Buyer pickup uses the local Ozon provider and selected-point repricing; buyer courier uses trusted checkout coordinates in the official `/v1/order/checkout` courier delivery object. Shipment creation uses only actual Shipment modal places and item assignments, never checkout Packaging metadata. The buyer Ozon rates append the two required customer comments through a carrier-owned post-rule provider; the refusal amount is frozen from `crossed_price ?? price` and persists in pickup snapshot comments for pickup or generic order-level customer comments for courier. The carrier-owned post-create flow remains shared for order create, approve, confirmation polling, status, cancellation, returns, labels, manual attach, and order-admin selected-rate pickup context.

0.137.3 fixes the follow-up Jet Logistic production validation blocker where `calc_transport` returned a non-currency `valuta` value together with a readable RUB `valuta_name`. Checkout and connection diagnostics now share the same parser precedence: `valuta_name` is authoritative, textual `valuta` is used only when the name is absent, numeric `valuta` is treated as provider metadata, and missing/numeric values fall back to the confirmed RUB Jet profile. Explicit non-RUB readable currency still blocks rates; diagnostics expose only safe `valuta` and `valuta_name` fields.

0.136.0 live follow-up: 0.134.17 fixed sender warehouse modal override and switched courier shipment geography to `/branches/findzonebyaddress/`; 0.134.18/0.134.19 fixed new and historical courier location identity recovery. 0.135.0 added the generic attempt lifecycle that prevents a second PEK shipment after cancellation or discarded pending/local removal from accidentally reusing the previous `customerCorrelation`; 0.135.1 hardened the lock/lifecycle implementation, 0.135.2 addressed stale positive WordPress option cache, 0.135.3 fixed the successor-publication race, 0.135.4 surfaced safe PEK logical rejection details and staged read-only diagnostics, 0.135.5 applied the exact PEK preregistration services-object fix, 0.135.6 handled the first successful submit status sentinel, 0.135.7 handled live cancellation reconciliation plus string `"-1"` status sentinel compatibility, 0.135.8 handles cancelled-status integer `-3` compatibility, 0.135.9 makes fresh `CANCELLED` status update converge to the same terminal/delete lifecycle as confirmed cancellation, 0.135.10 serializes `services.sealing.enabled=false` for non-sealed cargo after the first controlled courier validation rejection, final 0.135.11 accepts exact production `deliveryPlanDate` milliseconds after the diagnostic phase captured `2026-08-19T22:23:16.833`, and 0.136.0 exposes PEK status mapping in admin settings without letting editable business mapping change carrier API safety/lifecycle. Pickup and courier production create/status/document/cancellation lifecycles, cancelled cleanup, and next-generation creation-attempt allocation have been verified for the supported RU physical-recipient scope.

Stable subsystems:

- core bootstrap and DI composition root;

- Ozon Delivery secure API, pickup pricing, and pickup shipment creation. The carrier/service key is `ozon_delivery`; it is a builtin DeliveryService with initial `RU` country and generic service tabs, plus `API Ozon` and `ПВЗ Ozon` carrier tabs. The private application uses `client_credentials` and encrypted transient token cache; `refresh_token` is ignored and never stored. Pricing uses a carrier-owned quote layer over official `POST /v1/order/checkout`, not checkout/orchestrator logic. Shipment creation uses the same selected buyer PВЗ, manager-confirmed places from the Shipment modal, official pre-create `POST /v1/order/checkout` actual-cost calculation, official `POST /v1/order/create`, automatic carrier-owned approval, status sync, cancel, and labels.
- checkout orchestration;
- packaging;
- locations and pickup infrastructure;
- rules engine;
- shipment adapters, persistence mappers, lifecycle continuation, document providers, modal extensions, admin AJAX, carrier JS extensions;
- provider-owned document action contract;
- bounded plugin architecture smoke in the framework regression group;
- unified shipment regression profile.
- shipment cost analytics on the overview admin page, comparing the selected order delivery's base API planned cost with canonical actual shipment cost.
- CDEK EAEU support through the single `cdek` carrier/service with configurable `RU`, `AM`, `BY`, `KZ`, and `KG` availability.
- Jet Logistic support through one carrier/service with separate geography import, two quote rates from one API calculation, manual tracking attach, status mappings, and shared autosync.
- PEK foundation and delivery-service settings. The built-in PEK service is disabled by default, stores legal sender data and the default Russian sender warehouse, preserves canonical branch timezone/availability fields, and declares supported-country capability for `RU`, `AM`, `BY`, `KG`, and `KZ`; administrators still choose the enabled subset through the existing service-country UI. PEK Shipment Framework verification has passed for the supported RU physical-recipient scope: pickup and courier create/status/documents/pre-acceptance cancellation are verified, cancellation false-negative reconciliation is handled, live cancelled-status integer `-3` is bounded to the cancelled-before-acceptance status, and fresh carrier-cancelled status automatically removes active PEK shipment storage after terminalizing the creation attempt. For `AM/BY/KG/KZ`, automatic create/cancel/document mutation workflows remain intentionally absent.
- PEK geography, destination pickup, quote foundation, and checkout runtime. `PekCarrier` is registered in `CarrierRegistry` while PEK automatic Shipment Framework creation remains RU-only. PEK calculation supports `RU -> RU/AM/BY/KG/KZ`, and the delivery service is still controlled by the existing service/country/admin enablement pipeline. The runtime reuses carrier-owned geography mappings, provider-backed destination terminals, and `PekQuoteService` to return `ПЭК до терминала` and `ПЭК курьером` rates without adding a PEK branch to `CheckoutOrchestrator`. Pickup quotes use a trusted selected `pek:pickup` warehouse when present, otherwise a preliminary suitable terminal chosen through the provider; after save, the selected warehouse ID becomes calculator `receiverWarehouseId`. A provider-valid terminal can still fail calculator pricing; this selected-terminal failure now triggers explicit recovery metadata on the current recovered rate, family-scoped clearing of only `pek:pickup`, a preliminary PEK recovery rate, preserved PEK method choice, and an inline transient method-level message asking for another terminal, without silently keeping the bad card or clearing other pickup families. That message is stripped before `wdc_platform_rates`, order meta, and calculation data persistence; the frontend keeps it only in current-page module memory across internal WooCommerce recalculations. Stale DOM/local selected-point state and POST save success do not clear the message: only authoritative post-calculation `/checkout/state` with a valid same-family selection does. Authoritative empty selections remove stale local points while preserving the message; destination, method-family, and reload still clear it. PEK public pickup REST uses nonce-protected registry-backed queries from production `rate_meta['pickup_provider_query']` stored on the session rate; `/points`, `/points/search`, and pickup save bootstrap the existing WooCommerce customer session before reading trusted rate context, distinguish `provider_session_unavailable` (503) from missing stored rate context (400), and still ignore browser cargo/location/address/coordinate data. Address-only `null/null` coordinate snapshots are accepted for canonical location IDs, malformed coordinate pairs fail closed, save validates only a point code through fresh `resolve_selection()`, stores a provider-owned destination fingerprint separately from the generic checkout destination fingerprint, and stale destination selections are cleared for PEK only. Free PEK points display as `Собственный пункт выдачи ПЭК`; paid points display as `Партнерский пункт выдачи ПЭК` with a possible surcharge warning, while internal warehouse UUIDs remain technical metadata. Courier quotes use location-level address plus coordinates only before a full address exists; full-address quotes omit canonical city coordinates. The final checkout base price uses `PekQuoteResult::price_kopecks`; saved calculation `api_base_price_rub` now stores this adjusted base, while PEK `costTotal` is preserved separately as `pek_carrier_base_price_rub`/`pek_carrier_price_kopecks`. Configured bag/plombing surcharges are formula comments before rules, not applied Rule Engine entries or rule delta. Quote cache keys include generic address/package/pickup-selection fingerprints and PEK’s safe context for selected terminal/provider fingerprint, full-address scope, request-scoped memoized planned datetime bucket, sender warehouse, hashed pricing identity, surcharge settings, and transport fields. No raw PEK request/response, credentials, client card, INN/KPP, or raw provider references are persisted in rates/session/order context.
- PEK quote/calculator diagnostics remain available for admin live checks. They use the same plannedDateTime resolver as checkout, continue to send documented `isHP=false` and `sealingPositionsCount=0`, and continue to separate carrier `costTotal` from store-owned light-cargo surcharges. Calculator response parsing, redaction, service breakdown typing, and user-scoped diagnostic storage are unchanged by checkout runtime.
- PEK schema integrity recovery. Migration `0050_repair_pek_foundation_schema.php` covers installed sites where migration history and physical schema drift apart, including the observed partial state where `wdc_pek_terminals` exists but `wdc_pek_location_mappings` is missing. The carrier-owned integrity service checks both PEK foundation tables with the current WordPress prefix, creates only absent tables through the existing repository installers, verifies postconditions before migration state can advance, and is idempotent on repeated update runs. The mapping table now uses physical `mapping_precision` to avoid the MySQL 8 reserved identifier `precision`; repository read/write code keeps the domain key `precision`, and migration `0051_migrate_pek_mapping_precision_column.php` non-destructively backfills `mapping_precision` from any legacy physical `` `precision` `` column. PEK installers check `$wpdb->last_error`, and migration boot reports failures through logging/admin notice instead of an unhandled site-wide fatal. Recovery does not perform destructive SQL, PEK API calls, canonical location writes, or runtime diagnostic DDL.

Active known limitations:

- `ShipmentCreationService` still accepts an adapter array temporary test-construction fallback beside the registry for direct test construction.
- Russian Post document action is temporarily hidden because Otpravka currently returns `Forbidden mail type` for `/1.0/forms/backlog/{id}/forms` before batch formation; the download implementation remains in place for future API re-check.
- Regression manifest keeps two baseline failures: DPD shipment preparation dry-run date pickup and CDEK large pickup list rendering.
- Two optional Russian Post carrier tests still require fuller WooCommerce test stubs.

Recent fixes:

- CDEK now supports RU/AM/BY/KZ/KG through the existing service-country repository. Existing installs receive a one-time migration from empty/RU-only CDEK countries to the five-country default, while fresh installs seed the same defaults only when the built-in service is first created.
- DPD Geography imports AM/BY/KZ/KG locations into the shared locations table with `dpd_city_id` idempotency and without fake Russian identifiers. Foreign rows without GAR/FIAS use SQL `NULL` identity values in both single-save and bulk-upsert repository paths, ignore DPD foreign postcode/KLADR as canonical Russian fields, and reconcile the earlier malformed BY/Minsk row instead of requiring manual deletion. Foreign display names omit visual country-code prefixes while retaining `country_code` for identity and checkout filtering, and multiple existing exact foreign identity rows are resolved deterministically by current DPD mapping or lowest positive location id with `foreign_duplicate_identity_rows` diagnostics instead of creating more duplicates. Canonical, legacy, and DPD mapping lookups fail closed on SQL errors or invalid result shapes, so lookup failures become warning import row diagnostics instead of creating duplicate locations. Mapping finalization is transactional, preserves existing working mappings for conflict rows, skips stale cleanup on warning imports with row-level errors, reports logical changes separately from candidate count, and uses token-prefiltered PHP-normalized foreign identity rules in production SQL and in-memory smokes. Production finalization now splits candidate application into qualified UPDATE and INSERT SELECT statements, avoiding ambiguous `dpd_city_id` references from `INSERT ... SELECT ... ON DUPLICATE KEY UPDATE`. The serialized location index is built from fail-closed SQL pages before artifact creation, versioned, checksum-validated before each step, and separates own `fias_id` from contextual `city_fias_id` so a settlement's own FIAS cannot be shadowed by another row's city FIAS; the compact index stores only `own_fias`, `city_fias`, `kladr`, and `name`, while hierarchy metadata for own-FIAS ambiguity is loaded lazily for the candidate ids. Diagnostics now expose own-FIAS matches, city-FIAS fallback matches, hierarchy disambiguations, and true own-FIAS ambiguity separately. Page-query failures, corrupt indexes, and count SQL failures stop before destructive stale cleanup, public failure responses redact internal paths and index metadata, and rollback preserves working mappings. Browser import progress now separates read-only status polling from mutating locked step requests, caps browser steps at 500 rows, rejects stale offsets, and uses a self-scheduling runner without `setInterval`, so multiple tabs or slow foreign-row chunks cannot process the same CSV range concurrently. The import lock now uses atomic SQL compare-and-delete for expired takeover/release, protects start/step/reset, rejects failed jobs with saved artifacts until reset, blocks legacy runner-protocol jobs from automatic continuation, and keeps `state_revision` monotonic across jobs. Checkout location search now fetches direct own-name candidates before broad hierarchy candidates and orders direct candidates exact-before-prefix in production and in-memory paths, so exact BY Minsk is not hidden behind large Minsk-region result sets. Selected local DB checkout locations write only the own typed place into the visible WooCommerce city field while preserving region/district/canonical city metadata in hidden fields and session context; country changes clear destination city/state/postcode, local-location metadata, pickup selection, and cached quotes. CDEK city resolution is country-aware, supports manual city input, and does not persist CDEK city codes.
- CDEK shipment creation now handles international pickup and courier on the shared adapter/request/status/document pipeline. Non-RU courier uses raw WooCommerce address data plus resolved CDEK city code, and the CDEK-only optional recipient document field is visible from initial modal render for AM/BY/KZ/KG, maps to `recipient.tin` for KZ/KG or `recipient.passport_number` for AM/BY, and is never persisted or logged.
- CDEK pickup maps for manual EAEU cities reuse the resolved CDEK city code and fit the initial map viewport to loaded pickup point coordinates when no trusted destination coordinates exist. The derived viewport is never persisted as location data, physical user interaction cancels pending initial auto-fit, provider fit events no longer suppress the first subsequent user pan, explicit address/geolocation viewport actions refresh pickup points without being overridden by a later initial fit, and confirmed point selection cancels pending provider fit even when it does not focus the map.
- Checkout and order delivery recalculation now share a delivery lead-time pipeline: carrier raw lead time, shop processing calendar, optional carrier working-day conversion, delivery date rules, and final planned date. The global processing default is `2` shop working days, the per-service working-day flag defaults to off, and existing manual processing additions in rules should be removed manually where they are no longer wanted.
- The overview admin page now shows only platform information and delivery quote cache cleanup, while system requirements notices remain handled globally by `AdminNotices`.
- Checkout runtime gating now uses `CheckoutFeatureGate` with `enable_new_checkout_shipping` as the single source of truth; the legacy feature flag service was removed.
- Yandex Delivery shipment modal restores source drop-off PVZ presentation from the canonical V2 pickup context, including `full_address`, `schedule_text`, and coordinates, so the modal address and drop-off map survive the shipment framework flow.
- Shipment pickup picker defines its local `pickupContext()` resolver again, so Yandex source drop-off picker can open, run initial map search, and perform address search without a `ReferenceError`.
- Yandex cancellation polling toast treats `cancellation_started` lifecycle/poll flags as pending and no longer interprets the intermediate `carrier_status_title` as terminal failure.
- Yandex cancellation polling success toast now requires explicit cancellation confirmation via `cancelled_and_removed` or `yandex_status=CANCELLED`; empty `yandex_status` stays non-success.
- Yandex cancellation polling success now finalizes the existing progress toast by replacing it with a success message and auto-hiding it, so no persistent progress toast remains after cancellation.
- Shipment place analytics now refresh directly from current `shipment_item_rows` when item place assignment changes, so split rows are counted in their selected cargo places immediately.
- Ozon actual shipment declared value now comes from manager-confirmed Shipment modal rows: quantity times unit price grouped by actual place. Manual rows and edited prices are respected; Ozon no longer matches rows back to WooCommerce order items for declared value.
- Yandex cancellation `cancelled_and_removed` now finalizes an existing cancellation progress toast even after the shipment UI has already been reset.
- Yandex terminal cancellation tick now treats the active cancellation toast state as lifecycle ownership, so missing terminal purpose metadata cannot leave the last progress toast visible.
- Yandex cancellation status AJAX now preserves the adapter's `cancelled_and_removed` marker in JSON, so terminal polling resets the shipment UI and finishes the cancellation toast instead of falling back to persistent progress.
- Shipment cost analytics now appears on `admin.php?page=wdc-platform`. It uses the `wdc_shipment_cost_analytics` materialized read-model table, indexes at most one matching created shipment per eligible order, uses `ShipmentBaseApiCostResolver`, canonical `actual_cost_*` fields, registry-driven carrier filters, integer 3% threshold checks, SQL sorting/pagination/aggregates, and no runtime WooCommerce order scan.
- PEK foundation now seeds a predefined disabled `pek` API delivery service with only `RU` on first setup and preserves administrator country choices on later boot, including an explicit empty selection. PEK settings store login, encrypted API key, legal sender data, client card, HTTP limits, SMS-release contractual limit, and the default sender warehouse snapshot including safe source (`free`, `nearest_cached_selection`, or `nearest_fresh_revalidation`). PEK admin POST actions execute through the carrier-owned admin component and PRG redirect back to the PEK tab; notices are user-scoped one-shot transients, not global settings. `/typesOfDelivery/all/` uses GET with JSON headers and no body; branch/legal/warehouse foundation methods use POST. Manual diagnostics run product, country, legal-form, and warehouse API checks independently, so a 403 from one reference endpoint does not interrupt the remaining read-only checks. Diagnostics expose `connection_ok`, `all_checks_passed`, endpoint, method, status, HTTP status, and public-safe messages; `/branches/all/` HTTP 200 confirms warehouse API availability without resaving the selected warehouse, while saved-ID semantic matching is reported separately as informational `warehouse_match` with safe counters and no raw response. A warehouse ID mismatch no longer means PEK explicitly rejected the ID and does not affect `all_checks_passed`. Machine `checked_at` datetimes are no longer masked as phones. Manual warehouse search uses `/branches/nearestdepartments/` with `departmentOperation=2`, `type=3`, and cargo constraints, preserves the current-user cache until a replacement search succeeds, normalizes official `timeZone` values like `04:00:00` into canonical `branchTimezone` values like `UTC+04:00`, stores only successful new short-lived current-user results, excludes paidDepartments for sender self-delivery, and returns controlled safe errors instead of uncaught exceptions on read-only API failure. Selected warehouse IDs are saved only from fresh current-user cache; shipment preview revalidates defaults through fresh nearestdepartments search by stored address, may use exact trusted persisted snapshot fallback on non-authoritative access/transport/5xx failures, and validates modal overrides through the same cache plus constraints fingerprint. Existing PEK pickup shipment preparation may also use an exact persisted checkout terminal selection when fresh destination nearestdepartments recheck is unavailable, but only with matching carrier/service/family, warehouse, branch, RU location, provider fingerprint, timestamp/source, availability, and local cargo limits; checkout terminal discovery still remains fail-closed and never uses stale lists. Unresolved timezone/date rows fail closed rather than falling back to `/branches/all/`. Shipment Framework remains unchanged.
- PEK shipment creation uses sender-paid services, self-delivery to the selected PEK warehouse (`orderType=0`), mandatory insurance, product-line declared value, physical recipient SMS-release preconditions, framework-owned creation attempts, manual reconciliation for uncertain submit results, status/autosync, actual cost from `services.sum`, provider-owned documents, and fail-closed pre-acceptance cancellation through `/order/cancellation/`. Production verification on the test site now covers pickup and courier creation, status, documents, cancellation, automatic cancelled cleanup, and generation 2 allocation after terminalized cancellation. No further production PEK mutation testing is required for release readiness; do not create or cancel additional real PEK shipments merely for testing. Legal recipients (`physical | legal`) are a future design target, accepted-cargo return remains cabinet-only, and the plugin must never use `/cargos/cancelandreturncargo/`.

Canonical docs:

- [plugin architecture](../architecture/plugin-architecture.md)
- [shipment framework](../architecture/shipment-framework.md)
- [new carrier guide](../development/new-carrier-guide.md)
- [testing and regression](../development/testing-and-regression.md)

Primary test command:

```bash
php tests/shipments/run-shipment-regression-profile.php
```

PEK foundation profile:

```bash
php tests/shipments/run-shipment-regression-profile.php --group=pek
```
