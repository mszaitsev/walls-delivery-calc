# Testing And Regression

Version 0.147.8 keeps the PEK shipment smokes in the regression manifest and keeps `ozon-delivery.shipment`, `ozon-delivery.returns`, and `ozon-delivery.modal-limits` for Ozon Delivery Shipment Framework integration. The delivery-services smoke verifies country repository no-op writes, normalization, stale-row diff deletion, duplicate-race UPSERT behavior, repeated builtin bootstrap idempotency, missing fixed-country self-heal, exact admin replace, and genuine DB failure surfacing. The Ozon checkout smoke verifies independent pickup/courier rates, exact courier `/v1/order/checkout` coordinates payload from server-confirmed DaData exact-address evidence or the nearest active Ozon pickup proxy within 1 km, forged hidden DaData coordinate rejection, active billing/shipping prefix isolation, FIAS mismatch rejection, safe courier HTTP 200 carrier-rejection diagnostics, delivery+insurance pricing, customer comments for both Ozon rates, promo/crossed-price semantics, WooCommerce rate metadata, and no duplicate comments. The WooCommerce checkout smoke verifies generic order-level courier customer-comment persistence/rendering and now freezes generic structured recipient address snapshots only from trusted checkout evidence. The Ozon shipment smoke verifies pickup create parity plus courier `/v1/order/create` payload, shipment-time preflight/create parity, multi-box postings, pickup isolation, preflight failure blocking, Ozon courier address normalization using server-owned selected location context instead of browser modal location/FIAS fields, fail-closed city/settlement FIAS locality correlation, courier modal UX gating until house-level server-side address analysis succeeds, initial raw-address Create disabling through the real JS availability path, post-error create busy-state release through canonical availability recalculation, Ozon-only recipient email hiding, success/failure wording, Array `fields: []` handling without `Array.prototype.flat` leaks, left-column original address plus optional apartment/entrance/floor/intercom placement, right-column analysis/confirmed address placement, and responsive 2-to-1 internal field grids. The Ozon smokes are fake/local only and must not call production Ozon APIs.

Version: 0.155.16

## One-Click GAR Regression

Run `php tests/locations/run-locations-incremental-update-smoke.php` and `node tests/locations/run-locations-incremental-update-ui-smoke.js`. Coverage includes the full automatic workflow, source-field matrix, foreign empty-FIAS preservation, NEW-only scoped enrichment, postcode-before-coordinates, limit/resume cursor, bounded seed/changes/aliases, cancel, failed validation, atomic-apply recovery and post-apply cache invalidation. `php tests/locations/run-location-database-backup-smoke.php` additionally checks the shared logical/physical lock, own-job requests, start reservation and terminal release. Its download test uses binary child-process pipes on Windows.

Keep the FIAS/GAR, postcode, Russian Post courier, DPD geography, locations/coordinates, architecture and full shipment profiles green. Local database fakes and mocked HTTP do not replace production-sized MySQL and API acceptance: create a backup, test disk/SQL timeouts, pause/resume, foreign preservation and checkout after apply.

Locations backup regression: `php tests/locations/run-location-database-backup-smoke.php` covers strict paired discovery, non-default prefix, row/schema preservation, aliases-copy failure, shadow-copy cleanup, atomic paired restore, incompatible schemas, active jobs, shared-lock contention, release before AJAX exit, fixed script download bytes and exact multiline command rendering. Run all `tests/locations/run-*-smoke.php`, FIAS smoke, architecture smoke and both framework/full shipment profiles after composition changes. SQL-shaped doubles do not replace a disposable MySQL/MariaDB integration check of privileges, table engines, metadata locks and large-table operation timing.

The platform setting `checkout_sort_selector_enabled` defaults to true, including upgrades with a missing key. When enabled, customers can use the checkout sorting selector; when disabled, no selector markup is rendered and `checkout_sort_mode` from admin is authoritative, ignoring posted/customer modes. Checkout synchronizes the session to the forced mode so re-enabling starts from the last admin-synchronized value. Actual effective-mode transitions, whether customer-driven or forced by admin, reset tariffs and WDC method choices once; stable-mode refreshes preserve manual tariff/method selections. Runtime/selection smokes cover checkbox persistence, visibility, stale POST/session, forced sorting and re-enabling.

Run `php tests/checkout/run-checkout-rate-sorting-smoke.php` for the price/term matrices, zero-last ties, missing bounds, grouping, stable fallback and final-vs-original regression.

0.155.14 unifies both RateSorter stages on final checkout price and delivery days. "По цене": non-zero price ASC, zero LAST, then min days and max days ASC. "По сроку": min days ASC, max days ASC, then the same non-zero-first price order. Null bounds independently sort after known bounds; title, tariff key, rate ID and input index break exact ties. Rates are sorted within each method first; methods are then compared by their active rate. Grouping is unchanged and JavaScript does not sort rates.

Address autocomplete regressions cover all terminal premise aliases, preserved case and room identifiers, final reattachment, Enter submit prevention, trailing-comma cleanup through each finish path, late responses after finish, geographic-context clearing versus same-context refresh, and spinner reduced motion. Browser coverage runs at desktop/mobile widths. Location picker smoke includes the narrow `Москва`/`Москв` priority, a negative `Москов` case and forced-region isolation alongside the existing Ивановка cohorts.

0.155.13 replaces the checkout address modal with an inline autocomplete attached to `billing_address_1`. It is enabled only for a canonical RU WDC location with a FIAS identity; manual, unresolved and non-RU destinations keep a plain editable address. `AddressSuggestionAjax` verifies the checkout nonce and active DB location, then uses the dedicated `address_inline` DaData request with a fixed city boundary (street through house, maximum 8 results). It never retries without that boundary. Suggestions cannot change city, region, postcode or WDC location metadata. Manual address text is always allowed.

The frontend waits 300 ms after at least 3 characters, cancels stale requests, and supports arrows, Enter, Escape, Tab and outside clicks. Street selection keeps editing active. House metadata marks a final suggestion; its address-only prefix receives a comma separator, and apartment/office tail edits do not call DaData. Editing the confirmed prefix resumes search. The confirmed prefix is page memory only. Existing address evidence/session and order consumers remain supported; no DB migration or new persistent autocomplete state is introduced.

Run `node tests/checkout/run-checkout-address-autocomplete-smoke.js` and `php tests/address/run-dadata-suggestions-smoke.php`. The optional `run-checkout-address-autocomplete-browser-smoke.js` uses Playwright and `JQUERY_PATH` pointing to the installed WordPress jQuery; it checks real desktop/mobile DOM, focus, keyboard and immutable geography with stubbed AJAX and no DaData API calls.

0.155.12 adds `tests/checkout/run-checkout-address-fields-smoke.php` and `tests/checkout/run-checkout-address-fields-smoke.js` for checkout field UX. The PHP smoke verifies the actual Woo-style address/default, billing, and checkout field pipeline: field order priorities, exact labels, billing address line 2 removal, default optional billing address, required phone, multiline order-comments placeholder, generic courier metadata on rendered rates, billing courier address validation, and non-courier optional address behavior. The JS smoke verifies immediate `billing_address_1` required-marker toggling when switching pickup/courier methods and asserts the new script contains no shipping-address selectors. The PHP smoke is included in the status-core regression manifest.

0.155.11 extends checkout location coverage in `tests/checkout/run-checkout-location-picker-smoke.php` and `tests/checkout/run-checkout-city-selector-smoke.js`. The PHP smoke covers conservative legacy profile matching for Новосибирск and Саха/Якутия, typo/region mismatch/ambiguity rejection, country-index-supported AM/BY/KG/KZ/RU fixtures, and relevance-cohort region group sorting with alphabetic order inside a cohort. The JS smoke protects canonical region locking without disabling POST values and manual fallback clearing/marking/unlocking behavior. Checkout city changes should still run `node tests/checkout/run-checkout-city-selector-smoke.js` together with checkout/location PHP smokes.

0.155.10 adds `tests/checkout/run-checkout-delivery-messages-smoke.php` to the `status-core` regression group. The smoke covers disabled defaults, threshold kopeck storage, basis normalization, WYSIWYG sanitization and placeholder preservation, promo math at the threshold boundary, all-cart versus shippable-item basis selection, coupon-aware typed-total use, repeated AJAX-like renders without stale promo state, valid source table-row markup, relocation/idempotency source contracts, normal TinyMCE color toolbar config, multiline paragraph/`<br>` preservation, and scoped coupon-to-shipping spacing CSS. `tests/checkout/run-runtime-stabilization-smoke.php` also proves the message renderer is registered only when the global WDC WooCommerce runtime is enabled.

0.155.9 extends `tests/delivery-services/run-delivery-services-smoke.php` for the manual service create-screen flow. Coverage proves the list page no longer renders the inline create form, the primary create button links to `action=create`, the create route renders the existing manual service fields and Cancel link, successful POST redirects to the new service edit URL, validation errors preserve submitted values, duplicate manual/carrier/predefined/normalized keys are rejected, soft-deleted keys remain reserved, capability/nonce guards still stop persistence, downstream persistence failures roll back or compensating-clean the inserted service and free the key for retry, strict insert duplicate races show the duplicate validation error, genuine insert failures show storage failure, fresh unique creates follow the real `$wpdb->insert()` integer-return contract, and existing edit/delete/rename flows remain covered by the same smoke.

0.155.8 adds `tests/checkout/run-shop-processing-days-smoke.php` for the fixed/dynamic shop processing contract. It covers the exact dynamic formula matrix for capacity `20` and extra days `0/1/2`, capacity clamping, missing-mode fixed backward compatibility, dynamic settings isolation from fixed mode, cached order-count query behavior, status-change/settings-save invalidation, empty-status fail-closed behavior, HPOS-compatible `wc_get_orders()` paginated total usage, fallback to the last successful count, Delivery Services admin render/save sanitization, fixed/dynamic row markers, scoped admin JS enqueue/source checks for the visibility enhancement, and proof that resolver output still enters the existing shop calendar pipeline through `DeliveryLeadTimeNormalizer`. `tests/checkout/run-runtime-stabilization-smoke.php` also verifies that passive shop processing queue invalidation is registered when WDC WooCommerce runtime is both on and off, while shipment autosync and order-mutation background hooks remain runtime-gated.

0.155.7 extends `tests/checkout/run-runtime-stabilization-smoke.php` for the platform runtime contract. Coverage proves missing legacy setting, explicit enabled, explicit disabled, invalid persisted value, strict submitted checkbox sanitization, settings-page notice, checkout runtime registration gating, order/admin registration gating, already queued shipment status autosync execution guard, carrier geography/import/admin preparation availability while runtime is disabled, runtime-enabled preservation of the existing checkout/order/shipment registration behavior, and removal of the separate partial-checkout rollout flag from active code paths.

0.155.6 extends Rule Engine regression coverage for application condition totals. `tests/rules/run-rules-smoke.php` verifies persisted `order_total` conditions still use package-local delivered-goods totals, new `cart_total` conditions use `RuleEvaluationContext::all_cart_items_total()`, boundary comparisons work, fallback context remains safe, and old condition arrays roundtrip without key migration. `tests/rules/run-rules-admin-smoke.php` verifies labels, select ordering, numeric ruble UI parity, and simulator behavior for delivered-goods versus full-cart conditions.

0.155.5 extends Rule Engine regression coverage for price-operation bases and registers the `rules` regression group. `tests/rules/run-rules-smoke.php` verifies old rubles/delivery/package-local bases, new full-cart bases, mixed physical plus virtual cart totals, fallback `all_cart_items_total = order_total`, persisted old keys, and formula labels. `tests/rules/run-rules-admin-smoke.php` verifies admin labels and simulator inputs, while `tests/checkout/run-woocommerce-checkout-smoke.php` verifies WooCommerce package `contents_cost` and full cart item total are mapped separately.

0.155.4 updates the `checkout-selection` regression group to model the real WooCommerce `add_rate()` contract. The primary reorder cases now use raw actual keys such as `selection_demo:a`, `selection_demo:b`, and `self_pickup`, assert the `NewShippingMethod::$rates` keys produced by `calculate_shipping()`, verify fresh price use after reorder, prove no synthetic prefix is written to session, and keep non-WDC methods untouched.

0.155.3 extends the `checkout-selection` regression group for WooCommerce order-sensitive default arbitration. Coverage proves that WDC choices survive fresh rate reordering (`A,B,C -> B,A,C` and `C,A,B`), price-caused reorder with fresh cost, pickup/self-pickup reorder, legacy WDC prefix normalization, disappeared-method fallback, and non-WDC no-op behavior at the `woocommerce_shipping_chosen_method` filter.

0.155.2 adds the `checkout-selection` regression group for preserving checkout shipping choice identity after cart/package recalculation. It covers courier survival with fresh price data, removed-method reset, valid pickup point preservation, carrier-rejected pickup family cleanup without method reset, disappearing pickup-family cleanup, grouped tariff preservation/reconciliation, post-`save_rates` ordering, and guards against carrier-specific reconciliation branches.

0.155.1 expands the mandatory `self-pickup` regression group after live verification. Coverage now includes hidden self-pickup Rules tab with normal services unchanged, generic CHEAPEST sorting of paid and zero-price rates, active-only fixed cards, separate card title snapshots, promotion availability vs applied fee semantics, structured inline-link whitespace parity between checkout and order/email/account rendering, dynamic `{s}` fee labels, and compact generic non-shipment shipment-metabox presentation.

0.154.0 filters dominated Russian Post domestic tariff rates by price and delivery period for checkout and order recalculation, preserving only meaningful price/speed trade-offs.

The mandatory `manual-delivery` regression group includes 0.153.0 manual shipment coverage. It verifies one manual adapter/service registration, manual attach through `OrderShipmentRepository`, concrete service-key persistence, historical service-title presentation, local remove, generic actual-cost set/update/clear, base-cost comparison, multiple manual service isolation, pickup/courier/custom parity, no documents/autosync/API-create, and architecture guards against manual branches in generic shipment creation or JS.

The mandatory `manual-delivery` regression group includes 0.152.11 coverage for checkout reload rehydration: a cached manual pickup WooCommerce rate without explicit `pickup_family` still resolves to `manual:<service_key>:pickup`, state returns the selected point, and no `manual:pickup` compatibility bucket is created.

The mandatory `manual-delivery` regression group includes 0.152.10 coverage for provider-owned pickup refresh behavior: manual REST points and saved selections expose `requires_rate_refresh=false`, registry-backed points without the capability still default to `true`, and the saved manual card remains visible across same-destination render/update/reload checks.

The mandatory `manual-delivery` regression group includes 0.152.4 coverage for manual pickup query resolution and cold checkout rendering: `location_id=0` plus textual locality is a valid provider locator, missing locator data fails closed, multi-item cargo remains valid, REST returns manual points with and without coordinates, the first checkout calculation renders the selector before reload, and REST can resolve the current manual rate immediately after that first calculation. The pickup routing JS smoke also covers cold DOM-discovered `manual:<service_key>:pickup` families.

The mandatory `manual-delivery` regression group includes a pickup provider routing smoke for 0.152.3. It verifies frontend parsing for `manual:<service_key>:pickup`, stale Russian Post context override by the current manual family, generated API params with `carrier=manual`, prefetch identity separation between manual services, and fail-closed behavior for generic pickup requests with no carrier context.

The mandatory `manual-delivery` regression group now includes the 0.152.2 full shipping-path reproduction: manual pickup rates are asserted after `ManualDeliveryCarrier`, `CheckoutOrchestrator`, `WooCommerceRateMapper`, `NewShippingMethod::calculate_shipping()`, actual `WC_Shipping_Rate` metadata access, and `CheckoutRateRenderer`. The smoke uses a keyed meta-object shape to guard the real WooCommerce metadata normalization boundary that the 0.152.1 synthetic renderer test missed.

The mandatory `manual-delivery` regression group now also covers the 0.152.1 presentation hardening: custom manual labels must survive `ManualDeliveryCarrier -> CheckoutOrchestrator -> WooCommerceRateMapper`, manual pickup metadata must not set truthy `no_pickup_selection`, `CheckoutRateRenderer` must render the existing generic "Выбрать пункт выдачи" control, and a synthetic suppressed pickup rate still proves the generic `no_pickup_selection` contract remains intact.

The mandatory `manual-delivery` regression group now also covers 0.152.0 manual delivery type and pickup-point behavior: backward default courier, courier/custom/pickup `DeliveryType` mapping, pickup requirement flags, point repository CRUD/deactivation/service isolation, provider registration, trusted service-key query resolution, provider-side selection revalidation, pickup rate disappearance when no active eligible point exists, generic pickup session metadata, and architecture guards against manual REST/frontend/order-meta/shipment branches.

The mandatory `manual-delivery` shipment regression group covers the manual foundation, geography hardening, and 0.151.2 pricing engine: flat backward compatibility, flat save with missing/zero stale billing-step POST, zero-weight physical WooCommerce packages, per-kg billing steps/minimums, integer kopeck rounding, upper-inclusive weight ranges, gaps and zero-weight fail-closed behavior, packaging-policy weight source, Rule Engine/post-processing order, multiple manual services with independent modes, order-admin parity, shared quote-cache invalidation, and architecture guards against generic checkout, Packaging, Rule Engine, and Shipment Framework leakage.

The shipment regression manifest includes a mandatory `manual-delivery` group for the manual delivery foundation. It covers manual service CRUD, flat runtime quoting, trusted `service_key` selection, existing Rule Engine fallback semantics, presentation/order snapshot behavior, country availability modes, and architecture guards proving that this stage did not add manual Shipment Framework branches or providers.

0.149.2 adds Ozon pickup ready-transition race regression. Fake/local tests must prove that cancellation between the final pending-ID check and ready transition leaves the generation `cancelled`, does not activate or write failure diagnostics, and cannot be reversed by a stale completion step. The same coverage verifies the normal `95 enriched + 5 rejected` completion, pending/counter/conflict/zero-discovery guards, and activation rejection after `ready` remaining a true failure.

0.149.1 adds Ozon pickup cancellation-race regression: fake/local tests must verify discovery and enrichment cancellation between API response and repository commit, no retry/fail/activate after cancellation, stale scheduled step no-op, generation-to-lock-owner persistence, stop releasing only matching owners, missing-lock cancellation, start-after-stop behavior, and authoritative `downloaded_count === discovered_count` from unique staging rows.

0.149.0 extends Ozon Delivery pickup regression around the two-phase import. Required fake/local checks cover discovery-only `/v1/delivery-point/list`, frozen ID staging, duplicate/idempotent discovered IDs, transition to enrichment only after `next_cursor=null`, pending-ID enrichment batches of at most 100, `/info` HTTP 404 binary salvage with `not_found_404`, HTTP 200 missing-ID rejection with `info_missing`, retry reuse of the existing 2/5/10 second contract for transient list/info failures, completion invariants before activation, staging cleanup after activation/failure/cancellation, and manual stop race protection. Ozon smokes must remain fake/local and must not call production Ozon APIs.

0.135.10 extends PEK focused regression with fake/local-only proof that `services.sealing` is present in actual `/preregistration/submit/` bodies for both light and heavy cargo. Light 2999 g cargo sends `sealing.enabled=true` with payer type 1; 3000 g, 3001 g, 8800 g courier, and heavy pickup send `sealing.enabled=false` without payer. Checkout light-cargo surcharge eligibility remains aligned with the same product-weight threshold and does not depend on packaging weight or PEK quote service text. Architecture smoke also asserts that `PekShipmentStatusService` requires `ShipmentCreationAttemptService`, has no nullable/default lifecycle fallback, and terminalizes before repository deletion.

0.135.11 adds PEK status DateTime regression for the exact production `deliveryPlanDate` value `2026-08-19T22:23:16.833`: the normalizer still accepts the official `/cargos/status/` example `2021-08-19T00:00:00` and the previously supported strict formats, accepts only 3-digit millisecond `Y-m-d\TH:i:s.v` for `deliveryPlanDate`, preserves that original string, rejects other fractional lengths, impossible/fuzzy values, and the same millisecond shape on other status DateTime fields, and still raises `PekShipmentStatusNormalizationException` with safe `field`, `value_type`, and bounded scalar `value` for unsupported status date fields. The architecture guard forbids fuzzy `strtotime()`/generic `DateTimeImmutable` parsing and keeps the millisecond compatibility field-specific.

0.136.0 adds PEK status mapping admin coverage: empty SettingsRepository config reproduces every 0.135.11 default, `Прибыл` has separate pickup/courier defaults, the `Выдан мест N из M` pattern is configurable without exposing regex editing, invalid submitted universal statuses fall back to defaults, and admin rendering/saving uses the normal delivery-service tab and nonce path without PEK API calls. Safety regressions prove editable mappings do not control PEK cancellation recognition, accepted-cargo protection, already-cancelled reconciliation, or automatic cancelled cleanup.

Jet Logistic critical coverage is registered as the mandatory `jet-logistic` group. It covers API envelope handling, token redaction, CSV/geography basics, one-call two-rate quoting, discounted package goods cost and `D_SDOC`, terminal-city presentation, status mapping with compact events, manual attach, unsupported create, and local remove.

## Commands

```bash
php tests/shipments/run-shipment-regression-profile.php --list
php tests/shipments/run-shipment-regression-profile.php --group=framework
php tests/architecture/run-plugin-architecture-smoke.php
php tests/shipments/run-shipment-regression-profile.php --group=cdek
php tests/shipments/run-shipment-regression-profile.php --group=dpd
php tests/shipments/run-shipment-regression-profile.php --group=russian-post
php tests/shipments/run-shipment-regression-profile.php --group=yandex
php tests/shipments/run-shipment-regression-profile.php --group=pek
php tests/shipments/run-shipment-regression-profile.php --group=ozon-delivery
php tests/shipments/run-shipment-regression-profile.php --group=status-core
php tests/shipments/run-shipment-regression-profile.php
```

Also run `php -l` for changed PHP files, `node --check` for changed JS files, docs link checks, and `git diff --check`.

Checkout pickup map lifecycle changes should also run `node tests/checkout/run-pickup-map-lifecycle-smoke.js`. This smoke executes the frontend map controller with fake providers/API and protects one-shot auto-fit, user interaction cancellation, provider pending-fit cancellation, coordinate validation, and the checkout pickup inline-notice latch. The latch test verifies that a rejected selected pickup point message is captured from the one-render server event, restored through repeated `updated_checkout` DOM replacements for the same family/destination, survives stale local selected-point memory until `/checkout/state` answers, clears only after authoritative state confirms a valid selected point, remains visible when authoritative state returns an empty selection bucket, clears on destination/method/reload, and is never written to browser storage.

Checkout city selector changes should also run `node tests/checkout/run-checkout-city-selector-smoke.js`. This smoke executes the frontend selector with a minimal DOM/timer harness and protects selected local-location field application, canonical hidden fields, country-change clearing, first-load and same-country no-op behavior, active shipping/billing scope handling, and the single `update_checkout` recalculation contract.

DPD Geography browser-import lifecycle changes should also run `node tests/dpd/run-dpd-geography-import-runner-smoke.js` in addition to `php tests/dpd/run-dpd-geography-import-smoke.php`. The Node smoke executes the admin runner with a fake DOM/fetch/timer harness and protects the read-only status endpoint contract, sequential step scheduling, network recovery through status, busy/stale step handling, legacy runner reset-required handling, monotonic cross-job `state_revision` rendering, foreign counter rendering, and the no-`setInterval` source guard. The PHP geography smoke owns the server lock/start/reset contract, including atomic compare-and-delete lease races, failed-artifact reset requirements, SFTP start continuation, legacy job refusal before reset, and RU DPD matcher regressions where own `fias_id` must not be hidden by another row's `city_fias_id`. `php tests/dpd/run-dpd-location-index-smoke.php` covers the separated own/city FIAS buckets, lazy own-FIAS disambiguation, true own-FIAS ambiguity, city-FIAS fallback, and the compact no-`location_meta` index contract.

## Manifest

The manifest is `tests/shipments/regression/shipment-regression-manifest.php`. Shipment and carrier smoke tests that protect framework contracts should appear there.

PEK foundation is registered as mandatory `pek.foundation` in group `pek`; PEK admin routing, admin UI rendering, warehouse datetime correctness, and the fixed `RU -> RU/AM/BY/KG/KZ` international runtime policy are registered as mandatory `pek.admin-routing`, `pek.admin-ui`, `pek.warehouse-datetime`, and `pek.international-runtime`. Run them directly with `php tests/pek/run-pek-foundation-smoke.php`, `php tests/pek/run-pek-admin-routing-smoke.php`, `php tests/pek/run-pek-admin-ui-smoke.php`, `php tests/pek/run-pek-warehouse-datetime-smoke.php`, and `php tests/pek/run-pek-international-runtime-smoke.php`, or through `php tests/shipments/run-shipment-regression-profile.php --group=pek`. The smokes use fake HTTP only, verify no production network calls are made, assert `/typesOfDelivery/all/` is called with GET, PEK JSON headers, and no body while branch/legal/warehouse methods use POST, check independent diagnostics after endpoint 403, validate `connection_ok` versus `all_checks_passed`, verify selected-warehouse operational diagnostics are read-only, separate `/branches/all/` API availability from informational saved-ID matching, cover positive and mismatch warehouse matcher cases with safe counters, check official `shortName`/`codeByClassifier` country diagnostics, protect stale warehouse search cache clearing, user-scoped one-shot notices, PEK closing-date validation, branch timezone handling from `/branches/all/` `timezone` and `/branches/nearestdepartments/` `timeZone`, strict future impossible-date rejection, real Unix timestamp comparison without `current_time('timestamp')`, mbstring-independent cargo acceptance matching, nested diagnostic rendering, machine datetime sanitation before phone redaction, required `PekCountryPolicy` DI, no hidden policy construction, international auto-create/cancel mutation guards, and PEK registration only in the intended checkout/shipment registries.

PEK geography and destination pickup foundation are registered as mandatory `pek.geography`, `pek.pickup-provider`, and `pek.schema-integrity` in group `pek`, and the generic pickup provider registry contract is registered as mandatory `framework.pickup-provider-registry` in group `framework`. Run them directly with `php tests/pek/run-pek-geography-smoke.php`, `php tests/pek/run-pek-pickup-provider-smoke.php`, `php tests/pek/run-pek-schema-integrity-smoke.php`, and `php tests/pickup/run-carrier-pickup-provider-registry-smoke.php`. These smokes verify coordinate-first and address-fallback PEK location resolution, mapping fingerprints with an internal contract revision, rejection of legacy fingerprints, persisted mapping structural validation before fresh hit/stale fallback, lazy refresh of legacy address mappings with `warehousePoint` coordinates or missing `mainWarehouseId`, fingerprint-matched stale fallback only for current-contract usable rows, `warehousePoint` exclusion from destination coordinates, typed coordinate/address zone root contracts, strict `GeoData`/`GeoData.Address`/`formatted` validation, official-only `GeoData.precision`, `mainWarehouseId` requirement for address exact/near mappings, top-level address alias rejection, business unsupported versus API contract failure, strict critical zone field and malformed country rejection without overwriting working mappings, WordPress-timezone mapping freshness, documented destination `/branches/nearestdepartments/` JSON-list collections with `departmentOperation=3` and `type=3`, non-empty address on every destination terminal payload, PEK decimal-string outgoing coordinates as an additional coordinate-search field, address-only payloads without coordinates, full-payload terminal fingerprinting, blank-address fail-closed behavior before API calls, production validation regression for required address, safe response-shape diagnostics for malformed terminal responses, query coordinate-pair validation, partial/invalid canonical coordinate fallback, PEK canonical `location_id` requirement, project-unit to PEK-unit conversion including multi-place volume, strict free/paid terminal row and schedule normalization, positive limit enforcement with malformed limits rejected as invalid rows, all-invalid collection failure without cache/repository writes, rejection reason counters by allowlist, mixed valid/invalid counters, terminal cache format `2`, invalidation/deletion of legacy format `1` transients, safe point projection including successful empty-result hits, fresh selection validation without an opt-out flag, current-search-only `last_report` after resolver and terminal API exceptions, destination diagnostic report clearing/sanitization/readable section rendering/safe logging, redacted `api_error_message` and normalized `field_errors` rendering/logging for PEK logical errors with credential redaction, escaping, control-character normalization, length limits, duplicate-field merging, rejected-value exclusion, and empty-message fallback, `PickupPoint::city` semantics, SQL failure handling, PEK schema integrity recovery for partial/missing tables, reserved-identifier protection through physical `mapping_precision`, legacy `` `precision` `` backfill via migration `0051`, migration failure marker/version preservation, no runtime PEK `dbDelta`, no public REST wiring, and fake HTTP only.

PEK quote foundation is mandatory as `pek.quote-foundation` in group `pek` and runs with `php tests/pek/run-pek-quote-foundation-smoke.php`. It verifies typed `POST /calculator/calculateprice/`, calculator-owned root `hasError` handling as `pek_quote_root_error`, generic non-calculator `pek_has_error` preservation, safe response metadata propagation, `currencyCode=643`, `types=[3]`, pickup and courier payloads, configured Russian sender warehouse, mandatory insurance from `Package::declared_value`, counterpart/client-card fields, one aggregate cargo place, outgoing PEK cargo always using `isHP=false` and `sealingPositionsCount=0`, configurable store light-cargo surcharges with defaults 70/20/3000, strict `<` eligibility based on `Package::weight_g` without packaging weight, total calculator weight from `Package::total_weight_g`, separate carrier/final prices, zero/custom surcharge behavior, returned carrier service breakdown without text matching or manual summing, upward weight/dimension/volume rounding, strict LTL `transfers[].type=3` and Boolean `transfers[].hasError` parsing, cost-to-kopecks normalization, delivery-time validation, safe services with string-only text fields and Boolean `insuranceTerm`, redaction of actual credentials/counterpart values from API messages plus field-error names/messages, preservation of canonical field paths, duplicate sanitized-field merging, minimized logger context without API/field messages, logical field errors, unsupported-direction fail-closed behavior, deprecated-field absence, and fake HTTP only.

PEK checkout runtime is mandatory as `pek.checkout-runtime` in group `pek` and runs with `php tests/pek/run-pek-checkout-runtime-smoke.php`. The smoke covers the real WooCommerce rate projection shape (`DeliveryRate` to `WooCommerceRateMapper` to stored session rate), where carrier/service/family flags live on the top-level envelope and trusted PEK provider context lives in nested `rate_meta['pickup_provider_query']`. It also covers a separate REST lifecycle that starts without `WC()->session`, bootstraps the same WooCommerce customer session, and restores stored `wdc_platform_rates` before resolving PEK pickup provider context. It accepts both numeric coordinate snapshots and address-only `null/null` snapshots, rejects missing `rate_meta`, wrong carrier/service/family, missing service key, courier rates, non-Boolean pickup requirements, empty destination fingerprints, partial/empty/non-numeric/out-of-range coordinates, browser-forged cargo/location/family values, verifies PEK free/paid public point titles and UUID filtering, preserves provider destination fingerprint separately from the generic checkout location fingerprint, proves selected terminal recalculation changes `receiverWarehouseId`/price/cache key, verifies selected-terminal calculator failure recovers to a preliminary PEK rate with one-calculation generic rejection metadata, verifies PEK adjusted `api_base_price_rub` versus carrier-only `pek_carrier_base_price_rub`, and verifies request-scoped plannedDateTime memoization keeps PEK quote cache context and calculator payload aligned.

`php tests/orders/run-delivery-calculation-data-builder-smoke.php` covers saved calculation-data semantics for PEK store base adjustments. It verifies that PEK bag/plombing surcharges appear as formula comments for both, bag-only, and plombing-only cases, do not appear for zero surcharge configurations, render even without normal rules, and do not change Rule Engine `applied_rules` or `price_delta_rub`.

The plugin architecture smoke is `framework.plugin-architecture`; it is registered in the framework group and checks architecture boundaries rather than carrier business behavior. It favors dynamic Reflection-based checks where the contract is inspectable. Where runtime proof would require changing production construction, the smoke checks a narrower contract instead: registry duplicate behavior is tested with stubs, providers are discovered without uninitialized method calls, and composition-root ownership is checked for the current `Container::register()` wiring pattern.

Allowed matrix values:

- `contract`: shared framework contract smoke.
- `carrier smoke`: required carrier-specific smoke.
- `partial`: covered, but not all variants are represented.
- `baseline`: known failing baseline entry.
- `optional`: optional environment-dependent entry.
- `N/A`: capability does not apply.

## Coverage Matrix

| Capability | Framework | CDEK | DPD | Russian Post | Yandex | PEK |
| --- | --- | --- | --- | --- | --- | --- |
| architecture/foundation | `contract`: `framework.plugin-architecture` | `N/A` | `N/A` | `N/A` | `N/A` | `carrier smoke`: `pek.foundation` |
| admin routing | `N/A` | `N/A` | `N/A` | `N/A` | `N/A` | `carrier smoke`: `pek.admin-routing` |
| admin UI rendering | `N/A` | `N/A` | `N/A` | `N/A` | `N/A` | `carrier smoke`: `pek.admin-ui` |
| warehouse availability | `N/A` | `N/A` | `N/A` | `N/A` | `N/A` | `carrier smoke`: `pek.warehouse-datetime` |
| PEK schema integrity | `N/A` | `N/A` | `N/A` | `N/A` | `N/A` | `carrier smoke`: `pek.schema-integrity` |
| pickup provider registry | `contract`: `framework.pickup-provider-registry` | `N/A` | `N/A` | `N/A` | `N/A` | `carrier smoke`: `pek.pickup-provider` |
| geography mapping | `N/A` | `N/A` | `N/A` | `N/A` | `N/A` | `carrier smoke`: `pek.geography` |
| quote foundation | `N/A` | `N/A` | `N/A` | `N/A` | `N/A` | `carrier smoke`: `pek.quote-foundation` |
| create | `contract`: `framework.persistence-mappers` | `carrier smoke`: `cdek.order-creation` | `carrier smoke`: `dpd.create-order` | `carrier smoke`: `russian-post.shipments` | `carrier smoke`: `yandex.shipment-framework` | `N/A` |
| preview | `contract`: `framework.admin-ajax` | `carrier smoke`: `cdek.order-creation` | `carrier smoke`: `dpd.create-order` | `carrier smoke`: `russian-post.shipments` | `carrier smoke`: `yandex.shipment-payload` | `N/A` |
| persistence | `contract`: `framework.persistence-mappers` | `carrier smoke`: `cdek.order-creation` | `carrier smoke`: `dpd.create-order` | `carrier smoke`: `russian-post.shipments` | `carrier smoke`: `yandex.shipment-framework` | `N/A` |
| status update | `contract`: `framework.status` | `carrier smoke`: `cdek.order-creation` | `carrier smoke`: `dpd.status-mapping` | `carrier smoke`: `russian-post.shipments` | `carrier smoke`: `yandex.shipment-framework` | `N/A` |
| autosync/polling | `contract`: `framework.lifecycle-contract` | `partial`: `cdek.order-creation` | `carrier smoke`: `dpd.status-autosync` | `carrier smoke`: `status.status-autosync` | `carrier smoke`: `yandex.shipment-framework` | `N/A` |
| lifecycle continuation | `contract`: `framework.lifecycle-contract` | `partial`: `cdek.order-creation` | `carrier smoke`: `dpd.shipment-lifecycle` | `N/A` | `carrier smoke`: `yandex.shipment-framework` | `N/A` |
| cancel/remove | `contract`: `framework.admin-ajax` | `partial`: `cdek.order-creation` | `carrier smoke`: `dpd.shipment-buttons` | `carrier smoke`: `russian-post.cancel` | `carrier smoke`: `yandex.shipment-framework` | `N/A` |
| manual attach | `contract`: `framework.admin-ajax` | `partial`: `cdek.order-creation` | `partial`: `dpd.shipment-buttons` | `carrier smoke`: `russian-post.cancel` | `carrier smoke`: `yandex.shipment-framework` | `N/A` |
| documents | `contract`: `framework.document-actions` | `carrier smoke`: `cdek.order-creation` | `carrier smoke`: `dpd.documents` | `carrier smoke`: `russian-post.documents` | `carrier smoke`: `yandex.shipment-framework` | `N/A` |
| tracking presentation | `contract`: `framework.status` | `carrier smoke`: `cdek.order-creation` | `carrier smoke`: `dpd.status-mapping` | `carrier smoke`: `russian-post.shipments` | `carrier smoke`: `yandex.shipment-framework` | `N/A` |
| actual cost | `contract`: `framework.actual-cost-presentation` | `carrier smoke`: `cdek.order-creation` | `carrier smoke`: `dpd.event-sync` | `carrier smoke`: `russian-post.price` | `carrier smoke`: `yandex.shipment-framework` | `N/A` |
| modal | `contract`: `framework.modal-extensions` | `carrier smoke`: `cdek.order-creation` | `carrier smoke`: `dpd.create-order` | `carrier smoke`: `russian-post.shipments` | `carrier smoke`: `yandex.shipment-framework` | `N/A` |
| AJAX wiring | `contract`: `framework.admin-ajax` | `carrier smoke`: `cdek.order-creation` | `carrier smoke`: `dpd.create-order` | `carrier smoke`: `russian-post.cancel` | `carrier smoke`: `yandex.shipment-framework` | `N/A` |
| JS structure | `contract`: `framework.admin-js-structure` | `carrier smoke`: `cdek.order-creation` | `carrier smoke`: `dpd.create-order` | `partial`: `russian-post.cancel` | `carrier smoke`: `yandex.shipment-framework` | `N/A` |
| pickup/courier | `partial`: `core.checkout-location-picker` | `baseline`: `baseline.cdek-pickup-points` | `carrier smoke`: `dpd.create-order` | `carrier smoke`: `russian-post.pickup-import` | `carrier smoke`: `yandex.pickup-selection` | `N/A` |
| allocation | `contract`: `framework.allocation` | `carrier smoke`: `cdek.order-creation` | `carrier smoke`: `dpd.create-order` | `carrier smoke`: `russian-post.shipments` | `carrier smoke`: `yandex.shipment-framework` | `N/A` |
| source station/dropoff | `contract`: `framework.admin-ajax` | `N/A` | `partial`: `dpd.create-order` | `N/A` | `carrier smoke`: `yandex.source-station` | `N/A` |
| error paths | `contract`: `framework.admin-ajax` | `carrier smoke`: `cdek.order-creation` | `carrier smoke`: `dpd.create-order` | `carrier smoke`: `russian-post.shipments` | `carrier smoke`: `yandex.shipment-framework` | `N/A` |

## Baseline And Optional

Current baseline/optional allowances are active, not historical:

- `baseline.dpd-shipment-preparation`
- `baseline.cdek-pickup-points`
- `optional.russian-post-carrier`
- `optional.russian-post-domestic-carrier`

Reasons are tracked in [technical-debt.md](../operations/technical-debt.md).
